# Web API 依赖(Web UI 重构)
# 与 requirements.txt 合并使用: pip install -r requirements.txt -r requirements-web.txt
# 注: requirements.txt 中的 PyQt6/pyqtdarktheme 将在阶段 4 收尾时移除

# --- Web 后端 ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12          # /api/analyze multipart 文件上传
sse-starlette>=2.1.3              # Server-Sent Events 流式推送
pydantic>=2.9.0
pydantic-settings>=2.5.0
