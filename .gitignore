# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
graft/.cache/
graft/.graph/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Virtual env
venv/
.venv/

# Runtime artifacts
logs/
cache/
crash_log.txt
crash_debug.txt

# Agent tools
.agents/
.claude/
.cline/
.codebuddy/
.codegraph/
.code-review-graph/
.continue/
.junie/
.kiro/
node_modules/
config/history.db
config/chroma_db/
config/app_config.ini
config/skills_state.json

# website/ 官网源码 —— 作为本仓库的一部分纳入版本控制，
# 以便 clone 者能拿到完整 Next.js 源码（之前被排除导致 README 截图外无源码交付）。
# 仅排除其构建产物与 node_modules。
website/node_modules/
website/.next/
website/out/
website/.env*.local
.env
E2E实测结果/
