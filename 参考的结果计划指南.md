# 参考的结果计划指南 — Video Analysis Pro 跨项目对标与扩展路线

> **单一状态源（Single Source of Truth）·Phase A 分析专用**。
> 本版为 **v4 修正版（2026-09-04，经独立 Critic 审查 FAIL → 修复后重写）**。
> v3 版存在三处事实错误（skills 机制误判"完全缺失"、headless 鉴权误判"无鉴权"、v5.0 提交状态误判），均已在 v4 修正并附实测证据。
> **重要背景**：会话进行中发现主项目工作区仍有活跃写入（src/skills/* 文件时间戳 17:26-17:32，晚于本会话开始），疑似另一并行会话在推进 v5.0 后续批次。Phase B 启动前必须与用户协调工作区归属。

---

## 0. 决策摘要

| 项 | 结论 |
|----|------|
| 任务复杂度 | 深度型（主项目 v5.0 现状重评 + CL4R1T4S 28 厂商提示词深读 + 参考根2 ~450 项目筛选 + 扩展面 skills 可行性评估） |
| 判断理由 | 主项目 v5.0 实际进度远超预期：agent_prompt/llm_gateway/surveillance/rtsp/**skills 骨架**/headless 鉴权均已落地（多数已 committed）。剩余瓶颈：① **P0 新发现——seaborn 缺失致 ADVANCED_FEATURES_AVAILABLE=False，Phase 3 摘要媒体全链路被禁用**；② Agent 提示词深度（缺 Loop/thought/gate）；③ 黑匣子展示层截断；④ skills/surveillance Tab 已写未接线；⑤ 扩展面 skills 包缺失 |
| 当前阶段 | **Phase A — ANALYSIS_ONLY（分析完成并经独立审查修正，待用户审批 Phase B）** |
| 当前授权 | 只读分析主项目 + 参考根1（8 项目）+ 参考根2（~450 项目，深读 30 个）+ CL4R1T4S 28 厂商提示词；允许创建/更新本文件；**禁止改源码/配置/依赖** |
| 成功标准 | 9 节分析报告（识别→现状→亮点→差距→迁移分类→P0/P1/P2 路线图→实施方案→审批清单→待补信息），全部结论带实测证据 |
| 停止条件 | 报告完成并等待用户确认实施范围，不自动进入 Phase B |
| 已知约束 | ① 参考项目脚本/提示词仅作分析材料，非执行授权；② MCP 图谱服务连接失败，改用直接扫描 + 三 Explore 子代理并行 + 独立 Critic 复验；③ **工作区归属不明**（skills 等文件在本会话期间被另一进程写入），Phase B 前须协调 |
| 关键未知项 | ① 扩展面落地形态（可选 skills 包 vs 内置）；② skills/surveillance Tab 接线与死代码（llm_gateway 判图引擎已被 surveillance_tab 使用）决断；③ 工作区并行会话协调 |
| 最脆弱假设 | "升级 agent_prompt 补 Loop/thought/gate 即可显著提升智能度"——本地小模型推理能力有限，需最小实验验证（v4 已补 3 问题+阈值设计） |
| 预计工作流 | Phase B 按 B0→B4 批次实施，每批独立验证 + Critic 复验 |
| 主要验证方式 | pytest（91 collected 实测）/ mypy / E2E 冒烟 / 本地 qwen2.5-vl 最小对比实验 |
| 当前阻塞项 | 无硬阻塞；待用户审批批次 + 确认工作区归属 |

---

## 1. Task Contract

- **原始目标**：基于主项目（Video Analysis Pro v5.0）与两个参考根路径，自动完成项目识别、同类筛选、优点提炼、差距分析、优化方案设计；用户确认后逐步实施改造。核心诉求：① 让 agent 更完美、小白易用；② 黑匣子日志全开、让用户理解产品+学习背后原理；③ 沉淀用户自己的 skills；④ 扩展性延伸（图片/视频生成、电商、PPT 等，不局限软件项目）；⑤ 深度参考 CL4R1T4S 顶级模型系统提示词。
- **当前阶段**：Phase A — ANALYSIS_ONLY（v4 修正版）
- **当前授权**：只读 + 本文件
- **成功标准**：9 节完整报告 + 本文件
- **停止条件**：报告完成，等待 Phase B 批准

---

## 2. Task Graph

| ID | Agent/Owner | Goal | Dependencies | Deliverable | Validation | Status |
|----|-------------|------|--------------|-------------|------------|--------|
| T0 | Orchestrator | 工作区检查 + 状态文件建立 | - | 本文件 | git status/diff 已查 | DONE |
| T1 | MainProjectMapper | 主项目 v5.0 现状重评（agent_prompt/llm_gateway/surveillance/rtsp/skills/headless 逐项实测） | - | 结构化摘要 + 真实瓶颈清单 | file:line 证据 | DONE |
| T2 | DiffAndRiskAuditor | 未提交 diff 风险检查（v4 修正：区分 committed/untracked） | - | 工作区改动清单 | git status --porcelain | DONE |
| T3a-h | Scout×8（参考根1） | 8 个视觉参考项目亮点（沿用旧指南） | - | 项目卡片×8 | 旧指南已 DONE | DONE（沿用） |
| T4a | Scout: CL4R1T4S | 28 厂商提示词深读（ANTHROPIC/OPENAI/MANUS/DEVIN/REPLIT/CURSOR/XAI/GOOGLE 8 家） | - | 15 可迁移模式 + 8 反面模式 + 升级 3 重点 | 子代理报告 | DONE |
| T4b | Scout: 参考根2 agent 扩展类 | 深读 9 个（video-use/OpenHands/mem0/superpowers/skills-manager/ECC/ClaudeSkills/Agent-Skills-CE/OpenKB） | - | 项目卡片×9 + skills 体系落地方案 | 子代理报告 | DONE |
| T4c | Scout: 参考根2 扩展面类 | 21 个图片/视频/PPT/电商/TTS 项目评估 | - | 项目卡片×21 + v1 扩展包 3 skill 推荐 | 子代理报告 | DONE |
| T5 | SynthesisArchitect | 评分筛选、差距分析、路线图 | T1-T4 | 分析报告 v3 | 逐项证据 | DONE（v3 有误） |
| T5b | Critic（独立审查） | 六方面复验 v3 报告（需求完整/逻辑/边界/质量/测试/运行） | T5 | 审查报告（总评 FAIL + 3 BLOCKER/MAJOR） | 逐条实测复现 | DONE |
| T5c | Builder（报告修复） | 按 Critic 意见修正 v3 → v4 | T5b | 本文件 v4 | 修正点逐条实测（ls src/skills、grep _check_auth、git log、pytest collect+run） | DONE |
| T6 | User Gate | 用户确认实施范围 + 工作区归属 | T5c | 批准批次清单 | 用户回复 | PENDING |

---

## 3. Project Inventory

### 3.1 主项目识别摘要（v5.0 实测重评，v4 修正版）

- **类型**：单体桌面应用（PyQt6 GUI），本地 AI 视频分析工具
- **技术栈**：Python 3.10+ / PyQt6 / OpenCV / ultralytics(YOLO11) / faster-whisper / MoviePy 2.x / sentence-transformers(CLIP) / ChromaDB / SQLite / requests(Ollama+OpenAI 格式) / keyring
- **规模**（实测）：src/core 10 py（logic.py 1407、agent_tools.py 380、history_manager.py 315、kb_indexer.py 97、agent_prompt.py 89、llm_gateway.py ~400、surveillance_agent.py、rtsp_stream.py）+ **src/skills 4 py**（schema 40 / loader 153 / state 72 / __init__ 17）；src/ui 12 py（main_window.py 2405、agent_panel、skills_manager_tab 214、surveillance_tab 307 等）；src/server/headless.py；src/utils 4 py；tests 15 文件 **91 collected（pytest --collect-only 实测）**
- **架构**：三阶段流水线 + Agent 面板（ChatWorker ReAct，messages 化）+ 模型管理（显存互斥）+ 跨视频 KB（ChromaDB kb_frames）+ 全协议 LLM 网关（llm_gateway）+ 监控/RTSP（surveillance_agent/rtsp_stream + surveillance_tab）+ **skills 机制骨架（src/skills + skills_manager_tab）**

#### 3.1.1 v5.0 已落地能力清单（逐项实测，证据含 git log 提交号）

| 能力 | 状态 | 证据 | 提交状态 |
|------|------|------|---------|
| agent_prompt.py 六模块（identity/capabilities/rules/tool_use/context/output） | ✅ 已建+已接线 | agent_prompt.py:1-89；main_window.py:1166-1174 send_to_agent 调用 | committed `eef2cce` |
| ChatWorker messages 化 + args 按 schema 首参解析 | ✅ 已落地 | main_window.py:191-277（B2 注释 204、260-264；schema 解析 244-252） | committed `eef2cce` |
| llm_gateway.py 四协议后端 + GatewayRouter | ✅ 已建；**surveillance_tab 已使用 build_backend 作判图引擎**，主聊天链路未接 | llm_gateway.py:1-400；surveillance_tab.py:6 提及 | committed `1b3fa78` |
| surveillance_agent.py + rtsp_stream.py | ✅ 已建 | surveillance_tab.py docstring：接线 RtspMonitor + SurveillanceAgent | committed `213cf57` 等 |
| surveillance_tab.py（监控 Tab UI） | ✅ **已写未接线** | src/ui/surveillance_tab.py:131-138 SurveillanceTab 类；main_window.py:876-944 仅 7 个 addTab，无 surveillance | **untracked** |
| skills 机制骨架（schema/loader/state + skills_manager_tab + builtin skill + 3 单测） | ✅ **已建，Tab 未接线进 main_window** | src/skills/loader.py:121-153 load_skills；config/skills/builtin-video-summary/SKILL.md；main_window addTab 无 skills | **untracked**（本会话期间写入） |
| headless Bearer Token 鉴权（VAP_HEADLESS_TOKEN） | ✅ **已实现（可选，默认关闭）** | headless.py:145-162 _check_auth（hmac.compare_digest 防时序攻击） | **modified 未提交**（.env.example 同步） |
| search_by_image 第9工具 | ✅ 已落地 | agent_tools.py:335-380 + main_window.py:1755-1763 | committed |
| user_preferences collection（remember/recall） | ✅ API 已建；**Agent 未调用 recall 个性化** | history_manager.py:177-219；grep recall_preferences src/ui/ agent_prompt.py 均 空 | committed |
| OllamaClient num_ctx 可配 | ✅ 已落地 | logic.py:685-693（VAP_OLLAMA_NUM_CTX） | committed |
| config 默认 api_url 中立空 | ✅ 已落地 | config_manager.py:75 | committed |
| highlight_cut 词频打分 | ✅ 已落地 | agent_tools.py:226-269 | committed |
| search_visual shared_embedder + 帧缓存 | ✅ 已落地 | agent_tools.py:167-202 + kb_indexer.py:25-43 | committed |
| **Phase 3 摘要媒体全链路** | ❌ **生产 venv 中被禁用（P0 新发现）** | `_detect_advanced_features()`（logic.py:63-78）要求 seaborn，venv 无 seaborn 且 requirements.txt 未声明 → ADVANCED_FEATURES_AVAILABLE=False → MediaWorker 跳过 → E2E `media_clips: 0 clips` FAIL | **bug** |

#### 3.1.2 Git 工作区实际状态（v4 修正，git status --porcelain 实测）

- **committed（勿误判为未提交）**：agent_prompt / llm_gateway / surveillance_agent / rtsp_stream / search_by_image / user_preferences / num_ctx / highlight 词频 / search_visual 缓存 / kb_indexer 图像 embedding 修复
- **modified 未提交**：.env.example、SPEC.md、src/server/headless.py（+_check_auth）、tests/test_headless_server.py、website/*（4 改 1 删）、本文件
- **untracked**：config/skills/、src/skills/、src/ui/skills_manager_tab.py、src/ui/surveillance_tab.py、tests/test_skills_loader.py、tests/test_surveillance_tab.py、website/（error/loading/not-found/ui/lib）
- **⚠ 工作区归属警告**：src/skills/* 文件 mtime 17:26-17:32（本会话进行中），且 headless 鉴权与 surveillance_tab 同期出现——疑似另一并行会话在推进 v5.0 批次。Phase B 前必须由用户确认归属与基线。

#### 3.1.3 实测测试基线（v4 修正：91 collected / 90 passed 1 failed）

- `pytest --collect-only -q` 实测：**91 tests collected**（15 文件：agent_tools 8、api_clients 11、api_gateway_stream 7、core_pipeline 9、e2e_full 1、e2e_smoke 1、headless_server 10、history_manager 8、llm_gateway 9、model_manager 5、rtsp_stream 5、skills_loader 3、surveillance_e2e 2、surveillance_tab 3、ui_components 9）
- `pytest tests/ -q` 全量实测（226.56s）：**90 passed, 1 failed**
- **唯一失败**：`test_e2e_full_pipeline::test_full_pipeline` —— `media_clips: 0 clips` + `summary_video` FAIL。**根因已定位（P0）**：logic.py:63-78 `_detect_advanced_features` 把 seaborn 列为必需依赖，venv 未装、requirements.txt 未声明 → ADVANCED_FEATURES_AVAILABLE=False → MediaWorker 直接跳过摘要媒体。**这同时意味着生产用户 Phase 3（🎬 生成摘要媒体）按钮实际不可用**。

### 3.2 参考项目根1（视觉参考项目）候选清单（沿用旧指南，8 个全部有效）

| # | 项目 | 类型 | 相似度 | 评级 |
|---|------|------|--------|------|
| 1 | video-analyzer | CLI 视频分析（同域） | ★★★★★ | 高价值 |
| 2 | claude-video-vision | Claude 视频感知插件+MCP | ★★★★ | 高价值 |
| 3 | video-search-and-summarization | NVIDIA VSS 蓝图 | ★★★ | 高价值（架构） |
| 4 | edit-mind | 本地视频知识库+插件架构 | ★★★ | 高价值 |
| 5 | VibeNVR | NVR 监控系统 | ★★ | 高价值（扩展） |
| 6 | yolo-training-template | YOLO 训练模板 | ★★ | 中价值 |
| 7 | VideoPipe | C++ pipeline 框架 | ★ | 中价值（思想） |
| 8 | Deep-Learning-Experiments | 教学材料 | ★ | 低价值 |

### 3.3 参考项目根2（智能渗透/参考项目，~450 个）高价值候选清单

> 三 Explore 子代理并行扫描筛出 **21 强相关 + 9 agent 工程化核心**。子代理级结论（标注为 INFERENCE-AGENT 级证据，Phase B 实施前对个别项做最小复验）。

#### A. CL4R1T4S（诉求⑤核心——28 厂商 103 提示词文件，.md/.txt/.json）
- 代表深读：ANTHROPIC（Claude_Sonnet_3.5/OPUS-5 204K）、OPENAI（Codex_Sep-15/ChatGPT5）、MANUS（Manus_Prompt 14K+Functions 26K）、DEVIN（Devin2 51K）、REPLIT、CURSOR（2.0 23K）、XAI（Grok4）、GOOGLE（Gemini-2.5-Pro）
- 15 可迁移模式 + 8 反面模式（§7.1）· 评级 ★★★★★

#### B. agent 工程化核心 9 项目（诉求①②③④）
| # | 项目 | 核心机制 | 诉求匹配 | 评级 |
|---|------|---------|---------|------|
| B1 | video-use | Ask→confirm→execute→self-eval→persist + Hard Rules vs 艺术自由 + project.md 会话记忆 | ①②③④ | ★★★★★ |
| B2 | OpenHands | CodeActAgent + microagent 三类型触发 + system_prompt.j2 分段 + condenser 压缩 | ①②③ | ★★★★★ |
| B3 | mem0 | ADD-only 累积记忆 + 多信号检索 + temporal reasoning | ③ | ★★★★ |
| B4 | superpowers | 14 skills TDD 闭环 + writing-skills 元 skill + SKILL.md frontmatter 规范 | ②③ | ★★★★★ |
| B5 | skills-manager | Tauri+React skills 管理 GUI 五元组（Library/Preset/Workspace/Activity/Backup） | ③ | ★★★★★ |
| B6 | ECC | 286 skills 扁平分类 + contexts 三模式行为档位 + agent.yaml 声明清单 | ②④ | ★★★★ |
| B7 | ClaudeSkills | SKILL.md 编写标准 + install_skill.py + validate.yml CI 校验 | ③ | ★★★★ |
| B8 | Agent-Skills-for-Context-Engineering | Progressive Disclosure + context-degradation 教学素材 | ②③ | ★★★★★ |
| B9 | OpenKB | 四层 wiki（concepts/entities/summaries/sources） | ③ | ★★★★ |

#### C. 扩展面项目（诉求④——图片/视频/PPT/电商/TTS）
| # | 项目 | 域 | 核心能力 | 集成成本 | 评级 |
|---|------|----|---------|---------|------|
| C1 | FunClip | 视频剪辑 | FunASR 本地 ASR + 文本/说话人定向剪辑，纯 Python+torch+moviepy | 低（但 funasr torch 版本需与主项目对齐，待验证） | ★★★★ |
| C2 | EpicInfographics | 信息图 | brief→PNG，色盲校验 | 低（许可协议待 file:line 复验） | ★★★★ |
| C3 | LuxTTS | 轻量 TTS | <1GB VRAM，150x 实时，CPU 可跑 | 低（zipvoice 许可待复验） | ★★★★ |
| C4 | CosyVoice | 语音克隆 | 9 语种+18 方言，0.5B 需 GPU | 中 | ★★★ |
| C5 | MoneyPrinterTurbo | 短视频生成 | 主题→脚本→素材→配音→成片 | 中（默认云端 API，需本地化改造） | ★★★ |
| C6 | dashi-ppt-skill | PPT | HTML→PPTX 可编辑 | 中（Node 异构+AGPL） | ★★★ |
| C7 | 100x-skill-tiktok | TikTok 运营 | 8 可验证 skill | 低 | ★★★ |
| C8 | MultiPost-Extension | 多渠道发布 | 一键发 10+ 平台 | 中高（异构+合规） | ★★ |
| C9 | Face-Recognition-SDK | 人脸识别 | 100% 本地 | 低 | ★★★★ |

> v4 修正：C1-C3 的许可协议与依赖矩阵在 v3 中未经 file:line 复验（Critic 指出 UNVERIFIED），已降半星并在 §13 列为 Phase B 前置复验项。

#### 排除/降级说明（参考根2）
- ~420 项目未列入：安全渗透类域不匹配（仅借"权限边界+审计日志"方法论→Agent 工具护栏）；captcha 类借"多 provider 容错+降级链"→llm_gateway 故障转移；-main 镜像重复；CLI/harness 栈差异大；token 优化类价值低。
- **不相关项目的深层价值**：方法论迁移而非功能移植——安全类→护栏范式；captcha 类→故障转移范式（llm_gateway 的 GatewayRouter 故障转移队列正是这一思想的落地印证）。

---

## 4. Evidence Ledger（v4 修正：四级标签）

> 标签：**VERIFIED**（实际运行/命令输出）· **STATIC**（读代码确认）· **INFERENCE**（多证据推断）· **UNVERIFIED**

| Claim | Evidence | Type | Confidence |
|-------|----------|------|------------|
| skills 机制骨架已建（schema/loader/state/UI/builtin/3 单测） | ls src/skills/（4 py）+ ls config/skills/builtin-video-summary/ + read loader.py:121-153 | VERIFIED | 高 |
| skills/surveillance Tab 未接线进 main_window | grep addTab main_window.py:876-944 仅 7 处，无 skills/surveillance；grep SkillsManagerTab\|SurveillanceTab main_window.py 空 | VERIFIED | 高 |
| headless 已实现可选 Bearer 鉴权（默认关闭） | read headless.py:145-162 _check_auth（hmac.compare_digest）+ git diff 显示为本次工作区改动 | VERIFIED | 高 |
| agent_prompt/llm_gateway/surveillance/rtsp 均 committed | git log --oneline -- <file> → eef2cce / 1b3fa78 / 213cf57 | VERIFIED | 高 |
| Phase 3 全链路被禁用（P0） | pytest 实测 ADVANCED=False；venv python -c 实测 seaborn FAIL；logic.py:63-78 seaborn 列必需 | VERIFIED | 高 |
| 测试基线 91 collected / 90 passed 1 failed | pytest --collect-only -q → "91 tests collected"；pytest tests/ -q → "1 failed, 90 passed"（226.56s） | VERIFIED | 高 |
| e2e_full_pipeline 失败根因=media_clips 0 clips | pytest 输出 `"media_clips": {"ok": false, "detail": "0 clips"}` | VERIFIED | 高 |
| main_window.py 2405 行 God Object | wc -l 实测 | VERIFIED | 高 |
| ChatWorker 展示层截断 [:100] | main_window.py:256 | STATIC | 高 |
| inject_agent_system_context 旧分隔符拼接 | main_window.py:2307-2336 | STATIC | 高 |
| agent_prompt.py 六模块已建+被调用 | read agent_prompt.py 全文 + main_window.py:1166-1174 | STATIC | 高 |
| llm_gateway 主聊天链路未接线（surveillance_tab 已用） | grep GatewayRouter src/ui/ 仅 surveillance_tab docstring；main_window.py:395/1910 仍 APIGatewayClient | STATIC | 高 |
| recall_preferences 未被 Agent 个性化调用 | grep recall_preferences src/ui/ src/core/agent_prompt.py 空 | VERIFIED | 高 |
| api_intro_page 仍硬编码 iflow.cn | api_intro_page.py:21-56 | STATIC | 高 |
| plot_metrics 基础 matplotlib 折线 | main_window.py:2215-2250 | STATIC | 高 |
| FunClip 纯 Python+funasr 本地 ASR | 子代理读 FunClip/README.md | INFERENCE-AGENT | 中（funasr torch 版本对齐 UNVERIFIED） |
| EpicInfographics MIT 许可 | 子代理读 README.md:7,13 | UNVERIFIED（待 file:line 复验许可字段） | 低-中 |
| LuxTTS <1GB VRAM / zipvoice 许可 | 子代理读 README.md:16-25 | UNVERIFIED（许可待复验） | 中 |
| CL4R1T4S 15 模式置信度百分比 | 子代理深读 8 厂商 | INFERENCE-AGENT | 中（百分比为经验评估非量化实测） |
| 工作区存在并行会话写入 | src/skills/* mtime 17:26-17:32 > 本会话开始时间 | INFERENCE | 中 |

---

## 5. Decisions and Assumptions

### 已确认事实（v4 修正后）
- v5.0 实际进度：agent_prompt/llm_gateway/surveillance/rtsp/**skills 骨架**/headless 鉴权均已落地；多数 committed，skills+surveillance_tab+headless 鉴权在工作区
- **P0 新发现**：seaborn 依赖声明缺失 → Phase 3 摘要媒体全链路在真实环境禁用（90/91 测试中唯一的失败即由此而来）
- 测试基线实测：91 collected / 90 passed 1 failed
- 三 Explore 子代理 + 独立 Critic（FAIL→修复）审查链闭环完成

### 当前假设
- 扩展面以"可选 skills 包"落地（主项目已有 src/skills 机制可挂载）
- "沉淀用户 skills"指用户可定义/管理工作流模板（骨架已建，需接线+扩展）
- 并行会话若存在，其改动是用户授意的（Phase B 前由用户确认）

### 架构决策
1. **不推翻重写**——渐进增强
2. **迁移思想不迁移代码**——跨栈适配
3. **Agent 提示词升级**：在已有六模块上补 Loop/thought/gate/citation/fail-safe（非从零写）
4. **skills 机制**：骨架已建，方向转为"接线 + 触发机制 + 扩展 skill 挂载"，非从零搭建
5. **黑匣子透明化**：eli5 + 决策日志面板 + self-eval
6. **死代码决断收窄**：llm_gateway 已被 surveillance_tab 用作判图引擎（非纯死代码）；真正待决断的是"主聊天链路是否迁移到 GatewayRouter"

### 被否决的方案
- mem0/letta 本体 / 多 agent 编排 / video-use Hard Rules 全套 / 安全渗透功能移植 / MoneyPrinterTurbo 云端链路照搬 / PaddleDetection 双栈 / PolyForm 非商业 skill 引入

---

## 6. 主项目现状总结（v5.0 实测重评，v4 修正版）

### 当前架构特点
- 三阶段流水线 + Agent 面板（ReAct messages 化）+ 显存互斥模型管理 + 跨视频 KB + 全协议网关（监控链路已用）+ 监控/RTSP + **skills 机制骨架**
- main_window.py 2405 行 God Object（109 def + 10 Worker 类 + 7 tab，surveillance/skills 2 个新 Tab 未挂）

### 当前优点（v5.0 已落地，实测确认）
1. 三阶段链路 + 加载态/任务面板/资源监控
2. ModelContextManager 显存互斥 + CUDA 健康检查 + CPU 回退
3. 跨视频 KB（kb_frames）+ user_preferences API
4. **9 个 Agent 工具全接线**（含 search_kb + search_by_image）
5. agent_prompt.py 六模块 + ChatWorker messages 化 + schema 参数解析
6. Ollama SSE 客户端层解析（纯文本 delta + `</think>` 包装）
7. API Key OS 密钥环
8. **91 测试（90 绿 1 红）**，含 llm_gateway/skills_loader/surveillance_tab/rtsp 新模块测试
9. Docker + headless（/healthz + /analyze + **可选 Bearer 鉴权**）
10. skills 机制骨架（schema 校验/loader/启用状态/管理 Tab/内置 skill/单测）

### 当前主要问题（v4 修正后，按优先级）

#### P0 — 真实功能破坏（Critic 审查后实测新发现）
- **P0-1 Phase 3 摘要媒体全链路被禁用**：logic.py:63-78 `_detect_advanced_features()` 要求 seaborn，venv 未装、requirements.txt 未声明 → ADVANCED_FEATURES_AVAILABLE=False → MediaWorker 直接跳过（"依赖可用: False"）→ 用户点"🎬 生成摘要媒体"无产出；e2e_full_pipeline 唯一失败项即此。**修复成本极低**（requirements.txt 补 seaborn + venv 安装），但影响是主功能级。

#### P1 — 影响"agent 更完美+黑匣子透明"核心诉求
- **P1-1 Agent 提示词深度不足**：六模块已建，但缺 ① Agent Loop 六步骨架（Manus）② thought 块引导（Gemini，适配小模型）③ 意图澄清 gate ④ 时间戳引用【frame:HH:MM:SS】⑤ 失败3次求助 ⑥ notify/ask 双消息 ⑦ 独立工具并行 ⑧ 调用前说明意图/不暴露工具名
- **P1-2 黑匣子展示层未透明**：ChatWorker 展示截断 [:100]（main_window.py:256），工具气泡无完整参数/返回值/eli5，无决策日志面板，无 self-eval
- **P1-3 inject_agent_system_context 旧分隔符**（main_window.py:2307-2336）与 agent_prompt context 槽位未对齐

#### P2 — 接线收尾与工程质量
- **P2-1 skills 机制接线**：骨架已建（loader/schema/state/UI/builtin/测试全齐），但 ① main_window 无"🧩 Skills"Tab（addTab 缺失）② 无触发机制（triggers 字段已解析但 send_to_agent 不按触发注入）③ 内置 skill 仅 1 个（video-summary）
- **P2-2 surveillance_tab 接线**：307 行 Tab 已写，main_window 未挂载；**注意**：它依赖 untracked 状态文件，接线即把 untracked 纳入
- **P2-3 APIGatewayClient 脆弱协议**（logic.py:866-882 字符串分隔符）：主聊天链路若迁 GatewayRouter 则自然消解（需决断）
- **P2-4 api_intro_page iflow 商业推荐残留**（api_intro_page.py:21-56）
- **P2-5 plot_metrics 基础折线**（main_window.py:2215-2250）
- **P2-6 main_window God Object**：2405 行 + 2 个已写未挂 Tab，新 Tab 挂载是拆分契机
- **P2-7 user_preferences 未接线个性化**：Agent 提示词未注入 recall_preferences
- **P2-8 扩展 skills v1 缺失**：无 FunClip/EpicInfographics/LuxTTS 对应 skill 挂载（依赖 P2-1 接线完成）

#### P3 — 长期增强
- P3-1 训练中心 tab（yolo-training-template）/ P3-2 Web 版（VSS）/ P3-3 插件系统（edit-mind）

### 风险点
- 并行会话写入工作区（skills/headless/surveillance_tab 同期出现）→ Phase B 冲突风险
- 本地小模型推理能力有限 → P1-1 需最小实验验证
- FunClip funasr torch 版本与主项目 ultralytics torch 共存性 → B4 前最小验证

### 技术债
- main_window God Object（P2-6）
- 主聊天链路与 GatewayRouter 双轨（P2-3 决断）
- api_intro iflow 残留（P2-4）

---

## 7. 参考项目亮点提炼

### 7.1 CL4R1T4S 15 可迁移模式 + 8 反面模式（子代理 T4a，INFERENCE-AGENT 级）

#### 15 可迁移模式
1. **Agent Loop 显式化**（Manus_Prompt.txt:55-67：Analyze→Select→Wait→Iterate→Submit→Standby）·95%
2. **Planner/Knowledge/Datasource 三模块 event 注入**（Manus:69-99）·85%
3. **notify/ask 双消息**（Manus_Functions:1-43：进度非阻塞/关键阻塞）·90%
4. **AGENTS.md 分层作用域**（Codex_Sep-15:18-30）·80%
5. **工具名对用户隐藏**（Cursor_Prompt.md:20-21）·90%
6. **调用前一句话说明意图**（Cursor:22, Windsurf:18-19）·95%
7. **独立工具并行调用**（Claude_Code_03-04-24:50）·90%
8. **失败3次求助**（Devin:18-19, Replit:52）·90%
9. **planning/standard 双模式**（Devin:44-49）·80%
10. **todo 持久化清单**（Manus:119-127）·85%
11. **持久记忆**（Windsurf:58-68）·75%
12. **citation 引用格式**（Codex_Sep-15:33-46：结论引用【frame:HH:MM:SS】）·85%
13. **显式 thought 块**（Gemini:6-9）·88%
14. **意图澄清前置 gate**（DROID:47-51, Lovable:21-27）·90%
15. **idle 显式收尾**（Manus_Functions:1-8）·80%

> v4 修正：百分比为子代理经验评估（INFERENCE-AGENT），非量化实测；使用时按方向参考不按数值引用。

#### 8 反面模式
超长提示词（204K，小模型 8K 即崩，控 2-3K 字）/ 强制散文禁列表（视频分析需结构化列表）/ Artifacts 重 UI / WebContainer 依赖 / 强制 make_pr / 长篇免责 / 万字硬指标 / 云服务依赖（Lovable/Bolt）。

#### Agent 提示词升级 3 重点
①补 Agent Loop 六步骨架；②意图澄清 gate + 删除工具硬护栏；③thought 块 + 时间戳引用 + 失败3次求助。

### 7.2 agent 工程化 9 项目共性（子代理 T4b）
1. SKILL.md + YAML frontmatter（name+description 必需，description "Use when..."）
2. 触发三分类（常驻/关键词/任务，OpenHands microagent/types.py:6-9）
3. Progressive Disclosure（启动只载 description，命中载全文）
4. Hard Rules vs 艺术自由分层（video-use）
5. 记忆三档（project.md 会话追加 / ADD-only 累积 / 文件系统即记忆）
6. Ask→confirm→execute→self-eval→persist（video-use:84-100）
7. 管理 GUI 五元组（skills-manager：Library/Preset/Workspace/Activity/Backup）

### 7.3 扩展面 v1 推荐（子代理 T4c，许可/依赖待复验）
1. **FunClip 视频精剪包**：本地 ASR 剪辑（funasr torch 对齐待验）
2. **EpicInfographics 信息图包**：MIT 待 file:line 复验
3. **LuxTTS 本地配音包**：<1GB VRAM（zipvoice 许可待验）

### 7.4 不应照搬
mem0/letta 本体 / 多 agent 编排 / video-use Hard Rules 全套 / CL4R1T4S 防御条款 / MoneyPrinterTurbo 云端链 / PaddleDetection / PolyForm skill。

---

## 8. 差距分析

### 8.1 功能层
| 维度 | v5.0 现状 | 参考做法 | 差距 |
|------|-----------|---------|------|
| Agent 提示词 | 六模块已建，缺 Loop/thought/gate/引用 | CL4R1T4S 15 模式 | 中 |
| 黑匣子透明 | 展示层截断 [:100]，无决策日志/self-eval | video-use self-eval + eli5 | 大 |
| skills 沉淀 | **骨架已建**（loader+UI+builtin），缺接线+触发+扩展 | superpowers/skills-manager | 中（从大缩小） |
| 扩展面 | 无扩展 skill 挂载 | FunClip/EpicInfographics/LuxTTS | 大 |
| 跨模态搜索 | 文→帧+图→帧 ✅ | — | 已达标 |
| **摘要媒体** | **被 seaborn 缺失禁用** | — | **P0** |

### 8.2 架构层
| 维度 | 现状 | 差距 |
|------|------|------|
| main_window God Object | 2405 行，2 个新 Tab 未挂 | 大（挂 Tab 是契机） |
| LLM 网关双轨 | 监控链路已用 GatewayRouter，主聊天链路仍 APIGatewayClient | 中（决断） |
| 记忆层 | user_preferences API 建、未接线 | 小 |

### 8.3 工程层
类型注解部分 / mypy 渐进 / 可观测日志 500 行滚动 / 提示词模板 3 份 TXT —— 均中差距。

### 8.4 性能层
shared_embedder+帧缓存 ✅ 已达标 / num_ctx 可配 ✅ / 长视频分块摘要 中差距。

### 8.5 安全层
keyring ✅ / config 中立 ✅ / headless **可选鉴权已实现（默认关闭，向后兼容）** — 残余风险仅"默认关闭时公开"，建议生产部署文档强制 VAP_HEADLESS_TOKEN / 工具调用护栏 中差距。

### 8.6 运维层
Docker/headless/CI 三平台矩阵 — 已达标。

### 8.7 测试与真实用户体验
91 测试（90 绿 1 红，失败=P0 摘要媒体）/ E2E 冒烟有 / 小白路径：Agent 面板对小白仍不友好（截断+无 eli5）。

---

## 9. 可迁移 / 半可迁移 / 不建议迁移

### 9.1 可迁移（直接落地）
| 项 | 来源 | 成本 | 风险 | 适配方式 | 最小实验 |
|----|------|------|------|---------|---------|
| P0-1 修 seaborn 依赖 | 内部 | 极低 | 低 | requirements.txt + venv 安装 + e2e 复跑 | e2e_full_pipeline 转绿 |
| agent_prompt 补 Loop/thought/gate/引用/求助 | CL4R1T4S | 低 | 低（文本可回滚） | 现有六模块上增补 | 3 问题对比（§11.7 有设计） |
| 黑匣子透明化（不截断+决策日志+eli5+self-eval） | video-use/eli5 | 中 | 低 | ChatWorker 展示层 + agent_panel | 1 工具试点 |
| skills Tab 接线 + 触发机制 | 内部骨架 + OpenHands 触发三分类 | 中 | 低 | main_window.addTab + send_to_agent 注入 | 加载内置 skill |
| inject_context 对齐 agent_prompt | 内部 | 低 | 低 | context 改结构化 | 现有测试 |
| 扩展 skills v1（FunClip/EpicInfographics/LuxTTS） | 参考根2 | 中-高 | 中 | 挂 src/skills 机制 | 每包独立最小验证 |

### 9.2 半可迁移（局部借鉴）
mem0 跨会话记忆→user_preferences 接线个性化 / OpenHands microagent 触发→skills triggers / Manus todo→Agent 多步任务清单 / superpowers verification→agent_prompt finalize 守卫 / ECC contexts→行为档位切换。

### 9.3 不建议迁移
mem0/letta 本体 / 多 agent 编排 / video-use Hard Rules 全套 / 安全渗透功能 / VideoPipe C++ / CL4R1T4S 防御条款 / MoneyPrinterTurbo 云端链 / PaddleDetection / PolyForm skill。

---

## 10. 优先级优化路线图（v4 重排：P0 浮出）

### P0（新发现，修复成本极低、影响主功能）

| ID | 做什么 | 为什么 | 涉及模块 | 预期收益 | 风险 | 验证 | 回滚 |
|----|--------|--------|---------|---------|------|------|------|
| P0-1 | requirements.txt 补 seaborn + venv 安装（或把 seaborn 移出必需探测列表，改为可选增强） | _detect_advanced_features 判 False → Phase 3 全链路禁用 → 用户主功能不可用 + 唯一红测 | logic.py:63-78 或 requirements.txt | Phase 3 恢复 + e2e 全绿 | 极低 | e2e_full_pipeline 转绿 + 手动点"生成摘要媒体" | requirements 还原 |

### P1

| ID | 做什么 | 为什么 | 涉及模块 | 前置 | 收益 | 风险 | 验证 | 回滚 |
|----|--------|--------|---------|------|------|------|------|------|
| P1-1 | agent_prompt 补 Agent Loop/thought/gate/citation/失败求助/notify-ask/并行/意图前置 | 8 项缺口（CL4R1T4S） | agent_prompt.py + main_window send_to_agent | 无 | 智能度深度 | 低 | 3 问题对比（§11.7） | 还原旧文本 |
| P1-2 | 黑匣子透明化：去 [:100] 截断 + 完整工具气泡 + eli5 + 决策日志面板 + self-eval | 小白看不懂 | main_window ChatWorker 展示 + agent_panel | P1-1 | 理解+学习 | 低 | 1 工具试点 | 隐藏面板 |
| P1-3 | inject_context 对齐 agent_prompt context 槽位（结构化替代分隔符） | 格式不一致 | main_window.py:2307-2336 | P1-1 | 一致性 | 低 | 现有测试 | 还原 |

### P2（接线收尾）

| ID | 做什么 | 为什么 | 涉及模块 | 风险 |
|----|--------|--------|---------|------|
| P2-1 | skills Tab 挂载（addTab）+ 触发机制（triggers 命中→send_to_agent 注入）+ 补 2 个内置 skill | 骨架全齐只差接线 | main_window + agent_prompt/ChatWorker | 低-中 |
| P2-2 | surveillance_tab 挂载（addTab）+ closeEvent 补 worker 清理 | 307 行已写 | main_window | 低-中 |
| P2-3 | 主聊天链路 GatewayRouter 迁移决断（迁=获故障转移；不迁=保留 APIGatewayClient 并在 llm_gateway 标注仅监控用） | 双轨并存 | main_window + logic | 中 |
| P2-4 | api_intro 去 iflow | 中立性 | api_intro_page.py | 低 |
| P2-5 | plot_metrics 升级（ADVANCED 修复后 seaborn 可用） | 报表专业度 | main_window | 低 |
| P2-6 | main_window 拆分（Worker 抽 src/workers/；挂 2 Tab 作为拆分第一步） | God Object | main_window | 中 |
| P2-7 | user_preferences 个性化接线（send_to_agent 注入 recall） | API 建未用 | agent_prompt + history_manager | 低 |
| P2-8 | 扩展 skills v1 挂载（FunClip/EpicInfographics/LuxTTS 三包独立验证后挂 src/skills） | 诉求④ | src/skills/ + service 层 | 中 |

### P3
训练中心 tab / Web 版 / 插件系统。

---

## 11. 全栈实施方案

### 11.1 前端（UI）
- **Agent 面板**（P1-2）：工具气泡完整参数+返回值+eli5；"Agent 决策日志"可展开侧栏；self-eval max 3
- **Skills Tab**（P2-1）：main_window.addTab（skills_manager_tab 已写，挂载即用）
- **Surveillance Tab**（P2-2）：同样挂载即用
- **行为档位**（ECC 借鉴，可选）：分析/调试/审查档

### 11.2 后端（核心逻辑）
- agent_prompt 增补段（P1-1）/ ChatWorker 展示层（P1-2）/ inject_context 结构化（P1-3）/ skills 触发注入（P2-1）/ recall_preferences 注入（P2-7）/ **P0-1 依赖修复**

### 11.3 数据层
ChromaDB 已够（不引 mem0）；skills 元数据走 config/skills_state.json（已建）。

### 11.4 接口层
- 主聊天链路 GatewayRouter 决断（P2-3）
- headless：**鉴权已实现**，剩余动作仅生产部署文档强制 `VAP_HEADLESS_TOKEN`（P2 收尾一行文档）

### 11.5 配置管理
api_intro 去 iflow（P2-4）；skills 配置段沿用现有。

### 11.6 日志与监控
决策日志面板（P1-2）；工具调用审计（借安全类方法论）。

### 11.7 测试策略（v4 补全：P1-1 最小实验设计）

**P1-1 最小实验（3 问题对比，可执行版）**：
- 环境准备：本地 Ollama + 同一模型（默认 qwen2.5-vl 7B；若用户环境不同按实际）+ 同一测试视频（合成 2 分钟视频即可，复用 e2e 合成逻辑）
- **Q1（单工具准确性）**："视频里第 10 秒的画面是什么？" → 通过标准：正确调用 get_frame_details({"seconds":10}) 且不臆造画面内容
- **Q2（多轮 ReAct）**："找出视频中所有出现文字的画面并列出内容" → 通过标准：≥2 次工具调用（search_visual/OCR）且最终列表时间戳与工具返回一致（不虚构）
- **Q3（意图澄清 gate）**："给我剪个精彩片段" → 通过标准：先 ask 澄清（时长/风格/数量）而非直接调 create_highlights
- **基线对比**：旧 agent_prompt vs 新版各跑 3 次，量化指标：工具选择正确率（/3）、时间戳引用正确率（/3）、澄清触发率（Q3，/3）、幻觉次数（虚构时间戳/画面）
- **通过阈值**：新版三项正确率 ≥70%（即 7/9 次以上）且幻觉次数 ≤1 → P1-1 通过；否则回退或换更强模型
- 全部用本地模型零 API 成本，mock 不适用（本实验目的就是测真模型行为）

- P0-1：e2e_full_pipeline 转绿（唯一红测）
- P1-2/P2-1：1 工具试点 + 加载内置 skill
- 回归基线：**91 collected / 90 passed**（P0-1 修复后应 91 全绿），每批次结束复跑

### 11.8 部署与运维
扩展 skills 独立分发不进核心包；Docker 不含扩展 skills；**部署文档补 VAP_HEADLESS_TOKEN 强制项**。

### 11.9 建议实施批次（v4 重排）
| 批次 | 内容 | 风险 | 依赖 | 需审批 |
|------|------|------|------|--------|
| **B0** | **P0-1 seaborn 依赖修复**（一行依赖 + e2e 复跑） | L1 | 无 | 否（建议立即批） |
| B1 | P1-1 agent_prompt 升级 + P1-3 context 对齐 | L1 | B0（基线全绿后再动核心） | 否 |
| B2 | P1-2 黑匣子透明化 | L2 | B1 | 否 |
| B3 | P2-1 skills 接线+触发 + P2-2 surveillance 挂载 + P2-7 user_pref 注入 | L2 | B1 | 是（UI 入口+工作区协调） |
| B4 | P2-3 GatewayRouter 决断 + P2-4/2-5 工程收尾 + P2-8 扩展 skills v1 | L1-L2 | B3 | 是（决断+扩展方向+许可复验） |
| B5 | P2-6 main_window 拆分 | L3 | B2/B3 | 是（建议 v5.x） |

---

## 12. 实施授权状态

当前**未授权**修改源代码（P0-1 修复建议单独立即批准，因成本一行且主功能受损）。

| 批次 | 影响范围 | 风险 | 验证方式 | 需单独审批 |
|------|---------|------|---------|-----------|
| B0（P0-1） | requirements.txt + venv | L1 | e2e 转绿 | **建议立即批（主功能受损）** |
| B1（P1-1/1-3） | agent_prompt.py + main_window 两处 | L1 | 3 问题实验 + 回归 | 否 |
| B2（P1-2） | ChatWorker 展示层 + agent_panel.py | L2 | 1 工具试点 | 否 |
| B3（P2-1/2-2/2-7） | main_window addTab ×2 + 触发注入 | L2 | 挂 Tab+加载 skill | **是（且需先确认工作区归属）** |
| B4（P2-3/4/5/8） | logic/main_window 决断 + api_intro + 扩展 skills | L1-L2 | 决断实验+skill 跑通 | **是（决断+扩展+许可复验）** |
| B5（P2-6） | main_window 拆分 | L3 | 独立 Critic | **是（建议 v5.x）** |

**不得在本阶段实施代码修改。**

---

## 13. 需要我补充的信息（v4 更新）

| # | 问题 | 为什么需要 | 可否假设继续 | 建议默认 |
|---|------|-----------|-------------|---------|
| 1 | **工作区归属**：src/skills/surveillance_tab/headless 鉴权是另一并行会话在写吗？Phase B 以哪份为基线？ | B3 改同文件必冲突 | 不可 | 用户确认后以其为基线增量 |
| 2 | P0-1 修复方式：requirements.txt 补 seaborn（推荐，plot_metrics 升级也要用）还是移出必需探测？ | 两种回滚语义不同 | 可 | 补 seaborn（一处修复两处收益） |
| 3 | 扩展面形态：可选 skills 包 vs 内置？ | P2-8 方向 | 可 | 可选包 |
| 4 | P2-3 GatewayRouter 决断：主聊天链路迁移 or 保持双轨标注？ | 双轨不能永久并存 | 不可 | 建议迁移（获故障转移，监控链路已验证） |
| 5 | 本地测试模型版本？ | P1-1 实验基线 | 可 | qwen2.5-vl 7B |
| 6 | 是否批准 B3/B4/B5？ | 新 UI 入口/扩展方向/大拆分 | 不可 | 建议按序批准 |

---

## 14. Next Gate

- **下一步动作**：交付 v4 修正版报告，等待 Phase B 授权 + 工作区归属确认
- **是否需要用户批准**：是
- **需要批准的准确范围**：
  - **立即建议批准 B0（P0-1）**：一行依赖修复恢复 Phase 3 主功能，e2e 转绿
  - 优先批准 **B1+B2**（提示词升级 + 黑匣子透明化，直击诉求①②）
  - **B3** 需先确认工作区归属（问题1）再批
  - **B4** 需 GatewayRouter 决断（问题4）+ 扩展许可复验后批
  - B5 建议 v5.x

---

## 15. Validation Matrix（v4 更新）

| Requirement | Validation Method | Actual Command/Action | Result | Evidence |
|-------------|-------------------|----------------------|--------|----------|
| 主项目 v5.0 现状重评 | 实测逐文件 + git log | read 20+ 文件 + git log -- <file> ×5 | 完成 | §3.1.1 |
| skills 机制现状 | ls + read | ls src/skills → 4 py；read loader.py/schema.py/SKILL.md | 完成 | §3.1.1/§4 |
| headless 鉴权现状 | grep + read | grep hmac headless.py → :10,:145-162 | 完成 | §3.1.1 |
| 提交状态区分 | git log/status | git status --porcelain + git log --oneline -- <file> | 完成 | §3.1.2 |
| 测试基线 | pytest 实测 | --collect-only → 91；全量 → 90 passed 1 failed（226.56s） | 完成 | §3.1.3 |
| P0 根因定位 | python -c 实测 | seaborn import FAIL；ADVANCED=False | 完成 | §3.1.1/§6 |
| 参考根1/2 + CL4R1T4S | 三子代理 | 8+30 项目 + 28 厂商 | 完成 | §3.2/3.3/§7 |
| 独立审查闭环 | Critic 子代理 | 六方面复验 → FAIL → v4 修复 | 完成 | §2 T5b/T5c |

---

## 16. Review Findings（独立 Critic 审查记录）

- **轮次1（Critic，六方面）**：总评 **FAIL**。发现 2 BLOCKER + 8 MAJOR + 5 MINOR：
  - [BLOCKER] skills 机制误判"完全缺失"（实际 src/skills 骨架+UI+builtin+3 测试已建）→ ✅ v4 已修正（实测 ls/read 确认），P1-3 降级为 P2-1 接线
  - [BLOCKER] headless 误判"无鉴权"（实际 _check_auth hmac.compare_digest 已实现，默认关闭）→ ✅ v4 已修正（实测 read 确认）
  - [MAJOR] "v5.0 未提交改动"事实错误（agent_prompt 等均 committed）→ ✅ v4 已修正（git log 实测）
  - [MAJOR] P1-1 最小实验无设计 → ✅ v4 补 3 问题+阈值（§11.7）
  - [MAJOR] 测试数字不符（59+9 vs 实际）→ ✅ v4 实测 91 collected / 90 passed 1 failed
  - [MAJOR] Evidence 无四级标签 → ✅ v4 引入 VERIFIED/STATIC/INFERENCE/UNVERIFIED
  - [MAJOR] surveillance_tab.py 存在被忽略 → ✅ v4 区分"Tab 已写未接线"
  - [MAJOR] B3 依赖 B1 人为串行 → ✅ v4 调整批次依赖
  - [MAJOR] 扩展 skills 许可/依赖未复验 → ✅ v4 降级标注 UNVERIFIED，列为 B4 前置
  - [MINOR] P2-7/P2-8 编号混乱 → ✅ v4 统一编号
- **Builder 修复状态**：10/15 已修复并实测复验（v4 本版）；剩余 5 项为 UNVERIFIED 事实（扩展 skill 许可、并行会话确认）需用户提供或 Phase B 前复验
- **Critic 意外收获**：复验过程中 Builder（总协调）实测发现 **P0-1（seaborn 缺失→Phase 3 禁用）**，为 Critic 未发现的真问题，已列为最高优先级
- **剩余风险**：① 并行会话归属未知；② 3 个扩展 skill 许可待复验；③ P1-1 实验效果依赖本地模型

---

## 17. Risks and Blockers

| 风险 | 等级 | 触发条件 | 影响 | 缓解 | 需用户确认 |
|------|------|---------|------|------|-----------|
| 并行会话写工作区 | 高 | Phase B 与另一会话同文件 | 合并冲突/覆盖 | 先确认归属（问题1） | **是** |
| 本地小模型能力不足 | 中 | P1-1 实验不达标 | 提示词升级收益有限 | 阈值实验（§11.7）+ 可选 API 模型 | 否 |
| FunClip funasr torch 冲突 | 中 | B4 集成时 CUDA 版本冲突 | 扩展包失败 | 独立 venv/子进程隔离方案 | 否 |
| 扩展 skill 许可风险 | 低 | EpicInfographics/LuxTTS 许可与 GPL-3.0 冲突 | 法律风险 | B4 前 file:line 复验 | 否 |
| main_window 拆分回归 | 中 | B5 拆分 | 现有功能破坏 | v5.x + 独立 Critic | 是 |
| MCP 图谱失败 | 已发生 | — | — | 已改直接扫描 | 否 |

---

*本文件为 Phase A 单一状态源（v4 修正版）。Phase B 获批后在此追加 Implementation Batches / Validation Log / Critic Findings。*
