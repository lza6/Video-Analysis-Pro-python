# 参考的结果计划指南（Workflow Status）

> 本任务是 ANALYSIS_ONLY 阶段的单一状态源。用于长任务恢复、节点协作与最终验收。
> 仅在主要阶段变化或重要发现改变计划时更新。未经允许不提交、不推送。

---

## Task Contract

- **原始目标**：基于主项目（Video-Analysis-Pro-python，视频分析 Agent 产品）与两个参考根目录，完成项目识别、同类筛选、优点提炼、差距分析、优化方案设计；获确认后再逐步实施。重点：迁移**设计思想**，不照抄代码；看似无关的参考项目也可能是为未来扩展（图片/视频生成、电商、PPT、可观测性、MCP、skills 沉淀、系统提示词）做准备。
- **当前阶段**：Phase A — 项目识别与对标分析（ANALYSIS_ONLY）。
- **当前授权**：只读分析；允许创建/更新本文件；禁止修改源码/配置/依赖/部署文件；禁止提交、推送、发布、部署；禁止运行参考项目脚本；禁止真实付费 API 调用（预算 0）；禁止回滚/覆盖用户已有改动（含本会话期间用户的删除操作）。
- **成功标准**：产出 9 节结构化分析报告，每条结论带 file:line 证据与置信度；报告完成后停止，等待用户批准实施范围。
- **停止条件**：报告完成并交付；或遇到阻塞即停止并报告。

---

## Task Graph

| ID | Agent/Owner | Goal | Dependencies | Deliverable | Validation | Status |
|----|-------------|------|--------------|-------------|------------|--------|
| N1 | MainProjectMapper | 映射主项目真实架构（Web UI 重构后状态） | 无 | 11 节结构化现状 + Top5 技术债 | 每条 file:line 证据 | DONE |
| N2 | VisionRefScout | 递归发现 `视觉参考项目` 所有独立项目 | 无 | 项目清单 + Top5 排名 | 每条文件路径证据 | DONE |
| N3 | IntelRefScout | 递归发现 `智能渗透\参考项目` 所有独立项目（含扩展方向分组） | 无 | 项目清单 + Top8 排名 + 扩展方向分组 | 每条文件路径证据 | IN_PROGRESS |
| N4 | CL4R1T4SAnalyst | 深挖 CL4R1T4S 系统提示词与 agent 设计模式 | 无 | 8 节提示词模式 + Top5 可借鉴模式 | file:line 证据 | DONE |
| N5 | SynthesisArchitect | 对标、差距分析、跨技术栈适配、路线图 | N1/N2/N3/N4 | 9 节分析报告 | 证据齐全 | PENDING |
| N6 | Critic | 独立审查分析报告（不改代码） | N5 | 六维审查发现清单 | 每发现带严重级+证据 | PENDING |
| N7 | Evaluator | 对照任务契约终验 | N5/N6 | PASS/CONDITIONAL/FAIL/BLOCKED | 逐项证据 | PENDING |

节点状态：PENDING / READY / IN_PROGRESS / BLOCKED / REVIEW / FIX_REQUIRED / VERIFIED / DONE / SKIPPED

---

## Project Inventory

### 主项目识别摘要（N1 回报，DIRECT 证据为主）

- **真实版本**：APP_VERSION = `"8.0.0"`（`src/utils/constants.py:6`）。项目级 CLAUDE.md 写 v4.5.0/PyQt6 MVC 已过期。
- **架构现状**：已完成 PyQt6→Web UI（FastAPI + Next.js）主体迁移；`src/ui/` 老目录已删除。但 PyQt6 残留未清干净（见 Risks）。
- **入口**：`launcher.py:257-282` → `src/web/serve.py:62-109`（`run_server`→`uvicorn.run("src.web.app:app")`）；FastAPI app 在 `src/web/app.py:137`；前端 `webapp/package.json`（Next 16.3.4 + React 19.2.8 + Tailwind v4，静态导出到 `webapp/out/`，由 FastAPI StaticFiles 同源挂载，`src/web/app.py:32,129-134`）。
- **目录地图**：`src/core/`（24 .py，9608 行，业务逻辑层）/ `src/web/`（3425 行，FastAPI 后端 + 12 router + services）/ `webapp/`（Next.js 前端 11 路由）/ `src/skills/`（skill 加载器）/ `src/server/headless.py`（旧 headless，仍存）/ `config/`（prompts/skills 8 个内置/history.db/runs.db/chroma_db）。
- **Agent 系统**：`agent_orchestrator.py`（状态机：classify_intent→select_skill→build_plan→run_plan，7 类 Intent，规则关键词匹配）；`agent_tools.py`（ToolRegistry + 15 个 create_*_tool 工厂，Web 端注册 5 个子集）；skills 系统 `src/skills/loader.py` + `config/skills/` 8 个 skill。
- **LLM 路由（NVIDIA 多 key）**：`provider_router.py:200-423`（ProviderKey + RateLimiter 40req/min 滑动窗口 + per-key 并发信号量默认 2 + select_key priority desc 最久未用优先 + 429/5xx 切 key + 503 短退避同 key 重试 2 次 + 401/403/404/422 标失效）；`load_from_env` 读 `VAP_NV_API_KEYS`/`VAP_KILO_API_KEYS`。协议后端抽象 `llm_gateway.py`（ProtocolBackend ABC + Anthropic/OpenAIChat/OpenAIResponses/Gemini 4 实现 + GatewayRouter）。
- **三阶段流水线**：`analyzer_service.py:189-268` `_run_pipeline`（Phase1 抽帧 SSE FRAME → Phase1.5 Whisper 转录 → Phase2 LLM 拼 prompt **整段推 REPORT_TOKEN，非逐 token 流式**，`:242` 注释自承留作优化）。
- **数据层**：`history_manager.py`（sessions+checkpoints SQLite + chromadb PersistentClient path=config/chroma_db）；`run_store.py`（runs/segments/clips 三表 + WAL + 外键级联）；`job_store.py` **进程内内存实现**（JobRecord 持 asyncio.Queue，`:1-8` 自承"阶段 0 内存实现"，重启即丢）。
- **配置/密钥**：`config_manager.py`（_secure_set/_secure_get 优先 OS keyring 降级 ini 明文 + is_keyring_available 真实探测 + audit_ini_key_cleared）；Web 配置 `src/web/config.py` pydantic-settings（env_prefix=VAP_）。
- **测试**：39 个测试文件；`test_web_api.py`（TestClient + 3s ffmpeg 测试视频，覆盖 health/analyze+SSE/jobs/路径遍历/Bearer）。conftest 仍 `from PyQt6.QtWidgets import QApplication` + qapp fixture。
- **构建/部署**：Dockerfile/Dockerfile.cuda CMD **仍指向 `src.server.headless`**（未切 src.web.serve）；build_windows.spec 仍按 PyQt6 GUI 打包（hiddenimports 含 PyQt6.QtMultimedia，console=False，未打包 webapp/out + fastapi/uvicorn）；CI `.github/workflows/ci.yml` **不装 fastapi/uvicorn、不跑 npm build、不验证 Web 层**。
- **迁移影响**：4 阶段 commit 实际改了什么见 N1 原报告；阶段4"删除 PyQt6 桌面端"**部分完成**——src/ui/ 删了，但 batch_runner/requirements/constants/spec/conftest 仍含 PyQt6。

### 参考项目候选清单（N2 + N3）

**视觉参考项目（N2，10 个，已全覆盖）**：
NVIDIA VSS / VibeNVR / VideoPipe / edit-mind / rf-detr / supervision / claude-video-vision / video-analyzer / Deep-Learning-Experiments / yolo-training-template

**智能渗透\参考项目（N3，数百个，进行中）**：含 CL4R1T4S（N4 已深挖）、Anthropic-Cybersecurity-Skills、ECC、OpenHands、SWE-agent、SuperAGI、agent-orchestrator、agent-skills、agentmemory-main、VideoRAG/VideoHub/VideoPipe/VideoCaptioner/Pixelle-Video/FunClip（视频生成方向）、CosyVoice、OfficeCLI/Infographic/EpicInfographics（PPT/信息图方向）、等众多。

### 排除目录及原因
依赖/构建产物：node_modules / .git / venv / .venv / dist / build / target / vendor / __pycache__ / .next / coverage / .cache（仅作构建/部署行为证据时才看）。

### 高价值参考项目（待 N5 综合后定稿，初选）
- 视频管线北参考架构：NVIDIA VSS、VibeNVR、edit-mind、VideoPipe
- 感知层栈：claude-video-vision + supervision + rf-detr
- Agent 系统提示词工程：CL4R1T4S（Devin think/Manus 多模块/Droid 双模/Devin DONE-BLOCK-NONE/Manus 工具三段式）
- 待 N3 补充：agent 框架 / MCP / 可观测性 / 多模态生成扩展方向项目

---

## Evidence Ledger

| Claim | Evidence | Type | Confidence |
|-------|----------|------|------------|
| 当前分支 main，本地领先 origin 6 commit | `git status` | DIRECT | 高 |
| 本会话期间用户删除了 workflow_status.md / 参考的结果计划指南.md / 计划书/ 三个工作树文件（HEAD 树里仍在） | `git status` 显示 D；`git cat-file -p HEAD:参考的结果计划指南.md` 返回内容 | DIRECT | 高 |
| APP_VERSION="8.0.0"，CLAUDE.md 写的 v4.5.0 过期 | `src/utils/constants.py:6` | DIRECT | 高 |
| src/ui/ 老目录已删除（PyQt6 桌面端清除） | `ls src/ui` No such file | DIRECT | 高 |
| PyQt6 残留：batch_runner.py 仍 QObject+pyqtSignal（CHANGELOG v8.0.0 声称改纯 Python 但代码未改） | `src/core/batch_runner.py:46,151,169-175` | DIRECT | 高 |
| Dockerfile/Dockerfile.cuda CMD 仍指向旧 headless（P0 部署断裂） | `Dockerfile:41` / `Dockerfile.cuda:35` | DIRECT | 高 |
| CI 未覆盖 Web 层（不装 fastapi、不跑 npm build） | `.github/workflows/ci.yml:26-39` | DIRECT | 高 |
| build_windows.spec 仍按 PyQt6 GUI 打包，未含 webapp/fastapi | `build_windows.spec:46-47,50,88` | DIRECT | 高 |
| launcher.py 启动入口已切到 src.web.serve | `launcher.py:257-282` | DIRECT | 高 |
| Phase2 LLM 非逐 token 流式（整段推 REPORT_TOKEN） | `analyzer_service.py:242-246` | DIRECT | 高 |
| JobStore 进程内内存，重启即丢 | `src/web/job_store.py:1-8,88` | DIRECT | 高 |
| NVIDIA 多 key 路由实现完整（429/5xx 切 key + 503 同 key 重试 + 401/403 标失效） | `provider_router.py:200-423` | DIRECT | 高 |
| CL4R1T4S 是提示词透明化仓库（非代码），许可证非 MIT/Apache（只迁移设计模式不照抄原文） | `CL4R1T4S/README.md:3-5,26-37` + `LICENSE:1` | DIRECT | 高 |
| Devin think 工具 MUST/MAY 触发 + 思考不污染上下文 | `CL4R1T4S/DEVIN/Devin_2.0_Commands.md:6-27` + `Devin2_09-08-2025.md:88,90` | DIRECT | 高 |
| Manus 多模块系统提示词（agent_loop/planner/knowledge/datasource/error_handling） | `CL4R1T4S/MANUS/Manus_Prompt.txt:55-116,246-251` | DIRECT | 高 |
| VSS 三层解耦 + MCP 工具层 + 多 workflow | `视觉参考项目/video-search-and-summarization/README.md:1-152` + `services/` | DIRECT | 高 |
| VibeNVR 生产级全栈（FastAPI+React+VibeEngine 解耦四模块+三级流降级） | `视觉参考项目/VibeNVR/README.md:1-446` + `backend/`+`engine/`+`frontend/` | DIRECT | 高 |
| edit-mind monorepo（pnpm+Turbo+BullMQ+ChromaDB+Prisma+13 packages 领域拆分+MCP app） | `视觉参考项目/edit-mind/README.md:1-249` + `apps/`+`packages/` | DIRECT | 高 |

证据类型：DIRECT（文件/运行直接证明）/ INFERENCE（多证据推断）/ UNVERIFIED（当前无法验证）

---

## Decisions and Assumptions

### 已确认事实
- 主项目 v8.0.0 已完成 PyQt6→Web UI（FastAPI+Next.js）主体迁移并删 src/ui/，但 PyQt6 残留未清干净。
- 两个参考根目录均含大量独立项目；视觉参考项目 10 个已全覆盖。
- CL4R1T4S 是提示词透明化仓库，只能迁移设计模式不能照抄原文。
- 本会话期间用户删除了 workflow_status.md / 参考的结果计划指南.md / 计划书/ 三个工作树文件（HEAD 树仍在，但我**不回滚用户删除**，重新创建本文件以履行任务要求）。

### 当前假设
- Web UI 迁移主体已闭环但 Docker/CI/打包形态未同步（已 DIRECT 证实）。
- 项目级 CLAUDE.md 部分内容（v4.5.0/PyQt6 MVC）已过期（已 DIRECT 证实）。
- 参考项目中"看似无关"的项目对主项目未来扩展有参考价值（待 N3 评估）。

### 架构决策
_分析阶段不下架构决策；仅记录候选。_

### 被否决的方案
- 不照抄参考项目代码（任务硬约束）。
- 不在分析阶段修改源码（授权约束）。
- 不回滚用户工作树删除操作（授权约束 + 用户意图不可臆测）。

---

## Risks and Blockers

| 风险 | 等级 | 触发条件 | 影响 | 缓解 | 需用户确认 |
|------|------|----------|------|------|------------|
| Dockerfile CMD 未切 src.web.serve（Docker 跑旧 headless 非 Web UI） | P0 | 部署 Docker 镜像 | 用户跑 Docker 拿不到 Web UI | 路线图 P0 项：切 CMD + COPY webapp/out | 否（仅记录，不实施） |
| batch_runner.py 仍 QObject+pyqtSignal，CHANGELOG 声称改纯 Python 与代码不符 | P1 | 无 Qt 环境 Web 端靠 video_concurrency=1 + DirectConnection workaround | 回归风险高，依赖 CPython 实现细节 | 路线图 P1 项：改纯 Python _Signal | 否 |
| requirements.txt/constants.py/build_windows.spec/conftest.py 仍含 PyQt6 | P1 | 新装环境拉 ~200MB 无用 PyQt6；打包产物可能不含 Web 依赖 | 环境污染 + 打包断裂 | 路线图 P1 项：清 PyQt6 + 更新 spec | 否 |
| CI 未覆盖 Web 层（test_web_api.py 因 fastapi 未装被 `|| true` 吞掉） | P1 | CI 跑不验证 Web API | Web 回归无守门 | 路线图 P1 项：CI 装 requirements-web + 跑 npm build | 否 |
| Phase2 LLM 非逐 token 流式（体验劣于原 PyQt6 SSE） | P2 | 用户等待报告整段返回 | 体验退化 | 路线图 P2 项：改逐 token 流式 | 否 |
| JobStore 进程内内存，重启即丢单视频作业 | P2 | 服务重启 | 作业状态丢失 | 路线图 P2 项：持久化 | 否 |
| 用户工作树删除了状态文件（本会话期间） | 低 | 本文件再次被删 | 状态丢失 | 已在 git HEAD 留底，可 `git show HEAD:参考的结果计划指南.md` 取回 | 否（不回滚用户操作） |

---

## Implementation Batches

_分析阶段不规划实施批次；待 N5 路线图产出后填充。_

---

## Validation Matrix

| Requirement | Validation Method | Actual Command/Action | Result | Evidence |
|--------------|-------------------|----------------------|--------|----------|
| 主项目真实状态映射 | N1 子代理只读侦察 | Read/Grep/Glob/Bash on 主项目 | DONE | 见 Project Inventory |
| 视觉参考项目清单 | N2 子代理递归发现 | Explore on 视觉参考项目 | DONE | Top5 + 10 项目清单 |
| 智能渗透参考项目清单 | N3 子代理递归发现 | Explore on 智能渗透参考项目 | IN_PROGRESS | 待回报 |
| CL4R1T4S 提示词模式 | N4 子代理深挖 | technical-researcher on CL4R1T4S | DONE | 8 节 + Top5 可借鉴模式 |
| 9 节分析报告 | N5 综合 | — | PENDING | 待填充 |
| 独立审查 | N6 Critic | — | PENDING | 待填充 |
| 终验 | N7 Evaluator | — | PENDING | 待填充 |

---

## Review Findings

_待 N6 审查后填充。_

---

## Next Gate

- **下一步动作**：等待 N3（智能渗透参考项目）回报后，由总协调执行 N5（SynthesisArchitect）综合对标与差距分析，产出 9 节分析报告。
- **是否需要用户批准**：否（当前仍在 ANALYSIS_ONLY）。9 节报告交付后，进入 Phase B 实施需用户明确批准具体路线图项目与批次。
- **需要批准的准确范围**：P0（Docker CMD 切换）、P1（PyQt6 清理 + CI 覆盖 Web + spec 更新）、Agent 系统增强（think 工具/多模块提示词/双模门禁/DONE-BLOCK-NONE/工具三段式）等，均待用户确认后才实施。

---

_最后更新：N1/N4 回报并入；状态文件因工作树删除已重建（不回滚用户操作）_
