# 参考的结果计划指南 — Video Analysis Pro 跨项目对标与扩展路线

> **单一状态源（Single Source of Truth）**。Phase A 分析专用。
> 分析阶段除本文件外不修改任何源代码/配置/依赖。
> 旧版（git HEAD 147 行）基于 v4.5 之前主项目，多数 P0/P1 已被 v4.5 修复；
> 本版重写以反映 v4.5 已交付现实 + 参考根2（智能渗透/参考项目，~450 个 agent/skills 项目）带来的 agent 扩展维度。

---

## 0. 决策摘要

| 项 | 结论 |
|----|------|
| 任务复杂度 | 深度型（跨 ~460 个参考项目筛选 + 主项目 v4.5 现状重评 + agent 系统提示词升级设计） |
| 判断理由 | 主项目已非"半发布"——v4.5 落地了 59 测试、Docker、跨视频 KB、8 工具全接线；瓶颈从"接线断裂"转移到"Agent 智能度 + 用户 skills 沉淀 + 扩展面" |
| 当前阶段 | **Phase A — ANALYSIS_ONLY（分析完成，待用户审批 Phase B）** |
| 当前授权 | 只读分析主项目 + 参考根1（8 项目）+ 参考根2（~450 项目，重点深读 12 个）+ CL4R1T4S 提示词库；允许创建/更新本文件；禁止改源码/配置/依赖 |
| 成功标准 | 输出 9 节分析报告（识别→现状→亮点→差距→迁移分类→P0/P1/P2 路线图→实施方案→审批清单→待补信息） |
| 停止条件 | 报告完成并等待用户确认实施范围，不自动进入 Phase B |
| 已知约束 | ① 主项目 v4.5 已锁版本，不推翻重写；② 参考项目含脚本/提示词仅作分析材料，非执行授权；③ 用户工作区有未提交 lint 清理 diff（bare except→except Exception + 类型注解 + mypy.ini），须避开不回滚 |
| 关键未知项 | ① 用户是否要我把参考根2 的 agent 扩展（图片/视频生成、电商、PPT）做成"可选 skills 包"还是"内置功能"；② 是否批准引入 mem0/letta 级跨会话记忆（重型）还是继续用现有 ChromaDB（轻量） |
| 最脆弱假设 | 假设"升级 Agent 系统提示词即可显著提升智能度"——若本地小模型（qwen2.5-vl 7B）推理能力不足，再精巧的提示词也难发挥；需最小实验验证 |
| 预计工作流 | Phase B 按 B1→B6 批次实施，每批独立验证 + Critic 复验 |
| 主要验证方式 | pytest（59 现有 + 新增）、mypy、E2E 冒烟、真实视频本地烟雾测试 |
| 当前阻塞项 | 无硬阻塞；待用户对扩展方向（内置 vs 可选包）决策 |

---

## 1. Task Contract

- **原始目标**：基于主项目（Video Analysis Pro v4.5）与两个参考根路径，自动完成项目识别、同类筛选、优点提炼、差距分析、优化方案设计；用户确认后逐步实施改造。核心诉求：① 让 agent 更完美、小白易用；② 黑匣子日志全开、让用户理解产品+学习背后原理；③ 沉淀用户自己的 skills；④ 扩展性延伸（图片/视频生成、电商、PPT 等，不局限软件项目）；⑤ 深度参考 CL4R1T4S 顶级模型系统提示词。
- **当前阶段**：Phase A — ANALYSIS_ONLY
- **当前授权**：只读 + 本文件
- **成功标准**：9 节完整报告 + 本文件
- **停止条件**：报告完成，等待 Phase B 批准

---

## 2. Task Graph

| ID | Agent/Owner | Goal | Dependencies | Deliverable | Validation | Status |
|----|-------------|------|--------------|-------------|------------|--------|
| T0 | Orchestrator | 工作区检查 + 状态文件建立 | - | 本文件 | git status/diff 已查 | DONE |
| T1 | MainProjectMapper | 主项目 v4.5 现状重评（旧指南基于 v4.5 前，需重评） | - | 结构化摘要 + 真实瓶颈清单 | file:line 证据 | DONE |
| T2 | DiffAndRiskAuditor | 未提交 diff 风险检查 | - | lint 清理 diff 记录 | git diff | DONE |
| T3a-h | Scout×8（参考根1） | 8 个视觉参考项目亮点（沿用旧指南 T3 结论） | - | 项目卡片×8 | 旧指南已 DONE | DONE（沿用） |
| T4a | Scout: CL4R1T4S | 顶级模型系统提示词模式（沿用旧指南，深读 MANUS/REPLIT/DEVIN/OPENAI/ANTHROPIC/CURSOR） | - | 15 模式 + 16 反面模式 | 旧指南已 DONE | DONE（沿用+扩展） |
| T4b | Scout: 参考根2 高价值项目 | ~450 项目筛选，深读 12 个 | - | 项目卡片×12 | file 证据 | DONE |
| T5 | SynthesisArchitect | 评分筛选、差距分析、路线图 | T1-T4 | 分析报告 | 逐项证据 | DONE |
| T6 | User Gate | 用户确认实施范围 | T5 | 批准批次清单 | 用户回复 | PENDING |

---

## 3. Project Inventory

### 3.1 主项目识别摘要（v4.5 重评）

- **类型**：单体桌面应用（PyQt6 GUI），本地 AI 视频分析工具
- **技术栈**：Python 3.10+ / PyQt6 / OpenCV / ultralytics(YOLO11) / faster-whisper / MoviePy 2.x / sentence-transformers(CLIP) / ChromaDB / SQLite / requests(Ollama+OpenAI 格式) / keyring
- **规模**：src 15 个 py 文件（main_window.py 2355 行、logic.py 1335 行、agent_tools.py 310 行、history_manager.py 264 行、kb_indexer.py 82 行 + 11 个 UI/server/utils 模块）；tests 11 文件 59 测试；launcher 352 行
- **架构**：三阶段流水线（ExtractionWorker→AnalysisWorker→MediaWorker）+ Agent 面板（ChatWorker ReAct 循环）+ 模型管理（ModelManager + ModelContextManager 显存互斥）+ 跨视频 KB（KBIndexWorker→ChromaDB kb_frames collection）
- **业务链路**：Phase1 抽帧(OpenCV+智能场景切分)+转录(faster-whisper) → KB 索引(clip-ViT-B-32) → Phase2 LLM 总结(逐帧拼文本提示词) → Phase3 MoviePy 集锦/GIF → Agent 面板(8 工具 ReAct)
- **数据层**：SQLite(sessions/checkpoints) + ChromaDB(kb_frames 全局 + session_{id} 会话级) + INI 配置 + keyring 凭据 + TXT 提示词模板(3 份)
- **分发**：PyInstaller spec(onedir+内置 FFmpeg) + Docker(CPU/CUDA) + headless HTTP + 跨平台启动脚本
- **v4.5 已修复（旧指南 P0/P1，逐条验证）**：
  - ✅ ExtractionWorker 返回 duration（main_window.py:144）
  - ✅ 智能关键帧 chk_smart 接线 extract_smart_keyframes（main_window.py:106-110）
  - ✅ 8 个 Agent 工具全部注册到 init_backend（main_window.py:1655-1730，含 search_web/search_visual/run_ocr/create_highlights/point_and_jump/search_kb）
  - ✅ seek_video 定义在 DesktopApp 类上（main_window.py:1735）
  - ✅ on_download_finished/plot_metrics 无重复定义（仅 1300/2165 各一）
  - ✅ OllamaClient 在客户端层解析 SSE，产出纯文本 delta（logic.py:671-710）
  - ✅ __FULL_RESPONSE_END__ 哨兵不再泄漏
  - ✅ API Key 用 keyring 存储（config_manager.py:11-40）
  - ✅ moviepy 2.x 迁移完成
  - ✅ 跨视频 KB 落地（history_manager.py:91-180，kb_indexer.py 全文）
- **v4.5 后仍存的真实瓶颈（Phase B 候选）**：见 §6 现状总结

### 3.2 参考项目根1（视觉参考项目）候选清单（沿用旧指南，8 个全部有效）

| # | 项目 | 类型 | 相似度 | 评级 |
|---|------|------|--------|------|
| 1 | video-analyzer | CLI 视频分析（同域） | ★★★★★ | 高价值 |
| 2 | claude-video-vision | Claude 视频感知插件+MCP | ★★★★ | 高价值 |
| 3 | video-search-and-summarization | NVIDIA VSS 蓝图 | ★★★ | 高价值（架构） |
| 4 | edit-mind | 本地视频知识库+插件架构 | ★★★ | 高价值 |
| 5 | VibeNVR | NVR 监控系统 | ★★ | 高价值（扩展） |
| 6 | yolo-training-template | YOLO 训练模板 | ★★ | 中价值 |
| 7 | VideoPipe | C++ pipeline 框架 | ★ | 中价值（思想） |
| 8 | Deep-Learning-Experiments | 教学材料 | ★ | 低价值 |

### 3.3 参考项目根2（智能渗透/参考项目，~450 个）高价值候选清单

> 用户核心诉求：agent 扩展面、用户 skills 沉淀、黑匣子透明化、不局限软件项目（图片/视频生成、电商、PPT 等）。下列 12 个按"诉求匹配度"精选。

| # | 项目 | 类型 | 与主项目诉求匹配点 | 评级 |
|---|------|------|-------------------|------|
| A | **CL4R1T4S** | 顶级模型系统提示词库（28 厂商：Anthropic/OpenAI/Google/Manus/Devin/Replit/Cursor...） | ⑤ CL4R1T4S 是核心参考——Agent 系统提示词升级直接素材 | ★★★★★ |
| B | **video-use** | Claude Code 视频编辑 skill（SKILL.md 工作流哲学） | 工作流哲学：LLM reasons from transcript、Audio primary visuals follow、Ask→confirm→execute→iterate→persist、Verify before showing | ★★★★★ |
| C | **awesome-llm-apps/multimodal_video_moment_finder** | Gemini Embedding 2 + ChromaDB 跨模态搜索 | 图搜帧/文搜帧（主项目 search_visual/search_kb 进阶：当前只用 CLIP 文本→帧，未做图→帧） | ★★★★ |
| D | **skills (dzhng)** | "software factories" skills 库（authoring/engineering/graphics/visual） | fog-of-war territory mapping、eli5（plain-language 解释）= 黑匣子透明化参考 | ★★★★ |
| E | **superpowers** | 完整软件开发方法论 skills 框架 | brainstorming→TDD→verification 闭环，与"让小白学习背后原理"契合 | ★★★★ |
| F | **skills-manager** | 跨工具 skills 管理 GUI（Tauri+React） | "沉淀用户自己的 skills"的 UX 参考（管理/启用/禁用 skills） | ★★★★ |
| G | **OpenHands** | codeact agent + microagent + skills 体系 | agent_loop 工程化、microagent 触发式注入、system_prompt.j2 模板化 | ★★★ |
| H | **mem0** | 跨会话记忆层（Memory.add/search） | HistoryManager 的进阶方向（跨视频/跨会话用户偏好记忆） | ★★★ |
| I | **agent-orchestrator / gsd-2-main** | 多 agent 编排 + Kanban 任务管理 | Agent 面板扩展为"多步任务规划+进度看板"参考 | ★★★ |
| J | **hermes-agent** | optional-skills 分类体系（autonomous/blockchain/creative/devops/email/health/mlops/research/security） | 扩展面 skills 组织参考（电商/PPT/图片视频生成如何分类） | ★★★ |
| K | **deep-eye / anything-analyzer** | 多 provider AI 桌面应用 | UX 对比（多 AI provider 切换、抓包+AI 分析） | ★★ |
| L | **claude-skills-main / everything-claude-code-main / ECC** | 233+ Claude Code skills/plugins 库 | skills 编写标准 + 大量现成可移植 skills | ★★★ |

#### 排除/降级说明（参考根2）
- ~440 个项目未列入高价值，原因：① 安全渗透类（AutoSploit/AutoPentestX/strix/pentagi 等）与主项目"视频分析+agent 易用"域不匹配，仅在"安全响应协议"层面有间接价值；② 重复（-main 后缀为镜像）；③ CLI/harness 框架类（caveman/ralph/gsd 等）方法论可借但实现栈差异大；④ token 优化类（claude-token-optimizer 等）对桌面单机应用价值低。
- **不相关项目的深层价值**（回应用户"为啥放不相关的"）：安全渗透类展示了**多工具编排+权限边界+审计日志**的工程范式，可迁移到"Agent 工具调用的权限护栏"；captcha 类展示了**多 provider 容错+降级链**，可迁移到"多 LLM 后端故障切换"。这些是**方法论迁移**，非功能移植。

### 3.4 高价值参考模块精选（跨两个参考根）

1. **video-analyzer**（根1）：两阶段提示词（逐帧描述→汇总重构）+ 上下文累积帧分析 + PromptLoader 双目录回退
2. **claude-video-vision**（根1）："感知层而非解释层"哲学 + video_analyze 先结构分析再抽帧 + 按意图选 ffmpeg 滤镜 + 帧级变速抽帧
3. **edit-mind**（根1）：AnalyzerPlugin 插件体系（objects/faces/text/color/shot_type/descriptor 六插件）+ BaseProcessingService 抽象 + BullMQ 后台任务 + 帧元数据 TypedDict
4. **VibeNVR**（根1）：CameraThread 模块化（stream_reader/motion/ai/record 分离）+ 双流优化 + CONTEXT.md 工程宪章
5. **NVIDIA VSS**（根1）：告警验证工作流（感知→行为分析→VLM 二次确认降误报）+ 长视频分块摘要聚合
6. **CL4R1T4S**（根2）：Manus 事件流+agent_loop+todo.md+Planner/Knowledge/Datasource 模块；Devin 规划/标准/编辑三模式+block_on_user_response；Replit 最小往返+feedback_tool+search_filesystem；OpenHands codeact system_prompt.j2 分段模板
7. **video-use**（根2）：7 条工作流原则 + 12 条 Hard Rules（production correctness）+ 目录布局（project.md 记忆+takes_packed.md 紧凑转录+edl.json 剪辑决策）
8. **multimodal_video_moment_finder**（根2）：Gemini Embedding 2 原生跨模态（图→帧/文→帧），1fps 抽帧 + cosine similarity + 点击跳转
9. **skills (dzhng)**（根2）：fog-of-war 地图递归切片 + eli5 plain-language 寄存器 + write-spec/close-spec/implement-spec 闭环
10. **superpowers**（根2）：brainstorming→writing-plans→TDD→verification-before-completion→finishing-a-branch 14 skills 闭环
11. **OpenHands microagent**（根2）：frontmatter metadata + 触发式注入（按关键词/文件路径激活对应 microagent）
12. **mem0**（根2）：Memory.add（自动抽取事实）+ Memory.search（语义召回）+ 跨会话用户画像

---

## 4. Evidence Ledger

| Claim | Evidence | Type | Confidence |
|-------|----------|------|------------|
| 主项目 v4.5 已交付 59 测试 | tests/ 11 文件，grep def test_ 计数 59 | DIRECT | 高 |
| 8 个 Agent 工具全部注册 init_backend | main_window.py:1655-1730（含 search_web/search_visual/run_ocr/create_highlights/point_and_jump/search_kb） | DIRECT | 高 |
| seek_video 已定义在 DesktopApp 类上 | main_window.py:1735 | DIRECT | 高 |
| ExtractionWorker 返回 duration | main_window.py:144, 1926 消费 | DIRECT | 高 |
| 智能关键帧已接线 | main_window.py:106-110（chk_smart→extract_smart_keyframes） | DIRECT | 高 |
| OllamaClient 已解析 SSE 产出纯文本 | logic.py:671-710（yield content 而非 raw JSON） | DIRECT | 高 |
| 跨视频 KB 已落地 | history_manager.py:91-180（add_frame_to_kb/search_kb/kb_count）+ kb_indexer.py 全文 + main_window.py:306 KBIndexWorker | DIRECT | 高 |
| API Key 用 keyring | config_manager.py:11-40（_secure_set/_secure_get，DPAPI/Keychain/SecretService） | DIRECT | 高 |
| **P1 真实瓶颈**：Agent 系统提示词薄弱——inject_agent_system_context 仅拼"--- System Context ---"+视频元数据，无 identity/agent_loop/todo/finalize 守卫 | main_window.py:2257-2286 | DIRECT | 高 |
| **P1**：ChatWorker ReAct 历史膨胀——current_prompt 累积拼接重发，max_turns=10，工具结果截断 100 字符，args 靠猜 JSON 或猜 key 名 | main_window.py:191-235（`args = {"query": args_str} if "search" in tool_name else {"seconds": args_str}`） | DIRECT | 高 |
| **P1**：create_highlight_cut_tool 仍硬编码前3帧±2秒，忽略 description | agent_tools.py:211-247（`valid_frames = [f for f in app.frames if f.vision_content][:3]`） | DIRECT | 高 |
| **P2**：search_visual 工具每次重载 SentenceTransformer + 全帧重编码（无缓存），与 kb_indexer 的 shared_embedder 不复用 | agent_tools.py:167-174 vs kb_indexer.py:18-40 | DIRECT | 高 |
| **P2**：APIGatewayClient 用字符串分隔符 "--- System Context ---" 解析 system 消息（脆弱协议） | logic.py:781-789 | DIRECT | 高 |
| **P2**：OllamaClient 硬编码 num_ctx=4096 | logic.py:664 | DIRECT | 高 |
| **P2**：plot_metrics 仍为基础 matplotlib 折线（README 宣称"高级可视化报表"） | main_window.py:2165 | DIRECT | 高 |
| **P2**：默认 API 端点硬编码 iflow.cn | config_manager.py:75, api_intro_page.py:21-56 | DIRECT | 高 |
| 未提交 diff 为 lint/类型清理（非功能改动） | git diff：bare except→except Exception ×6 + Optional[Dict] 类型注解 + assert self.model is not None + mypy.ini 新增 | DIRECT | 高 |
| video-use SKILL 工作流哲学 | video-use/SKILL.md:1-60（7 原则+12 Hard Rules） | DIRECT | 高 |
| Manus 提示词 6 模块（Event Stream/Agent Loop/Planner/Knowledge/Datasource/Todo） | CL4R1T4S/MANUS/Manus_Prompt.txt:30-160 | DIRECT | 高 |
| Devin 三模式（planning/standard/edit）+ block_on_user_response | CL4R1T4S/DEVIN/Devin2_09-08-2025.md:40-60 | DIRECT | 高 |
| Replit feedback_tool + search_filesystem + 最小往返 | CL4R1T4S/REPLIT/Replit_Agent.md:1-40 | DIRECT | 高 |
| OpenHands codeact system_prompt.j2 分段（ROLE/EFFICIENCY/FILE_SYSTEM/CODE_QUALITY/VERSION_CONTROL） | OpenHands/openhands/agenthub/codeact_agent/prompts/system_prompt.j2:1-35 | DIRECT | 高 |
| multimodal_video_moment_finder 图→帧/文→帧跨模态搜索 | awesome-llm-apps/.../multimodal_video_moment_finder/README.md:1-30 | DIRECT | 高 |
| skills(dzhng) fog-of-war + eli5 寄存器 | skills/skills/engineering/eli5/SKILL.md:1-30 | DIRECT | 高 |
| mem0 Memory.add/search API | mem0/mem0/memory/main.py:760,1379 | DIRECT | 高 |

---

## 5. Decisions and Assumptions

### 已确认事实
- 主项目 v4.5 已交付（59 测试、Docker、KB、8 工具全接线、keyring、跨平台）——旧指南 P0/P1 多数已修复
- 用户工作区有未提交 lint 清理 diff（bare except + 类型注解 + mypy.ini），须避开不回滚
- 参考根1 8 项目 + 参考根2 ~450 项目均经扫描，无空壳/损坏；CL4R1T4S 28 厂商提示词可读
- MCP 图谱服务（code-review-graph/graft/context7）全部连接失败，已改用直接扫描策略

### 当前假设
- 12 个参考根2 高价值项目均为可读代码库（已逐一验证 README/目录结构）
- 用户期望的"agent 扩展面"（图片/视频生成、电商、PPT）以"可选 skills 包"形式落地，而非全部内置（避免臃肿）
- 用户期望"沉淀用户 skills"指用户可定义/管理自己的分析模板/工作流，而非训练模型

### 架构决策（Phase A 结论性判断）
1. **不推翻重写**——v4.5 三阶段链路成立，瓶颈在"Agent 智能度+扩展面"，走渐进增强
2. **迁移思想不迁移代码**——参考项目（Python/TS/Go/C++）按设计思想跨栈适配
3. **Agent 系统提示词升级优先于新功能**——参考 CL4R1T4S 6 模块重写，是"让 agent 更完美"的最小高收益动作
4. **扩展面用 skills 包机制**——参考 hermes-agent/skills-manager，内置一批 + 用户可加，不全部硬编码进核心
5. **黑匣子透明化用 eli5 + 可观测日志**——参考 skills(dzhng) eli5 + superpowers verification-before-completion

### 被否决的方案
- 引入 mem0/letta 重型记忆框架（桌面单机形态不匹配，现有 ChromaDB 已够）
- 迁移 OpenHands/agent-orchestrator 多 agent 编排（主项目单 agent 形态，编排过度）
- 把参考根2 安全渗透项目功能移植（域不匹配，仅借方法论）
- 把 video-use 全套 Hard Rules 硬套（主项目是分析非剪辑，取哲学不取规则）

---

## 6. 主项目现状总结（v4.5 重评）

### 当前架构特点
- 三阶段流水线（ExtractionWorker→AnalysisWorker→MediaWorker）+ Agent 面板（ChatWorker ReAct）+ 模型管理（显存互斥）+ 跨视频 KB（ChromaDB kb_frames）
- MVC 变体：UI 层（src/ui/）+ 核心逻辑层（src/core/）+ 工具层（agent_tools.py）+ utils
- main_window.py 2355 行仍是"God Object"倾向（DesktopApp 含 ~74 方法 + 10 Worker 类），但比 v4.5 前已有改善

### 当前优点（v4.5 已落地）
1. 三阶段链路清晰，UI 有加载态/任务面板/资源监控（status_console.py）
2. ModelContextManager 显存互斥 + CUDA 健康检查 + CPU 回退（logic.py:52）设计成熟
3. ChromaDB 跨视频 KB 已落地（kb_frames 全局 collection + search_kb 工具 + KBIndexWorker 后台索引）
4. 8 个 Agent 工具全接线（含 search_kb 跨视频搜索）
5. Ollama SSE 在客户端层解析，产出纯文本 delta
6. API Key 用 OS 密钥环（DPAPI/Keychain/SecretService）
7. 59 个 pytest 测试（含 E2E 冒烟 + 全链路）
8. Docker + headless HTTP 服务（/healthz + /analyze）
9. 跨平台启动脚本 + PyInstaller spec
10. moviepy 2.x 迁移完成

### 当前主要问题（v4.5 后真实瓶颈，按优先级）

#### P1 — 影响"agent 更完美"核心诉求
- **P1-1 Agent 系统提示词薄弱**（main_window.py:2257-2286）：仅拼"--- System Context ---"+视频元数据，无 identity/agent_loop/todo/finalize 守卫。对照 CL4R1T4S（Manus 6 模块、Devin 三模式、OpenHands 分段 system_prompt.j2），差距巨大。
- **P1-2 ChatWorker ReAct 历史膨胀**（main_window.py:191-235）：current_prompt 累积拼接重发（每轮把 full_response+tool_output 拼回 prompt），max_turns=10，工具结果截断 100 字符展示，args 解析靠猜（`{"query": args_str} if "search" in tool_name`）。导致：① token 浪费；② 多轮后上下文爆炸；③ 参数解析脆弱。
- **P1-3 create_highlight_cut_tool 硬编码**（agent_tools.py:211-247）：忽略 description 参数，取前3帧各±2秒。README 宣称"AI 自动挑选精彩片段"实为摆设。
- **P1-4 黑匣子未透明化**：Agent 思考过程（ThinkingWidget）有，但：① 工具调用参数/完整返回值不可见（截断 100 字符）；② 无"为什么选这个工具"的推理展示；③ 无 eli5 plain-language 解释；④ 无可观测日志面板（当前仅 txt_logs 500 行滚动）。

#### P2 — 影响"扩展性延伸"与工程质量
- **P2-1 search_visual 无缓存**（agent_tools.py:167-174）：每次重载 SentenceTransformer + 全帧重编码，与 kb_indexer shared_embedder 不复用。
- **P2-2 APIGatewayClient 脆弱协议**（logic.py:781-789）：用字符串分隔符 "--- System Context ---" 解析 system 消息。
- **P2-3 OllamaClient 硬编码 num_ctx=4096**（logic.py:664）：长视频分析时上下文不足。
- **P2-4 默认 API 端点硬编码 iflow.cn**（config_manager.py:75, api_intro_page.py:21-56）：商业推荐嵌入默认配置，非中立。
- **P2-5 plot_metrics 基础折线**（main_window.py:2165）：README 宣称"高级可视化报表"实为简单 matplotlib。
- **P2-6 无 skills 沉淀机制**：用户无法定义/管理自己的分析模板（现有 prompts.json 仅存模板文本，非工作流）；无法扩展非视频域（图片生成/电商/PPT）。
- **P2-7 无跨模态图→帧搜索**：search_visual 仅文→帧（CLIP 文本编码→帧图像编码），未做图→帧（用户上传截图找时刻，multimodal_video_moment_finder 已验证可行）。
- **P2-8 main_window.py God Object**：2355 行 + ~74 方法 + 10 Worker 类，新功能难加入。

#### P3 — 长期增强
- P3-1 训练中心 tab（用户自训模型，yolo-training-template 参考）
- P3-2 实时流处理（RTSP，VibeNVR 参考）
- P3-3 Web 版（FastAPI+React，VSS 参考）
- P3-4 插件系统（edit-mind AnalyzerPlugin 参考）

### 风险点
- 本地小模型（qwen2.5-vl 7B）推理能力有限——升级提示词后效果需最小实验验证
- 打包体积（torch+ultralytics+paddle >3GB）——扩展 skills 不应再增核心体积
- 用户未提交 diff 须保护——Phase B 实施前先确认是否纳入

### 技术债
- main_window.py God Object（P2-8）
- agent_tools.py 部分工具实现简陋（P1-3, P2-1）
- 未提交的 lint 清理 diff 未完成（bare except→except Exception 已改 6 处，mypy.ini 已建，但未全量通过 mypy）

---

## 7. 参考项目亮点提炼

### 7.1 按项目分别列出亮点（高价值 12 个）

#### A. CL4R1T4S（核心参考——Agent 系统提示词升级直接素材）
- **Manus**：6 模块架构——Event Stream（Message/Action/Observation/Plan/Knowledge/Datasource）+ Agent Loop（Analyze→Select Tools→Wait→Iterate→Submit→Standby）+ Planner Module（编号伪代码步骤+反思）+ Knowledge Module（最佳实践按 scope 注入）+ Datasource Module（权威 API 优先于 web search）+ Todo Rules（todo.md 清单+完成即替换标记）+ Message Rules（notify 非阻塞/ask 阻塞，首回复仅确认收到）+ Tool Use Rules（必须 function calling、不提工具名、不臆造工具）
- **Devin**：三模式（planning 收集信息→suggest_plan / standard 按计划执行+新反馈先调查再改 / edit）+ block_on_user_response（BLOCKED/DONE 状态机）+ Truthful and Transparent（不造假数据/不 mock 过测/不假装修好）+ Coding Best Practices（先看邻近代码风格、不假设库可用、imports 顶部）
- **Replit**：最小往返原则 + feedback_tool（web_application_feedback_tool 让用户验证）+ search_filesystem 优先于 shell 定位 + str_replace_editor + LSP 错误先修再 ask feedback + 不改 DB 表/不用 DELETE/UPDATE 除非明确要求
- **OpenHands codeact**：system_prompt.j2 分段（ROLE/EFFICIENCY/FILE_SYSTEM_GUIDELINES/CODE_QUALITY/VERSION_CONTROL）+ microagent 触发式注入（frontmatter metadata + 按关键词/文件路径激活）
- **Cursor**：工具说明详尽（codebase_search 语义搜索、read_file 范围责任、run_terminal_cmd 用户审批、grep_search 50 匹配上限）
- **Anthropic Opus 5**：product_information（模型版本/Mythos 级）+ default_stance（默认帮助，仅具体严重风险才拒绝）+ refusal_handling（可客观讨论任何主题）

#### B. video-use（工作流哲学核心参考）
- 7 原则：① LLM reasons from raw transcript + on-demand visuals（唯一派生工件是 takes_packed.md）；② Audio primary, visuals follow（切口来自语音边界+静音间隙，决策点才看画面）；③ Ask→confirm→execute→iterate→persist（用户确认前不碰剪辑）；④ Generalize（不预设视频类型）；⑤ Artistic freedom default（所有具体值是 worked example 非 mandate）；⑥ Invent freely（helpers 是 ffmpeg+PIL，能做格式支持的任何东西）；⑦ Verify own output before showing
- 12 Hard Rules（production correctness）：字幕最后加、分段提取+lossless concat、30ms 音频淡变、setpts 偏移、master SRT 输出时间轴偏移、不切断词内、每切口垫 30-200ms、词级 verbatim ASR、转录缓存、并行子 agent 生成动画、策略确认后执行、输出在 videos_dir/edit/
- 目录布局：project.md（记忆，每会话追加）+ takes_packed.md（短语级转录）+ edl.json（剪辑决策）+ transcripts/缓存 + animations/slot_<id>/ + clips_graded/ + master.srt + verify/

#### C. multimodal_video_moment_finder（跨模态搜索进阶）
- 1fps 抽帧 + Gemini Embedding 2 原生跨模态 + ChromaDB + 图搜帧/文搜帧 + 点击跳转 + 不转录/不 OCR/不 caption（embedding 直接理解视觉）

#### D. skills (dzhng)（黑匣子透明化 + 软件工厂）
- fog-of-war territory mapping：把未知当战争迷雾，地图地形→切成可独立构建验证的领土→递归再切隐藏地图
- eli5 寄存器：其他 skill 输出指向此处当需对未参与工作的人可读——走一个具体场景端到端、首次使用定义每个术语、小具体例子不替换精确声明、控制流用伪代码
- write-spec/close-spec/implement-spec 闭环 + audit-choices/audit-performance

#### E. superpowers（让小白学习背后原理）
- 14 skills 闭环：brainstorming（创造性工作前必用）→ writing-plans（规格→多步任务计划）→ executing-plans（计划+审查检查点）→ test-driven-development（RED→GREEN→IMPROVE）→ verification-before-completion（宣称完成前必运行验证命令）→ finishing-a-development-branch（合并/PR/清理）
- session-start hook + dispatching-parallel-agents + systematic-debugging

#### F. skills-manager（用户 skills 沉淀 UX）
- Tauri+React 桌面 GUI，管理跨工具（Claude Code/Codex/Cursor 等）skills
- library/启用/禁用/安装/卸载 + skills.sh 分发

#### G. OpenHands（agent_loop 工程化）
- codeact agent + microagent（触发式注入）+ skills 体系 + critic（独立审查）+ events（Action/Observation）+ resolver
- system_prompt.j2 模板化分段 + additional_info.j2/microagent_info.j2 动态拼接

#### H. mem0（跨会话记忆进阶方向）
- Memory.add（自动抽取事实）+ Memory.search（语义召回）+ 跨会话用户画像 + 多后端（ChromaDB/Qdrant/Postgres）

#### I. agent-orchestrator / gsd-2-main（多步任务规划+看板）
- 每 coding 任务独立 agent+workspace+feedback loop + 项目感知 orchestrator + Kanban（worker/PR/CI/review 实时）
- gsd-2：plan→execute→verify 闭环 + context 管理 + stuck loop 检测 + crash 恢复 + 自动推进里程碑

#### J. hermes-agent（扩展面 skills 分类）
- optional-skills/：autonomous-ai-agents/blockchain/communication/creative/devops/email/health/mlops/migration/research/security
- skills/：apple/autonomous/creative/data-science/devops/diagramming/dogfood/domain/email/feeds/gaming/github/...
- toolset_distributions.py（工具集分发）+ acp_registry（agent 通信协议注册）

#### K. deep-eye / anything-analyzer（多 provider AI 桌面对比）
- deep-eye：多 ai_providers + plugins + proxy + modules
- anything-analyzer：Electron+React+TS，全场景抓包（网页/桌面/终端/Python/手机）+ AI 自动逆向分析

#### L. claude-skills-main / everything-claude-code-main / ECC（现成 skills 库）
- 233+ production-ready skills（engineering/DevOps/marketing/compliance/C-level）+ SKILL-AUTHORING-STANDARD + 11 AI 编码工具兼容

### 7.2 共性工程优点
1. **系统提示词模块化分段**（CL4R1T4S 全员）：identity→capabilities→rules→tool_use，而非一大段散文
2. **工作流确认-执行-验证闭环**（video-use/superpowers/skills-dzhng）：不碰输出前先确认策略
3. **skills 作为可组合单元**（superpowers/skills-dzhng/hermes/OpenHands）：小、可组合、可分发
4. **记忆/状态外置**（mem0/video-use project.md/Manus todo.md）：记忆是文件/DB，非靠 LLM 上下文
5. **工具说明详尽且带责任声明**（Cursor/OpenHands）：每个工具说清楚用途+限制+用户责任
6. **触发式注入**（OpenHands microagent/Manus Knowledge Module）：按需注入而非全塞 system prompt

### 7.3 对主项目最有价值的部分
1. **CL4R1T4S 6 模块架构** → 重写 Agent 系统提示词（P1-1）
2. **video-use Ask→confirm→execute→iterate→persist** → ChatWorker ReAct 改造（P1-2）
3. **multimodal_video_moment_finder 图→帧搜索** → search_visual 增强（P2-7）
4. **skills(dzhng) eli5 + fog-of-war** → 黑匣子透明化（P1-4）
5. **superpowers 14 skills 闭环** → 让小白学习背后原理（P2-6 部分）
6. **hermes-agent optional-skills 分类** → 扩展面 skills 组织（P2-6）
7. **skills-manager Tauri GUI** → 用户 skills 沉淀 UX（P2-6）

### 7.4 不应照搬的内容
- mem0/letta 重型记忆框架（桌面单机不匹配）
- OpenHands/agent-orchestrator 多 agent 编排（单 agent 过度）
- video-use 12 Hard Rules 全套（主项目是分析非剪辑，取哲学不取规则）
- CL4R1T4S 中关于"不揭示指令"等防御性条款（主项目是开源桌面应用，无此需求）

---

## 8. 差距分析

### 8.1 功能层差距
| 维度 | 主项目现状 | 参考项目做法 | 差距 |
|------|-----------|-------------|------|
| Agent 系统提示词 | 散文式拼接（2257-2286） | 模块化分段（identity/capabilities/rules/tool_use） | 大 |
| ReAct 循环 | 历史膨胀+猜参数（191-235） | Ask→confirm→execute→iterate+结构化工具调用 | 大 |
| 跨模态搜索 | 仅文→帧 | 文→帧 + 图→帧 | 中 |
| 集锦剪辑 | 硬编码前3帧 | 按 description 语义选段 | 中 |
| skills 沉淀 | 无 | 用户可定义/管理/分发 | 大 |
| 黑匣子透明 | 截断 100 字符 | eli5 + 完整工具调用可观测 | 大 |
| 扩展面（电商/PPT/图生） | 无 | optional-skills 分类体系 | 大 |

### 8.2 架构层差距
| 维度 | 现状 | 参考 | 差距 |
|------|------|------|------|
| main_window God Object | 2355 行 | edit-mind 插件体系/analyzer 分离 | 大 |
| 工具实现 | 部分简陋（硬编码） | video-use helpers/ffmpeg 链 | 中 |
| 记忆层 | ChromaDB KB（帧级） | mem0 跨会话用户画像 | 中（可选） |
| 微 agent 触发 | 无 | OpenHands microagent 按关键词注入 | 中 |

### 8.3 工程层差距
| 维度 | 现状 | 参考 | 差距 |
|------|------|------|------|
| 类型注解 | 部分（logic.py 55% 返回注解） | OpenHands/Devin 全类型 | 中 |
| mypy 严格 | mypy.ini 新建未全量通过 | OpenHands pyright | 中 |
| 可观测日志 | txt_logs 500 行滚动 | video-use verify/ 调试帧+时间轴 PNG | 中 |
| 提示词模板 | 3 份 TXT | video-use SKILL.md frontmatter+worked example | 中 |

### 8.4 性能层差距
| 维度 | 现状 | 参考 | 差距 |
|------|------|------|------|
| 模型加载缓存 | kb_indexer shared_embedder，search_visual 不复用 | multimodal_video_moment_finder 帧缓存 | 中 |
| 上下文窗口 | 硬编码 4096 | 可配置 | 小 |
| 长视频 | 逐帧全量 | NVIDIA VSS 分块摘要聚合 | 中 |

### 8.5 安全层差距
| 维度 | 现状 | 参考 | 差距 |
|------|------|------|------|
| API Key | keyring 已用 ✓ | mem0/OpenHands env+secret manager | 已达标 |
| 默认端点 | 硬编码 iflow.cn | 中立空默认 | 小 |
| 工具调用护栏 | 无 | 安全渗透类项目权限边界+审计 | 中 |

### 8.6 运维层差距
| 维度 | 现状 | 参考 | 差距 |
|------|------|------|------|
| Docker | ✓ CPU/CUDA | LightRAG k8s-deploy | 已达标 |
| headless | ✓ /healthz+/analyze | - | 已达标 |
| CI | 三平台测试矩阵 | gsd-2 auto-advance | 已达标 |

### 8.7 测试与真实用户体验差距
| 维度 | 现状 | 参考 | 差距 |
|------|------|------|------|
| 单元/集成/E2E | 59 测试 ✓ | superpowers TDD 80%+ | 中（覆盖率未量化） |
| 真实用户路径 | E2E 冒烟 | video-use verify before showing | 中 |
| 小白易用 | 启动脚本 ✓ | skills-manager 一键管理 | 中（Agent 面板对小白不友好） |

---

## 9. 可迁移 / 半可迁移 / 不建议迁移

### 9.1 可迁移（直接落地）
| 项 | 来源 | 原因 | 预期收益 | 实施成本 | 风险 | 适配方式 | 最小实验 |
|----|------|------|---------|---------|------|---------|---------|
| Agent 系统提示词模块化重写 | CL4R1T4S 6 模块 | 主项目 P1-1 直击 | Agent 智能度显著提升 | 低（改提示词文本） | 低（提示词可回滚） | 按 identity/capabilities/rules/tool_use 分段重写 inject_agent_system_context | 本地 qwen2.5-vl 跑 3 个典型问题对比 |
| ChatWorker ReAct 结构化 | video-use Ask→confirm + Devin block_on_user_response | P1-2 直击 | token 节省+多轮稳定 | 中（改 ChatWorker.run） | 中（影响主交互） | 用 messages 列表替代 current_prompt 累积；args 用 JSON schema 严格解析 | 单元测试覆盖 3 轮工具调用 |
| create_highlight_cut 按 description 语义选段 | video-use edl.json | P1-3 直击 | 集锦真正可用 | 中 | 低 | 用 search_visual 找 top-N 时刻±2s 拼接 | 真实视频测 3 种描述 |
| search_visual 复用 shared_embedder + 帧缓存 | kb_indexer + multimodal_video_moment_finder | P2-1 直击 | 搜索从秒级→毫秒级 | 低 | 低 | 复用 kb_indexer.get_embedder + 缓存帧 embedding 到 KB | 对比调用耗时 |
| 图→帧跨模态搜索 | multimodal_video_moment_finder | P2-7 直击 | 用户上传截图找时刻 | 中 | 低 | 新增 search_by_image 工具，encode 图像→cosine KB | 1 张图测 |
| eli5 黑匣子透明化 | skills(dzhng) eli5 | P1-4 直击 | 小白理解+学习 | 中 | 低 | 工具调用展示完整参数+返回值+eli5 解释 | 1 个工具试点 |
| 可观测日志面板 | video-use verify/ | P1-4 | 调试+学习 | 中 | 低 | 新增可展开的"Agent 决策日志"面板 | - |
| skills 包机制（内置+用户加） | hermes-agent/skills-manager | P2-6 直击 | 扩展面+用户沉淀 | 高 | 中 | 定义 SKILL.md 规范+加载器+管理 UI | 先内置 3 个 skills |

### 9.2 半可迁移（局部借鉴）
| 项 | 来源 | 原因 | 适配方式 |
|----|------|------|---------|
| mem0 跨会话记忆 | mem0 | 桌面单机不需重型，但"跨视频用户偏好"有价值 | 用现有 ChromaDB 加 user_preferences collection |
| OpenHands microagent 触发注入 | OpenHands | 单 agent 不需微 agent，但"按视频类型注入对应提示词"有价值 | 按视频元数据/用户查询关键词动态拼 system prompt 段 |
| gsd-2 多步任务看板 | gsd-2 | 单 agent 不需 Kanban，但"多步分析任务进度"有价值 | Agent 面板加 todo.md 式步骤清单（Manus todo_rules） |
| superpowers 14 skills 闭环 | superpowers | 桌面应用不需全栈方法论，但"verification-before-completion"对"声称完成前验证"有价值 | 内化到 Agent 系统提示词的 finalize 守卫 |

### 9.3 不建议迁移
| 项 | 来源 | 原因 |
|----|------|------|
| mem0/letta 框架本体 | mem0/letta | 重型，桌面单机 ChromaDB 已够 |
| OpenHands/agent-orchestrator 多 agent 编排 | OpenHands/agent-orchestrator | 单 agent 形态过度 |
| video-use 12 Hard Rules 全套 | video-use | 主项目是分析非剪辑 |
| 安全渗透项目功能 | 参考根2 安全类 | 域不匹配，仅借方法论 |
| VideoPipe C++ 代码 | VideoPipe | 生态鸿沟 |
| CL4R1T4S 防御性条款 | CL4R1T4S | 开源桌面应用无此需求 |

---

## 10. 优先级优化路线图

### P0（无——v4.5 已修复全部旧 P0，当前无阻塞/数据破坏/安全漏洞级问题）

### P1（高价值改造，明显提升 agent 智能度+易用性+扩展性）

| ID | 做什么 | 为什么 | 涉及模块 | 前置依赖 | 预期收益 | 风险 | 数据迁移 | 兼容性 | 验证方式 | 回滚 |
|----|--------|--------|---------|---------|---------|------|---------|--------|---------|------|
| P1-1 | Agent 系统提示词按 CL4R1T4S 6 模块重写（identity/capabilities/agent_loop/todo_rules/tool_use_rules/finalize 守卫） | 当前散文式拼接，智能度受限 | main_window.py:2257-2286 inject_agent_system_context | 无 | Agent 智能度显著提升 | 低（文本可回滚） | 无 | 不影响 | 本地 qwen2.5-vl 对比 3 问题 | 还原旧 context_str |
| P1-2 | ChatWorker ReAct 改造：messages 列表替代 current_prompt 累积；args 用 JSON schema 严格解析；工具结果完整展示（不截断） | 历史膨胀+猜参数+截断 | main_window.py:191-235 ChatWorker.run | P1-1 | token 节省+多轮稳定+黑匣子透明 | 中（影响主交互） | 无 | 不影响 | 单元测试 3 轮工具调用 | 还原旧 run |
| P1-3 | create_highlight_cut 按 description 语义选段（复用 search_visual 找 top-N 时刻） | 硬编码前3帧 | agent_tools.py:211-247 | P2-1 | 集锦真正可用 | 低 | 无 | 不影响 | 真实视频 3 描述 | 还原旧逻辑 |
| P1-4 | 黑匣子透明化：工具调用展示完整参数+返回值+eli5 plain-language 解释；新增可展开"Agent 决策日志"面板 | 截断 100 字符+无推理展示 | agent_panel.py + main_window.py ChatWorker | P1-2 | 小白理解+学习 | 低 | 无 | 不影响 | 1 工具试点 | 隐藏面板 |
| P1-5 | 内置 skills 包机制 v0：定义 SKILL.md 规范（frontmatter name/description/triggers）+加载器+管理 UI 雏形 | 无 skills 沉淀机制 | 新增 src/skills/ + src/ui/skills_manager_tab.py | 无 | 扩展面+用户沉淀基础 | 中（新模块） | 无 | 不影响 | 加载 1 个内置 skill | 删除新模块 |

### P2（锦上添花，非当前阶段必须）

| ID | 做什么 | 为什么 | 涉及模块 | 预期收益 | 风险 |
|----|--------|--------|---------|---------|------|
| P2-1 | search_visual 复用 kb_indexer shared_embedder + 帧缓存 | 每次重载重编码 | agent_tools.py:167-174 | 毫秒级搜索 | 低 |
| P2-2 | APIGatewayClient 用结构化 messages 替代字符串分隔符 | 脆弱协议 | logic.py:781-789 | 协议稳健 | 中 |
| P2-3 | OllamaClient num_ctx 可配置 | 硬编码 4096 | logic.py:664 | 长视频不截断 | 低 |
| P2-4 | 默认 API 端点改中立空 | 硬编码 iflow.cn | config_manager.py:75, api_intro_page.py | 中立性 | 低 |
| P2-5 | plot_metrics 升级 seaborn/交互图 | 基础折线 | main_window.py:2165 | 报表专业度 | 低 |
| P2-6 | 图→帧跨模态搜索（search_by_image 工具） | 仅文→帧 | agent_tools.py 新增 | 截图找时刻 | 低 |
| P2-7 | main_window 拆分：Worker 类抽到 src/workers/，DesktopApp 方法按域分组 | God Object 2355 行 | main_window.py | 可维护 | 中 |
| P2-8 | 跨视频用户偏好记忆（ChromaDB user_preferences collection） | 无用户画像 | history_manager.py | Agent 更懂用户 | 中 |

### P3（长期增强）
- P3-1 训练中心 tab（yolo-training-template）
- P3-2 实时流 RTSP（VibeNVR）
- P3-3 Web 版（FastAPI+React，VSS）
- P3-4 插件系统（edit-mind AnalyzerPlugin）

---

## 11. 全栈实施方案

### 11.1 前端（UI）改造建议
- **Agent 面板**（P1-2, P1-4）：ChatWorker 改造后，工具调用气泡展示完整参数+返回值+eli5 解释；新增可展开"Agent 决策日志"侧栏
- **skills 管理 tab**（P1-5）：新增 src/ui/skills_manager_tab.py，列表+启用/禁用+导入外部 skill
- **可观测日志面板**（P1-4）：txt_logs 升级为带级别/过滤/搜索的日志面板，关键决策高亮

### 11.2 后端（核心逻辑）改造建议
- **Agent 系统提示词**（P1-1）：inject_agent_system_context 重写为模块化分段
- **ChatWorker**（P1-2）：messages 列表+JSON schema 严格解析+完整工具结果
- **agent_tools**（P1-3, P2-1, P2-6）：create_highlight_cut 语义化+search_visual 缓存+新增 search_by_image

### 11.3 数据层改造建议
- **ChromaDB**（P2-8）：新增 user_preferences collection（跨视频用户偏好）
- **skills 元数据**（P1-5）：config/skills/ 存 SKILL.md + 加载状态

### 11.4 接口层改造建议
- **APIGatewayClient**（P2-2）：结构化 messages 替代字符串分隔符
- **OllamaClient**（P2-3）：num_ctx 可配置

### 11.5 配置管理建议
- 默认 API 端点改中立空（P2-4）
- skills 配置段（P1-5）

### 11.6 日志与监控建议
- Agent 决策日志面板（P1-4）
- 工具调用审计（参考安全渗透类项目权限边界+审计方法论）

### 11.7 测试策略建议
- P1-1：本地 qwen2.5-vl 对比 3 问题（最小实验）
- P1-2：单元测试 3 轮工具调用 + args 解析
- P1-3：真实视频 3 描述
- P1-5：加载 1 个内置 skill
- 现有 59 测试保持全绿

### 11.8 部署与运维建议
- skills 包作为独立分发单元（参考 skills-manager skills.sh）
- Docker 镜像不含 skills（用户按需挂载）

### 11.9 建议实施批次和依赖顺序
| 批次 | 内容 | 风险 | 依赖 |
|------|------|------|------|
| B1 | P1-1 Agent 系统提示词重写 | L1 | 无 |
| B2 | P1-2 ChatWorker ReAct 改造 | L2 | B1 |
| B3 | P1-4 黑匣子透明化 + P1-3 集锦语义化 + P2-1 search_visual 缓存 | L2 | B2 |
| B4 | P1-5 skills 包机制 v0 | L2 | B1（提示词引用 skills） |
| B5 | P2-2/2-3/2-4/2-5 工程质量收尾 | L1 | 无 |
| B6 | P2-6 图→帧 + P2-7 main_window 拆分 + P2-8 用户偏好 | L2/L3 | B3/B4 |

---

## 12. 实施授权状态

当前**未授权**修改源代码。本节只说明建议优先批准的批次：

| 批次 | 影响范围 | 风险 | 验证方式 | 需单独审批 |
|------|---------|------|---------|-----------|
| B1（P1-1） | main_window.py inject_agent_system_context + 可能新增 config/agent_prompts/ | L1 | 本地模型对比 | 否 |
| B2（P1-2） | main_window.py ChatWorker + agent_panel.py | L2 | 单元测试+真实对话 | 否 |
| B3（P1-3/1-4/P2-1） | agent_tools.py + agent_panel.py | L2 | 真实视频 | 否 |
| B4（P1-5） | 新增 src/skills/ + src/ui/skills_manager_tab.py | L2 | 加载内置 skill | 是（新模块+UI 入口） |
| B5（P2-2/2-3/2-4/2-5） | logic.py + config_manager.py + main_window.py | L1 | 现有测试 | 否 |
| B6（P2-6/2-7/2-8） | agent_tools.py + main_window.py 拆分 + history_manager.py | L2/L3 | 拆分需独立审查 | 是（main_window 拆分影响面大） |

**不得在本阶段实施代码修改。**

---

## 13. 需要我补充的信息

| # | 问题 | 为什么需要 | 不提供会影响什么 | 可否用临时假设继续 | 建议默认 |
|---|------|-----------|----------------|----------------|---------|
| 1 | 扩展面（图片/视频生成、电商、PPT）是"可选 skills 包"还是"内置功能"？ | 决定 P1-5 skills 机制的形态 | skills 包机制设计方向 | 可（先按可选包设计） | 可选 skills 包（参考 hermes-agent optional-skills） |
| 2 | 是否引入跨会话用户偏好记忆（P2-8）？用 ChromaDB 还是更重方案？ | 决定记忆层复杂度 | P2-8 是否纳入 | 可（先用 ChromaDB） | ChromaDB user_preferences collection（不引 mem0） |
| 3 | 用户工作区未提交的 lint 清理 diff（bare except+类型注解+mypy.ini）是否纳入 Phase B 一并提交？ | 避免与用户改动冲突 | Phase B 实施前需明确 | 不可（须用户确认） | 保留用户 diff，Phase B 在其基础上增量 |
| 4 | 本地测试模型是 qwen2.5-vl 7B 还是更大模型？ | P1-1 提示词升级效果依赖模型能力 | 最小实验设计 | 可（先用 7B） | qwen2.5-vl 7B（本地最常用） |
| 5 | 是否批准 P1-5 skills 包机制（新增 UI 入口+新模块）？ | 这是"扩展面+用户沉淀"的核心，但属新模块 | P1-5 是否实施 | 不可（需批准） | 建议批准（高价值） |

---

## 14. Next Gate

- **下一步动作**：向用户交付完整分析报告（本文件 + 对话摘要），等待 Phase B 授权
- **是否需要用户批准**：是
- **需要批准的准确范围**：
  - 建议优先批准 **B1+B2**（P1-1 Agent 提示词 + P1-2 ChatWorker 改造，纯核心增强，低风险高收益，直击"agent 更完美"诉求）
  - 其次 **B3**（P1-3 集锦语义化 + P1-4 黑匣子透明化 + P2-1 缓存，直击"黑匣子全开+小白易用"）
  - **B4**（P1-5 skills 包机制）需单独审批（新模块+UI 入口），但这是"扩展面+用户沉淀"核心，建议批准
  - B5（工程质量收尾）可穿插
  - B6（main_window 拆分等）建议放到 v5.x

---

## 15. Validation Matrix

| Requirement | Validation Method | Actual Command/Action | Result | Evidence |
|-------------|-------------------|----------------------|--------|----------|
| 主项目 v4.5 现状重评 | git show + 逐文件读取 + grep + AST | git status/diff, read main_window/logic/agent_tools/history_manager/kb_indexer | 完成 | §3.1, §6 |
| 参考根1 8 项目沿用 | 旧指南 T3a-h | 旧指南已 DONE | 完成 | §3.2 |
| 参考根2 ~450 项目筛选 | ls + README 速读 + 重点深读 12 个 | ls 参考根2, read README/SKILL.md | 完成 | §3.3, §7 |
| CL4R1T4S 提示词深读 | 逐厂商 head 读取 | MANUS/REPLIT/DEVIN/OPENAI/ANTHROPIC/CURSOR | 完成 | §7.1A |
| 真实瓶颈带 file:line | grep + sed 逐段 | main_window.py:2257-2286 等 | 完成 | §4 Evidence Ledger |
| 用户未提交 diff 识别 | git diff | lint 清理 diff | 完成 | §3.1, §4 |

---

## 16. Review Findings（Phase A 自审）

- T1 mapper（v4.5 重评）：旧指南 P0/P1 多数已修复，逐条验证；新真实瓶颈 P1×5/P2×8/P3×4，全带 file:line 证据
- T2 diff 审计：用户工作区有未提交 lint 清理 diff（bare except+类型注解+mypy.ini），须避开不回滚
- T3a-h（参考根1）：沿用旧指南 8 份 scout 报告
- T4a（CL4R1T4S）：沿用旧指南 15 模式+16 反面模式，新增 MANUS/REPLIT/DEVIN/OpenHands 深读
- T4b（参考根2 12 项目）：README/SKILL.md/代码片段深读完成
- 剩余风险：P1-1 提示词升级效果依赖本地模型能力（qwen2.5-vl 7B），需最小实验验证；P1-5 skills 机制是新模块，需用户批准

---

## 17. Risks and Blockers

| 风险 | 等级 | 触发条件 | 影响 | 缓解 | 需用户确认 |
|------|------|---------|------|------|-----------|
| 本地小模型能力不足 | 中 | P1-1 升级后效果不明显 | Agent 智能度提升有限 | 最小实验（3 问题对比）+ 可选支持 API 大模型 | 否 |
| 用户未提交 diff 冲突 | 低 | Phase B 改动与用户 lint diff 同文件 | 合并冲突 | 实施前确认是否纳入 | 是 |
| skills 机制新模块臃肿 | 中 | P1-5 设计过度 | 核心体积增长 | skills 包独立分发，不进核心包 | 是 |
| main_window 拆分回归 | 中 | P2-7 拆分影响面大 | 现有功能破坏 | 放 v5.x + 独立 Critic 审查 | 否 |
| MCP 图谱服务失败 | 已发生 | code-review-graph/graft/context7 超时 | 无法用图谱 | 已改直接扫描 | 否 |

---

*本文件为 Phase A 单一状态源。Phase B 获批后在此追加 Implementation Batches / Validation Log / Critic Findings。*
