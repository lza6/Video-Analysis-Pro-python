# Workflow Status — 参考的结果计划指南

> 本文件是本任务的单一状态源（Phase A 分析专用）。分析阶段除本文件外不修改任何文件。

## Task Contract
- 原始目标：基于主项目与参考项目根路径，完成项目识别、同类筛选、优点提炼、差距分析、优化方案设计；用户确认后逐步实施改造
- 当前阶段：**Phase A — ANALYSIS_ONLY（分析完成，待用户审批 Phase B）**
- 当前授权：只读分析主项目 + 8 个参考项目 + CL4R1T4S 系统提示词库；允许创建/更新本文件；禁止修改源代码/配置/依赖
- 成功标准：输出完整分析报告（识别结果、现状评估、亮点提炼、差距分析、迁移分类、P0/P1/P2 路线图、实施方案、审批清单）
- 停止条件：报告完成并等待用户确认实施范围，不自动进入 Phase B

## Task Graph
| ID | Agent/Owner | Goal | Dependencies | Deliverable | Validation | Status |
|----|-------------|------|--------------|-------------|------------|--------|
| T0 | Orchestrator | 建立状态文件与工作区检查 | - | 本文件 | 文件存在 | DONE |
| T1 | MainProjectMapper | 主项目结构/技术栈/流程/工程约束深度映射 | - | 结构化摘要 | 证据含 file:line | DONE |
| T2 | DiffAndRiskAuditor | Git 状态与未提交改动风险检查 | - | 风险清单 | git status/diff | DONE |
| T3a | Scout: video-analyzer | 参考项目扫描与亮点提炼 | - | 项目卡片 | file:line 证据 | DONE |
| T3b | Scout: VibeNVR | 同上 | - | 项目卡片 | 同上 | DONE |
| T3c | Scout: VideoPipe | 同上 | - | 项目卡片 | 同上 | DONE |
| T3d | Scout: claude-video-vision | 同上 | - | 项目卡片 | 同上 | DONE |
| T3e | Scout: edit-mind | 同上 | - | 项目卡片 | 同上 | DONE |
| T3f | Scout: Deep-Learning-Experiments | 同上 | - | 项目卡片 | 同上 | DONE |
| T3g | Scout: video-search-and-summarization | 同上 | - | 项目卡片 | 同上 | DONE |
| T3h | Scout: yolo-training-template | 同上 | - | 项目卡片 | 同上 | DONE |
| T4 | Scout: CL4R1T4S | 顶级模型系统提示词模式提炼 | - | 15 条模式清单+16 条反面模式 | file 证据 | DONE |
| T5 | SynthesisArchitect | 评分筛选、差距分析、路线图 | T1-T4 | 分析报告（终端输出+本文件） | 逐项证据 | DONE |
| T6 | User Gate | 用户确认实施范围 | T5 | 批准的批次清单 | 用户回复 | PENDING |

## Project Inventory

### 主项目识别摘要
- **类型**：单体桌面应用（PyQt6 GUI），本地 AI 视频分析工具
- **技术栈**：Python 3.10+ / PyQt6 / OpenCV / ultralytics(YOLO11) / faster-whisper / MoviePy / requests(Ollama+OpenAI 格式 API) / SQLite+ChromaDB
- **规模**：src 约 5517 行（15 个 py 文件），launcher 352 行，无测试、无 CI、无 Lint 配置
- **架构**：README 声称 MVC 变体，实际为"God Object"倾向——main_window.py 2199 行集中了 10 个 Worker 类 + DesktopApp(74 方法)
- **业务链路**：Phase1 抽帧(OpenCV+ThreadPool)+转录(faster-whisper) → Phase2 LLM 总结(逐帧 OCR/YOLO 拼文本提示词) → Phase3 MoviePy 集锦/GIF → Agent 面板(ReAct 循环+工具调用)
- **数据层**：SQLite(sessions/checkpoints 表) + ChromaDB(已初始化未使用) + INI 配置 + TXT 提示词模板(3 份)
- **启动分发**：启动应用.bat → launcher.py(tkinter 环境自检+venv 创建+依赖校验+GUI 启动)

### 参考项目候选清单（8 个，全部有效）
| # | 项目 | 类型 | 技术栈 | 相似度 | 参考价值评级 |
|---|------|------|--------|--------|-------------|
| 1 | video-analyzer | CLI 视频分析工具（与主项目几乎同域） | Python/Ollama/Whisper/OpenCV | ★★★★★ | **高价值** |
| 2 | claude-video-vision | Claude Code 视频感知插件+MCP | TS/MCP/ffmpeg/多后端 | ★★★★ | **高价值** |
| 3 | video-search-and-summarization | NVIDIA VSS 蓝图（视频搜索/摘要/告警代理） | Python/NIM微服务/RAG | ★★★ | 高价值（架构层） |
| 4 | edit-mind | 本地视频知识库（索引+语义搜索+MCP） | TS+Python/ChromaDB/插件架构 | ★★★ | 高价值 |
| 5 | VibeNVR | NVR 监控系统（RTSP/事件/存储/RBAC） | FastAPI+React/PyAV/FFmpeg | ★★ | 高价值（扩展方向） |
| 6 | yolo-training-template | YOLO 训练模板（Kaggle→训练→Streamlit） | Python/ultralytics/GroundingDINO | ★★ | 中价值（自训练扩展） |
| 7 | VideoPipe | C++ 视频结构化 pipeline 框架 | C++/节点图/多线程 | ★ | 中价值（架构思想） |
| 8 | Deep-Learning-Experiments | 深度学习课程讲义+实验集 | Python notebooks | ★ | 低直接价值（学习材料） |

### 排除/降级说明
- 无空壳或无效目录；`Deep-Learning-Experiments` 为教学材料，列为低直接价值（供"教育化叙事"借鉴）
- VideoPipe 为 C++ 生态，不建议代码迁移，仅借鉴 pipeline 节点图思想

### 高价值参考模块精选
1. **video-analyzer**：两阶段提示词架构（逐帧描述→汇总重构）+ 上下文累积帧分析 + 音频质量检查 + PromptLoader 双目录回退 + 轻量 Web UI 子项目分离
2. **claude-video-vision**："感知层而非解释层"哲学 + `video_analyze` 先结构分析再抽帧的工作流 + 按用户意图选择 ffmpeg 滤镜 + 段级变速抽帧 + MCP 工具化
3. **edit-mind**：AnalyzerPlugin 插件体系（objects/faces/text/color/shot_type/descriptor 六插件）+ BaseProcessingService 抽象 + BullMQ 后台任务 + 帧元数据 TypedDict 规范
4. **VibeNVR**：CameraThread 模块化组件（stream_reader/motion/ai/record 分离）+ 双流优化 + CONTEXT.md 工程宪章 + 分层存储清理
5. **NVIDIA VSS**：告警验证工作流（感知→行为分析→VLM 二次确认降低误报）+ 长视频分块摘要聚合
6. **CL4R1T4S**：Manus 事件流+agent_loop+todo.md 清单模式、Devin 规划/标准双模式、Replit 最小往返+反馈工具模式

## Evidence Ledger
| Claim | Evidence | Type | Confidence |
|-------|----------|------|------------|
| 主项目为 PyQt6 桌面 AI 视频分析应用 | README.md:1-56, requirements.txt | DIRECT | 高 |
| 主项目无测试目录/测试文件/CI 配置 | find 结果为空（无 test_*.py/pytest.ini/.github） | DIRECT | 高 |
| git 无未提交源码改动 | git status --short 仅列出 AI 工具配置目录与 __pycache__ | DIRECT | 高 |
| requirements.txt 固定依赖未锁版本（23 个包） | requirements.txt:1-23 | DIRECT | 高 |
| 主项目源码总量约 5517 行（15 个 py 文件） | wc -l 统计 | DIRECT | 高 |
| DesktopApp 存在 2 个重复方法定义 | AST 检测：on_download_finished@1157/1218，plot_metrics@1490/2003 | DIRECT | 高 |
| **P0 缺陷**：main_window.py:2170 `sys.exit(run_main())` 之后存在不可达死代码（init_backend@2171 含 7 工具注册 + seek_video@2188） | 逐行读取 2126-2199，AST 归属验证 | DIRECT | 高 |
| **P0 缺陷**：Agent 实际只有 3 个工具（get_video_meta/get_frame_details/delete_this_history），search_web/search_visual/run_ocr/create_highlights/point_and_jump 全部注册在不可达代码中 | main_window.py:1582-1593 vs 2178-2186 | DIRECT | 高 |
| point_and_jump 工具依赖 app.seek_video，但 seek_video 未定义在 DesktopApp 类上（hasattr 检查使其静默失效） | agent_tools.py:266-267 + AST 验证 | DIRECT | 高 |
| **P0 缺陷**：ExtractionWorker 结果 dict 缺 'duration' 键 → video_duration 永远为 0 → Agent 的 get_video_meta 报告错误时长 | main_window.py:80-120 (result 无 duration) vs 1779 | DIRECT | 高 |
| 智能关键帧提取 extract_smart_keyframes 与语义去重 filter_frames_semantically 是死方法（无调用方）；chk_smart 勾选项读入 config 后未被消费 | grep 调用计数=0；main_window.py:94-95 仅调用 extract_keyframes | DIRECT | 高 |
| _generate_plot_internal 成为死方法（其宿主 plot_metrics@1490 被 2003 版覆盖） | 交叉引用 | DIRECT | 高 |
| plot_metrics 生效版本为 2003 基础版；README 宣称的"高级可视化报表"实为简单 matplotlib 折线 | main_window.py:2003-2038 | DIRECT | 高 |
| export_report_as_pdf 依赖 weasyprint 但 requirements.txt 未声明 → PDF 导出必失败（静默返回 None） | logic.py:1232 vs requirements.txt | DIRECT | 高 |
| transcribe 依赖 pydub 做静音检测/波形，但 pydub 未在 requirements.txt → 降级为警告+全量转录 | logic.py:484-494 | DIRECT | 高 |
| API key 明文存入 config/app_config.ini 与 api_presets.json | main_window.py:906, 999；config_manager.py:31 | DIRECT | 高 |
| 默认 API 端点硬编码为 https://api.iflow.cn/v1（config_manager.py:40, api_intro_page.py:21-56 商业推荐） | DIRECT | 高 |
| visual_search 工具每次调用重新加载 SentenceTransformer('clip-ViT-B-32') 并对所有帧重新编码（无缓存） | agent_tools.py:167-174 | DIRECT | 高 |
| create_highlight_cut_tool 忽略 description 参数，硬编码"前3个有内容的帧各±2秒" | agent_tools.py:211-247 | DIRECT | 高 |
| APIGatewayClient 用字符串分隔符 "--- System Context ---" 解析 system 消息（脆弱协议） | logic.py:722-737 | DIRECT | 高 |
| OllamaClient 硬编码 num_ctx=4096 上下文窗口 | logic.py:623 | DIRECT | 高 |
| ChatWorker ReAct 循环：把累积 prompt 拼接重发（历史膨胀），工具结果截断 100 字符展示，args 解析猜 JSON 或猜 key 名 | main_window.py:160-232 | DIRECT | 高 |
| history_manager 有 checkpoints/chroma 帧向量记忆 API 但全部零调用（save_checkpoint/semantic_search_frames 无引用） | grep=0 | DIRECT | 高 |
| 5 个 UI 模块中 timeline_widget/help_dialog/ui_components 引用薄弱；README 文件结构与实际不符（custom_widgets.py/thinking_widget.py 不存在） | grep+ls | DIRECT | 高 |
| 错误处理：logic.py 40 try/6 bare except/2 silent pass；main_window.py 29 try/6 bare except | grep 统计 | DIRECT | 高 |
| 超长函数：setup_ui 393 行、chat_stream 141 行、transcribe 103 行、apply_theme 92 行、get_advanced_video_metrics 87 行 | AST 行数统计 | DIRECT | 高 |
| 类型注解：logic.py 47 个函数仅 26 个有返回注解（55%） | AST 统计 | DIRECT | 高 |
| 缓存目录名"软产生的缓存"（constants.py:61），目录尚未创建；日志 RotatingFileHandler maxBytes=100MB 过大 | launcher.py | DIRECT | 高 |
| gradio 在 launcher 依赖校验中强制 import 但 src 全程未使用（纯拖累启动） | launcher.py:94 + grep src=0 | DIRECT | 高 |
| pyannote.audio/open-clip-torch/chromadb 声明于 requirements 但 chroma 仅初始化未使用、pyannote/open-clip 零引用 | grep | DIRECT | 高 |

## Decisions and Assumptions
- 已确认事实：见 Evidence Ledger（30+ 条 DIRECT 证据）
- 当前假设：8 个参考项目均为可读代码库（已由 Scout 逐一验证，全部有效）；CL4R1T4S 为系统提示词集合（28 子目录，Manus/Devin/[CC]/Cursor/Replit 等深读完成）
- 架构决策（Phase A 结论性判断）：
  1. **不推翻重写**——主项目三阶段业务链路成立，问题集中在"接线断裂"与工程质量，走渐进修复路线
  2. **迁移思想不迁移代码**——所有参考项目（Python/TS/C++）按设计思想跨栈适配
  3. **Agent 能力升级优先于新功能**——5 个已实现但未接线的 Agent 工具是现成资产，接通即得
- 被否决的方案：
  - 直接大改代码（违反 ANALYSIS_ONLY 授权）
  - 引入 Redis/Postgres/Docker 等重型基础设施（桌面单机形态不匹配，edit-mind/VSS 的教训）
  - 迁移 VideoPipe C++ 代码（生态鸿沟，仅借思想）

## Risks and Blockers
| 风险 | 等级 | 触发条件 | 影响 | 缓解 |
|------|------|----------|------|------|
| 参考项目为空壳/损坏 | 低 | Scout 扫描失败 | 覆盖不全 | 标记并继续 |
| 参考项目含可疑脚本 | 低 | Scout 发现 | 安全风险 | 不执行，只分析 |
| MCP 图谱服务全部连接失败 | 已发生 | code-review-graph/graft/context7 超时 | 改用直接扫描 | 已采用直接扫描策略 |

## Implementation Batches（建议批次，待用户批准）
| 批次 | 内容 | 风险 | 依赖 |
|------|------|------|------|
| B1 依赖止血 | requirements 补齐 pydub/matplotlib/seaborn、pin moviepy<2.0、删 gradio/pyannote/decord、paddlepaddle-gpu 改可选、launcher 验证脚本修正 | L1 | 无 |
| B2 P0/P1 断线重接 | 死代码迁回 init_backend（5 个 Agent 工具激活）、报告重复输出修复、智能抽帧接线、提示词目录修复、duration 传递、重复方法清理、P1-2/3/7 修复 | L2 | B1 |
| B3 Agent 提示词升级 | 按 CL4R1T4S 15 模式重写 Agent 系统提示词 + finalize 完成守卫 + ✅/⚠️/❌ 验证符号 + 分析/编辑双模式 | L2 | B2 |
| B4 分析管线增强 | 分析先行→变速抽帧 + 滚动叙事链 + 长音频静音分块转录 | L2 | B2 |
| B5 知识库接线 | ChromaDB 帧向量记忆接线 + 检索段合并 + RRF 融合 + 语义去重激活 | L2 | B2 |
| B6 远期扩展 | 训练中心 tab（用户自训模型）/ 实时流方向 / 高光规则引擎 | L3 | B3-B5 |

## Validation Matrix
| Requirement | Validation Method | Actual Command/Action | Result | Evidence |
|-------------|-------------------|----------------------|--------|----------|
| 识别主项目结构 | 目录+依赖+入口+逐文件扫描 | ls/read/AST | 完成 | mapper 报告 |
| 递归发现参考项目 | ls 参考根目录 | 完成（8 项目全部有效） | 完成 | scout 报告 ×8 |
| 检查未提交改动 | git status | 完成 | 无源码改动 | Evidence Ledger |
| 缺陷证据可定位 | file:line 交叉验证 | AST/grep/逐行读 | 完成 | P0×1/P1×8/P2×10 全带证据 |
| 提示词模式提炼 | 深读 10+ 份顶级提示词 | 完成 | 15 模式+16 反面模式 | T4 报告 |

## Review Findings
- T1 mapper-main：P0×1（Phase2 报告重复输出全文）、P1×8、P2×10，全部 file:line 证据，已并入批次 B1/B2
- T2 diff 审计：git 干净（仅 AI 工具配置与 __pycache__ 未跟踪），无用户改动风险
- T3a-h：8 份 scout 报告全部验收通过，证据齐全（file:line 级）
- T4：15 条提示词模式 + 16 条反面模式，归入 Phase B 批次 B3
- 剩余风险：P1-8（launcher 验证脚本死循环）为"合理推断"级，需实机复现验证；mapper 报告与 orchestrator 独立验证结果互相印证（重复方法、死代码区、duration 缺失等 8 项双源一致）

## Next Gate
- 下一步动作：向用户交付完整分析报告，等待 Phase B 授权
- 是否需要用户批准：是
- 需要批准的准确范围：建议优先批准 B1+B2（纯缺陷修复，不引入新功能，低风险高收益），B3-B6 逐批审批
