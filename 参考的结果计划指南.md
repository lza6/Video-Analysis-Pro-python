# 参考的结果计划指南（v3 — 以 v10.3.0 真实现状 + 988 项目全量重扫为基准）

> 本文件由总协调 Agent 在 ANALYSIS_ONLY 阶段产出（2026-09-11），是本任务的**单一状态源**。
> 输入：① 主项目 v10.3.0 源码直读 + 实跑（6 个核查代理 + 主控自查）② 参考目录 988 条目全量重扫（33 批并行侦察，31 批产出卡片（2 批缺口））
> ③ 7 个新方向深挖代理（小白教学化 / 用户画像 / skills 沉淀 / 图像视频生成 / 电商 / PPT / 多智能体内核）④ 前任侦察产物 `.agent-scratch/recon/*` 与 `参考项目/vap_scout_memory.md`
> 约束：只读分析；除本文件外不修改主项目或参考项目任何文件；证据分级 DIRECT / INFERENCE / UNVERIFIED；不自动进入实施。
> **v2 已归档于 `docs/archive/参考的结果计划指南.md`。v2 的全部 P0-P3 批次已由 v10.3.0 实现——但本次核查发现「实现」与「送达用户」之间存在系统性断层，这是 v3 的立论基础。**

---

## Task Contract

- **原始目标**：全量重扫 988 个参考项目 → 独立核查主项目 v10.3.0 真实落地情况 → 产出对标差距分析与优先级路线图，服务用户 5 条原始意图 + 本轮新增 6 个方向：
  ① Agent 更完美、每个智能体深度优化 ② 小白易用 + 黑匣子日志全开，让用户边用边学原理 ③ AI 更懂用户 + 沉淀训练用户自己的 skills + 真干活 ④ 领域扩展：图像视频生成 / 淘宝电商 / PPT ⑤ 不被局限在"帮用户做软件项目" ⑥ 深度思考"为何参考目录包含不相关项目"
- **当前阶段**：ANALYSIS_ONLY（只读分析，产出本文件，不改源码）。
- **当前授权**：读/搜/非破坏性运行；创建或更新本文件；不修改源码/配置/依赖/部署文件；不提交不推送。
- **成功标准**：本文件九节齐全 + 证据可追溯（file:line 或命令输出）+ 区分 DIRECT/INFERENCE/UNVERIFIED + 孤证降权 + 以 v10.3.0 实际代码与运行结果为准 + 提供可执行、可回滚、分优先级的路线图。
- **停止条件**：本文件落盘 → 回传摘要 → **停下等待用户确认实施范围**，不自动进入 Phase B。

---

## Task Graph

| ID | Agent/Owner | Goal | Dependencies | Deliverable | Validation | Status |
|----|-------------|------|--------------|-------------|------------|--------|
| S1 | RefScan ×33（Workflow） | 全量扫描 988 条目，产出 912 个唯一项目卡片 | 无 | 每批 projects/notable/userFocusHits/deadDirs | 逐目录 README/清单证据 | **DONE**（31/33 批产出卡片；2 批缺口见下） |
| S2 | 主控自查 | 默认路径端到端追踪 | 无 | 默认后端/审批/工具链实证 | 源码直读 file:line | **DONE** |
| M1 | Agent 内核核查 | supervisor/loop/turn/session/eli5 | 无 | 逐项核查表 + 实跑 | pytest 实跑 62 passed | **DONE** |
| M2 | 工具与安全核查 | registry/scope_guard/审批/沙箱 | 无 | 逐项核查表 + 工具清单 | pytest 实跑 57 passed | **DONE** |
| M3 | 记忆与 skills 核查 | memory/*、skills/*、experience | 无 | 逐项核查表 + 链路图 | pytest 实跑 174 passed | **DONE** |
| M4 | Web/Electron/黑匣子核查 | app.py、routers、desktop/*、webapp | 无 | 能力矩阵 + 小白友好度 | 源码直读 + tsc 通过 | **DONE** |
| M5 | 测试/CI/版本核查 | tests、ci.yml、版本五同步、产物 | 无 | 收尾状态诚实评估 | **全量实跑 1 failed+1324 passed+3 skipped** | **DONE** |
| M6 | 扩展域核查 | media_gen/web_auto/mcp/商务 skill | 无 | 领域能力矩阵 | **pytest 实跑 82 passed + MCP stdio 冒烟 17 工具** | **DONE** |
| N1 | 小白教学化精读 | 可解释 UI/教学化机制 | 无 | 机制拆解 + 可执行五步 | 参考项目 file:line | **DONE** |
| N2 | 用户画像精读 | AI 懂用户 / 个人记忆 | 无 | 机制拆解 + 画像 schema | 参考项目 file:line | **FAILED ×2**（API 内容风控；改由 S1 卡片 + vap_scout_memory.md 覆盖） |
| N3 | skills 沉淀精读 | 蒸馏/验证/棘轮/安全 | 无 | 44 条机制 + 用户旅程 | 参考项目 file:line | **DONE** |
| N4 | 图像视频生成精读 | 本地可跑性 / 付费协议化 | 无 | 能力矩阵 + 10 件事清单 | 参考项目 file:line | **DONE** |
| N5 | 电商精读 | 电商链路 / 合规边界 | 无 | 能力矩阵 + 红线清单 + 中文平台勘误 | 参考项目 file:line | **DONE** |
| N6 | PPT/办公精读 | 真实产物 / 设计规则 | 无 | 方案 + 规则清单 | 参考项目 file:line | **DONE** |
| N7 | 多智能体内核精读 | 编排拓扑/上下文/成本/可见性 | 无 | 30 条机制 + 10 条优化清单 | 参考项目 file:line | **DONE** |
| N8 | Hermes/DSH 血统精读 | 同源产品线能力对标 | 无 | 血统地图 + 差距表 + 应补 10 件事 | 参考项目 file:line | **DONE** |
| X1 | SynthesisArchitect（主控） | 综合分析 + 产出本 v3 指南 | S1/S2/M*/N* | 本文件 | 输入全部直读确认 | **DONE** |
| X2 | 用户 | 审阅指南、批准实施范围 | X1 | 实施授权 | — | PENDING |

**已知覆盖缺口（2 批，共 60 个目录）**：

| 切片 | 目录范围 | 原因 | 补救 |
|------|----------|------|------|
| **#61-#90** | `ClawTeam-OpenClaw` → `Elysia-Api-for-Koishi`（含 DeepTutor / EcomAgent / CommerceAgentBench / Egonex-AI__Understand-Anything / Continuous-Claude-v3-main / CosyVoice / ClawX / DSH-taskboard / CowAgent / ECC 等） | 该批代理**未调用 StructuredOutput**（返回空） | ✅ 已另行补扫（结果并入本指南）；其中 DeepTutor / Understand-Anything / EcomAgent / CommerceAgentBench / CosyVoice 另已由 N1/N4/N5 精读直读 |
| **#421-#450** | `claude-context-master` → `cline`（30 个 `claude-*` 系目录） | 三次触发 API 内容风控（该批含 prompt 泄露类仓库） | ⚠️ 仅用"只看目录结构、不读正文"的轻量方式覆盖，标 **PARTIAL**；其中 `claude-devtools`/`claude-squad-main`/`claude-vibe-squad`/`claude-token-efficient`/`claude-plugins-official-main`/`claude-context`/`claude-hud-main` 已由其他批次或 N3/N7/N8 报告间接覆盖 |

**计数口径说明**：Workflow 返回 31 批结果，含 919 条卡片记录；去重后为 **912 个唯一项目**。本指南统一采用 912。

---

## Project Inventory

### 主项目识别摘要（v10.3.0，DIRECT）

**身份**：TingFeng Hermes = Electron 桌面壳 + Python FastAPI 后端 + Next.js 16 前端 的**通用 AI Agent 桌面平台**（原视频分析能力降级为内置工具集之一）。

**实测规模**（本会话直数）：`src/web/routers/` **16 个 router** · `tests/test_*.py` **91 个测试文件** · `webapp/src/app/**/page.tsx` **16 页** · `config/skills/` **12 个内置 skill** · 安装包 `desktop/dist/TingFeng Hermes Setup 10.3.0.exe` **697MB 真实存在** · `scripts/dev-setup.ps1` 存在。

**v10.3.0 已建成的模块**（源码实际存在，非文档声称）：
- `src/core/agent/supervisor.py`（17KB）：StuckDetector（3 规则）+ ContextCompressor + BudgetGuard
- `src/core/tools/scope_guard.py`（11.6KB）：三级决策表 + install_tool_guard + ApprovalBus
- `src/core/memory/{working,triplestore,connector}.py`：热层 + 三元组 + 分层装配
- `src/skills/{distiller,validator,scoring,spectre,roster}.py`：蒸馏/验证/棘轮/安全扫描/按需分配
- `src/core/tools/media_gen/`、`src/core/tools/web_auto/`、`src/mcp/server.py`
- `desktop/{log-store,crash-diagnostics,cua-service}.js` + `autoUpdater`
- `config/skills/` 新增 4 个：`ecommerce-merchant` / `ecommerce-shopper` / `marketing-publish` / `ppt-deck`

### 参考项目扫描结果（S1，DIRECT）

| 项 | 数值 |
|----|------|
| 参考根目录条目 | **988**（987 目录 + 1 文件 `vap_scout_memory.md`） |
| 成功扫描批次 | 31 / 33（2 批缺口，见 Task Graph） |
| 成卡项目数 | **912** |
| 本轮新增（Sep 9-11） | ~123 个条目 |

**成卡项目的类型分布**（919 条卡片记录；表内合计 = 919，去重后 912 个项目）：

| kind | 数量 | kind | 数量 |
|------|------|------|------|
| agent-framework | 251 | browser-auto | 37 |
| skill-system | 123 | library | 32 |
| other | 102 | model-serving | 29 |
| desktop-app | 82 | image-gen | 14 |
| security-tool | 69 | ecommerce | 8 |
| doc-collection | 61 | ppt-office | 7 |
| memory-system | 55 | | |
| video-tool | 49 | | |

**高价值（relevance=high）项目的方向分布**（406 个，14 类全列）：agent-framework 136 · skill-system 65 · desktop-app 56 · memory-system 43 · video-tool 32 · other 12 · browser-auto 11 · security-tool 11 · model-serving 8 · doc-collection 8 · **ppt-office 7** · library 7 · **ecommerce 5** · **image-gen 5**（合计 406）

### 参考项目的战略意图（回答用户"为何包含不相关项目"）

v2 已归纳三维（底座件对照库 / 视频生态延伸库 / 新领域预研库）。**v3 补充第四维——产品血统**：

**参考目录里存在一条完整的同源产品线，与主项目同名同构**：

| 血统层 | 目录 | 与 VAP 的关系 |
|--------|------|--------------|
| 上游主产品 | `hermes-agent` / `hermes-agent-main`（Nous Research） | 官方自述："The self-improving AI agent… creates skills from experience, improves them during use, searches its own past conversations, and **builds a deepening model of who you are across sessions**"——**逐条对应用户本轮诉求②③** |
| 观测/可视化 | `hermes-hud-main` / `hermes-hudui-main` | "a consciousness monitor for AI agents… watches an agent think — its memory, its mistakes, its growth over time… Part neofetch, **part flight recorder**"——**对应诉求①黑匣子教学化** |
| 记忆/追踪 | `hermes-zvec-memory` / `hermes-trace` / `dsh-mnemon` | VAP 记忆层的直接对照实现 |
| 插件生态 | `awesome-dsh-plugin` / `agentplane-hermes-plugin` / `hermes-quota-plugin` / `hermes-code-bridge` / `hermes-feishu-streaming-card` / `hermes-live-voice` | VAP `plugins/` 与 IM 网关的生态参照 |
| 桌面/自进化 | `dsh-desktop` / `hermes-agent-self-evolution-main` / `oh-my-hermes`（36 skills + 7 agents） | VAP Electron 壳与 skills 工作层参照 |
| 迁移工具 | `openclaw-to-hermes` | 反推 OpenClaw ↔ Hermes 配置模型差异 |

计数：`hermes*` 18 个 · `dsh*` 11 个 · `claw*/openclaw*` 13 个；三条集合**并集为 41 个条目**（`openclaw-to-hermes` 同时含 claw 与 hermes，此前误计为 42；Critic B 磁盘核对后修正）。

**结论**：参考目录不是杂乱的收藏，而是四库合一 —— ①**同源血统库**（Hermes/DSH/OpenClaw，主项目的直接蓝本与竞品跟踪）②**通用 Agent 平台底座件库**（记忆/上下文/编排/治理/桌面壳/审批）③**视频生态延伸库**（剪辑/字幕/生成/NVR）④**新领域预研库**（电商/PPT/办公/图像生成）。评价任何参考项目都应按这四维坐标，而非只看与当前功能的 1:1 吻合度。

### 排除目录及原因（延续 v2，v3 复核后维持）

| 排除项 | 原因 |
|--------|------|
| PostgreSQL + pgvector + Docker 重依赖方案 | 桌面单用户产品不匹配；坚持 SQLite + ChromaDB 本地优先（`vap_scout_memory.md` 第 5 节同结论） |
| Tauri 迁移 | 丢失 Node 托管 Python 的便利；VAP 已选 Electron，迁移成本 > 收益 |
| 验证码绕过 / 反检测浏览器（2Captcha、geetest、hcaptcha、camofox 系） | 法律灰色边界，与 GPL-3.0 开源产品定位不符 |
| 纯攻防黑产（AutoSploit / arsenal / hackingtool / pentagi 进攻面） | 定位不符；只借编排/治理/沙箱/审批的通用抽象，不迁进攻代码 |
| 重型浏览器逆向 / prompt 泄露集合 | 合规风险 + 维护成本 |
| 规模类 skill 合集（ECC 181 / antigravity 1381 / application-skills 3000+） | YAGNI：规模是结果不是起点；应学其**分档治理**而非数量 |
| 市场/分发基建（skills marketplace、跨用户共享存储） | 单机产品阶段不需要，二期再议 |

### 高价值参考项目（按用户 6 新方向 + 3 通用坐标精选）

**① 小白易用 + 黑匣子教学化**（诉求最强）
- `claude-devtools` —— 思考链 / 工具调用明细 / context 占用拆解 / subagent 树**四件套可视化**（README Key Features）
- `mimik` —— **录制用户浏览器操作自动生成带高亮截图的教程 + Guide Me 回放 + PII 智能打码**；本次唯一直接命中该方向的项目
- `Understand-Anything` / `Egonex-AI__Understand-Anything` —— 代码库/知识库 → 交互式知识图谱 + 依赖序 **guided tour**
- `agenttrail` —— `PLAN.md` 的**「声明 vs 观测」双信号实时地图**
- `ai-agents-from-scratch` —— 每个示例配 `CODE.md` + `CONCEPT.md` **代码+概念双讲解**
- `awesome-ux-skills` —— `ai-governors`（撤销/成本/引用）+ `ai-wayfinders`（引导式 onboarding）+ `ai-tuners`（可调行为）
- `ayghri__i-have-adhd` —— ADHD 友好输出规则集（先行动作/编号步骤/时间预估）
- `LifeOS` —— TELOS **访谈式 onboarding**
- `locally-uncensored` —— 小白向导（引擎探测 / 一键安装 / 模型硬件适配标注）
- `boardui` / `cate` / `callstack agent-device` —— chat / thinking indicator / agent log / 可读快照
- `RepoRewind` —— 本地 WebLLM 讲述代码库演化史
- `HERO-Anti-OverDefense` —— **用 4 类失败命名教学 agent 行为**
- `Program-Intelligence-Agent` —— 答案强制附「查询/推理/注意事项」
- `claude-hud-main` —— flight recorder 式 agent 状态仪表盘
- `EpicInfographics`（VAP 已改编）、`File_Converter_Pro`（成就化小白 UI）、`OpenMAIC`（互动课堂）

**② AI 更懂用户（画像 / 记忆）**
- `supermemory` —— 事实抽取 + **稳定事实 / 近期活动双段用户画像（~50ms）** + 矛盾消解 + 自动遗忘；提供单二进制本地离线部署
- `honcho-main` —— peer/session 实体状态 + **`peer.chat` 自然语言追问用户画像**；FastAPI 架构与 VAP 同构
- `agentmemory-main` —— 4 层记忆整合 + 强度衰减 + 级联过时 + 引用溯源 + token 预算注入 + **92% 省 token 实测**
- `VoiceMem` —— **事实脑 + 情绪脑双脑记忆** + 情绪归因 + 0–300ms 投机预取
- `caura` —— 矛盾检测与 **supersession 排序** + 失败自动生成 rule 记忆 + 四级信任与审计
- `remnic` —— `extraction-judge` **耐久性评分卡**
- `nanobot` —— `session → history.jsonl → SOUL/USER/MEMORY 三文件 → Git 版本化 → Dream 定时整合`
- `dsh-mnemon` —— 三层记忆（Runtime / Documents / Memory Spaces）+ provider/source/strategy 三类插件解耦
- `opencode-magic-context` —— **user memory ≥3 次 promotion** + 19 表 SQLite schema + historian/dreamer 双后台
- `ai-memory` —— git markdown wiki 为源 + **handoff「claimed exactly once」** 强类型协议 + 默认零 LLM 调用
- `atlas` —— checkpoint（commit ↔ session/提示/工具调用/推理绑定）+ 跨 Agent 共享本地记忆 + `atlas-redact` 落盘前脱敏
- `letta-code`（memory blocks + MemFS git 化）、`semantix`（三层语义缓存 + 无 LLM 在环淘汰）、`memorizz`、`gbrain`（夜间 dream cycle）、`EverOS-main`（HyperMem 超图）
- **补扫新增（切片 61-90，记忆/画像方向价值密度最高）**：
  - `Compartment` —— **记忆数据模型实证最扎实**：每条 = **单个 claim**（>200 字符拒绝、禁列表/标题）、必带 `source` + 发现日期、**重要性分层**（决策 0.90 / 个人事实偏好 0.80 / … / 闲聊 0.20，乘法作用于匹配分）、**偏好类新记忆替换旧记忆 / 事实类累加**、`expires` 过期机制、全盘加密含向量（含向量混合索引 ~12ms、零网络）。作者实证金句："agent 写出的记忆中位数是 **1938 字符 bullet 日志**"——故用硬限制强制原子化。**可直接整体移植到 VAP 画像/记忆模块**
  - `CodeStable` —— `.codestable/` 项目记忆：`attention.md` **至多 25 条常驻事实** + `lessons/` 每条经验一个文件、按 **observed → validated → retired** 演化、**写入前查重合并**；三原则"thin harness/thick context、证据先于结论、一个事实一个 canonical owner"
  - `ENZO` —— **Custom Agent Builder**（一句自然语言 → 两轮起草：先分析领域，再由用户自己的模型写"30 年老兵操作手册"：隐性知识/决策启发式/边界案例）+ **自我改进神经层**（每 Agent 神经权重向量每 90s 吸收领域匹配流量，按节奏把 lesson 深调进记忆）+ 7 段 CI 含黑盒安全 pentest 44 断言
  - `CowAgent` —— **与 VAP 用户方向重合度最高**（Python 同栈）：三层记忆（context → daily → core）+ **Deep Dream 自动蒸馏** + 关键词/向量混合检索 + 知识自动整理为 Markdown wiki 并构建可视化知识图谱；自然语言造技能 + 技能市场；Web 控制台零门槛；渠道覆盖 Web/微信/飞书/钉钉/企微/QQ/公众号/TG/Slack
  - `Continuous-Claude-v3-main` —— **"Compound, don't compact"**：YAML handoff 替代 compaction 传递上下文 + memory daemon 自动抽取 learning 供后续会话 recall + 消息进来时 hook 注入"技能激活检查"
  - `Codex-CLI-Compact-main` —— 语义图 + 相关文件预装进 prompt + 会话内**复利记忆**；基准：每 prompt $0.46→$0.27、轮次 16.8→10.3、质量 82.7→87.1
- 详见 `参考项目/vap_scout_memory.md`（前任 245 行侦察报告，18 个项目深读评分）

**③ 用户自己的 skills 沉淀**
- `SkillOpt` + `skillopt_sleep` —— **把 skill 当可训练参数**（学习率/编辑预算/保护区块/held-out 严格大于门）+ **夜间睡眠闭环**（harvest→mine→replay→consolidate→stage→**adopt**）
- `SkillClaw` —— **任务时环 + 任务后进化环**双环；`session_judge`（4 维加权）+ `skill_verifier`（保守否决发布门）；明确支持 Hermes 客户端
- `EvoSkill-main` —— 失败轨迹 → Proposer 提议 create/**edit** → 评测 → frontier 保留最优；git 分支即版本
- `darwin-skill-master` —— **8 维评分卡 + git 棘轮（严格大于才 keep）+ 独立评分子 agent + 人在回路**
- `nuwa-skill` —— 五层蒸馏 + 三重验证 + **保真度评分卡（答题与评分 agent 必须独立）**
- `gepa` / `hermes-agent-self-evolution-main` —— 读执行轨迹反思 + Pareto 选择
- `SkillSpector` —— **71 条漏洞模式 / 17 类**（含记忆投毒、MCP 工具投毒）+ 风险分防刷 + baseline 误报抑制 + SARIF
- `SkillX-main`（三级技能库）、`cangjie-skill`（RIA-TV++ 7 阶段）、`agent-skills-hub`（6 维质量评分）、`wild_agentos`（skill 包 CI gate + 版本化回滚）
- `agent-skills`（addyosmani）—— **三级评测：结构 CI / 触发路由 TF-IDF rank-1≥95% / 行为 eval**

**④ 图像 / 视频生成与编辑**
- `locally-uncensored` —— **本地 ComfyUI 图像视频生成** + 小白向导 + 签名自动更新（一并命中 ①⑧）
- `Pixelle-Video` / `ATH-MaaS__Pixelle-Video` —— 主题→文案→AI 配图/视频→TTS→BGM→成片全自动管线
- `AI-Youtube-Shorts-Generator` —— 高光评分 + **本地全离线模式**（yt-dlp + faster-whisper + ffmpeg/opencv）
- `MoneyPrinterTurbo`（主题→成片 + CPU/GPU 双镜像）、`OpenMontage`（13 条 YAML pipeline + checkpoint + 质量校验器）
- `Jellyfish` / `Forget-C__Jellyfish`（剧本→分镜→一致性→图/视频生成）、`HKUDS__ViMax`（多 agent 视频生成）、`chatfire-AI__huobao-drama`
- `LuxTTS`（1GB 显存本地音色克隆）/ `VoxCPM`（本地可跑 TTS）、`auto-subs`（Whisper/Moonshine/Parakeet 本地字幕）
- `Open-Generative-AI`（400+ 模型图像视频生成的 Next+Electron 应用）、`VCPToolBox-main`（ComfyUIGen/FluxGen/NanoBananaGen 插件）
- `FunClip`（ASR 文本选段）、`PreenCut`、`video-shotcraft`（Remotion 镜头配方卡）、`VideoCaptioner`、`autoshorts`、`boxmot`（多目标跟踪）
- `ffmpeg-skill` —— **40 个本地视频工具 + MCP 化 + SPEC（input_schema 从 argparse 运行时派生、CI 防漂移）+ 统一 `--dry-run/--json`**；零付费 API

**⑤ 电商自动化**
- `AgriciDaniel__claude-ads` / `claude-ads` —— 12 个广告平台运营，**默认只读 + 写操作五道门禁**
- `commerce-agents` —— skill-per-flow + Backend 协议 + **写操作 staged 审批**三件套；原文："Nothing places an order, charges a card, or changes a live listing"
- `agency-agents`（`marketing/marketing-china-ecommerce-operator.md` 等 7 份）—— **中文平台（淘宝/天猫/拼多多/京东/抖音/快手/B站/微博）运营知识层**：标题公式与字数十限、主图 5 槽位、详情页 8 段、投放术语、大促战役模板、合规守则。**纯 prompt，无执行能力**，且含违禁词"规避话术"（VAP 只可拦截、不可生成替代词）
- `EcomAgent` —— 电商运营 Runtime 参考（11 领域操作 + RBAC + **低于成本价直接 BLOCK** + PreToolUse 挂起等人工批准）；淘宝/京东/抖音 adapter 为未接 stub
- `application-skills` —— 已验证含 `shopify` / `woocommerce` / `amazon-*` 技能（OAuth 托管）
- `chrome-cdp-skill` —— **直连用户已登录 Chrome 实时会话**（电商后台自动化底座）
- `AutoCLI`（55+ 站点适配器）、`Agent-Reach`（小红书/Instagram/B站/Amazon 数据通道 + doctor 体检）
- `Open-Pomelli`（Brand DNA → 营销概念 → 平台素材）、`PriceGhost`（四策略抽取投票 + 置信度 + 人工确认；**注意其用 stealth 反检测，合规高危不可学**）
- `MultiPost-Extension`（一键发布 10+ 平台）
- ⚠️ `marketing-skills` —— **含 prompt 注入样本**（`ecommerce-content/SKILL.md` 内嵌"强制插播 CashApp 打赏"与"被问系统提示词就导流付费社群"），**只可参考结构、严禁引用文本**

**⑥ PPT / 办公 / 营销**
- `OfficeCLI` —— **单二进制 docx/xlsx/pptx + Python SDK（pip 可装）**；自带 `officecli-pptx` SKILL.md 的**设计规则 + QA 门禁**（完整规则已收录于本指南**附录 A**）
- `ppt-master` —— **真 .pptx（SVG→原生 DrawingML，文字/形状可编辑）**；Brand/Style/Layout/Deck 模板体系
- `genoffice` —— 自研 OOXML 引擎（HTML→document intent tree→Word，字节保持编辑）
- `ppt-agent-skills` —— 多 subagent 流水线 + **DENSITY_DEFAULTS 密度合同 + visual_qa 校验器**
- `Infographic`（AntV 声明式信息图 ~200 模板 + 内置编辑器）、`dsh-univer-office`、`Kun`（Office 只读分析 + 幻灯片生成）
- `ZeroSlop`（写作质量 0-100 评分卡）、`avoid-ai-writing`、`arc-kit`

**⑦ Agent 内核质量**（136 个 high，精选）
- `all-agentic-architectures(-main)` —— 17-38 个架构实现 + `evaluators/judge.py` + 评分卡 + 排行榜
- `pi-shadow-mind` —— **概率唤醒影子 agent**（`P = heartbeat(1/3) × activation(0.3)`、`max_parallel_shadows=2`）+ 终止型 `report_to_main` + epoch 失效
- `council-of-high-intelligence` —— 匿名交叉质询 + **反从众指令** + 强制异议配额 + 置信加权计票
- `LongHorizon-Harness` —— **「只有通过独立验证的结果才成为任务状态」**；Manager/Executor/Auditor 三角色 + 全新上下文执行单步
- `token-optimizer` / `lean-ctx` / `context-mode` / `headroom` —— 上下文压缩四件套（沙箱隔离原始输出 / 可恢复压缩 / 差量重读 / KV-cache 保护）
- `autonomous-os` —— **压缩失效的五种模式**（优先级倒置 / 泛化 / 冻结过期值 / 世代漂移 / 16k 硬帽）
- `mission-control` / `ccteam` / `OpenSwarm-main` / `agent-orchestrator-main` —— 任务状态机 + 质量门 + 三级模型路由 + **≤25 行完成通知格式**
- `three-man-team-main` —— **3-5 人团队甜点** + 文件交接替代对话历史 + 「没有 Should Fix」
- `spec-ptc` —— **LLM 流式生成期间投机执行工具调用**（影子 REPL + claim-or-run）

**⑧ 桌面壳健壮性与可观测性**
- `atlas`（Tauri + checkpoint 黑匣子）、`atmosphere`（**session tape 可无模型重放** + 治理在关键路径）
- `agent-inspect`（execution tree / Evidence v2 / CI 轨迹断言 / guardrails / redact）
- `pi-agent-desktop`（crash-recovery / restart-policy / port-selection / process-tree **各有独立文件+测试**）
- `AQBot`（逐次工具审批 UI + 单任务 token/成本账本）、`Kun`（GUI+TUI 同运行时）
- `Minke` —— Electron + pnpm 拆包 **`im-weixin`/`im-telegram`/`im-discord`/`im-gateway`/`remote-access`/`model-runtime`**，与 VAP 模块一一对应
- `VetarAI`（Electron + Python FastAPI **sidecar**，与 VAP 架构同构度最高）
- `UI-TARS-desktop`、`Windows-MCP`（Windows 桌面操作 MCP）

**⑨ 工程化**
- `AI-company` —— 2576 测试 + **21 项机器校验不变量** + 3378 间隔校准的停顿检测
- `bernstein`（确定性调度 + HMAC 链式审计 + artifact contract）、`casbin-gateway`（relay 层权限强制 + 全量请求审计）
- `OpenSandbox`（lifecycle/execd/egress 沙箱协议）、`agentseal`（225+86 探针、skill/MCP 扫描）

---

## Evidence Ledger

> DIRECT = 本会话直读源码/配置或实跑命令；INFERENCE = 多证据推断；UNVERIFIED = 缺环境未验证。

| Claim | Evidence | Type | Confidence |
|-------|----------|------|------------|
| **默认后端 = legacy** | `src/web/routers/agent.py:190` `os.environ.get("VAP_AGENT_BACKEND", "legacy")`；`.env.example:110` `VAP_AGENT_BACKEND=legacy` | DIRECT | 高 |
| **v10.3.0 全部 Agent 架构只在 react 分支装配** | `_build_react_agent` agent.py:751-879 是 supervisor/memory/scope_guard/roster 的唯一装配点；`_get_backend` 默认 legacy | DIRECT | 高 |
| **默认路径工具执行无任何治理** | `src/core/agent_tools.py:37-46` `execute_tool_call` 直接 `self._tools[name].execute(**args)`——无 waterfall/审批/沙箱/审计/锁 | DIRECT | 高 |
| supervisor 三件套**真实现且已接线**（但默认关） | `supervisor.py` 17KB 类定义；`loop.py:36` import、`:203-225` 压缩、`:275-285` 预算、`:348-353` 触发；`agent.py:871` `Supervisor.from_env()`；`from_env` 默认 `false`（supervisor.py:433） | DIRECT | 高 |
| `TurnStopReason` 已加 STUCK/BUDGET | `src/core/agent/turn.py:51-59` | DIRECT | 高 |
| `set_approval_fn` / `set_sandbox` 已接线（仅经 install_tool_guard） | 全仓非文档调用仅 `scope_guard.py:286/288`；装配点 `agent.py:798` `_install_tool_guard(...)` | DIRECT | 高 |
| **`set_error_policy` / `set_lock_resolver` 仍零调用方** | 全仓仅定义 `registry.py:101/147`，无任何生产调用 → 重试熔断、工具级读写锁在真实链路永不生效 | DIRECT | 高 |
| **默认路径无审批**：普通用户看不到审批弹窗 | 默认 legacy 走 `agent_orchestrator.py` + `agent_tools.py`，完全不过 scope_guard | DIRECT | 高 |
| **react 模式下审批事件也到不了前端** | `/chat` 在 react 分支返回 `auto_run=false`（agent.py:355-360）；前端仅 `r.auto_run && r.plan_steps.length>0` 才打开 `/run_stream`（`webapp/.../agent/page.tsx:186`）→ SSE 无消费方 → 写工具静默挂 60s 后失败 | DIRECT | 高 |
| **工具分类错误（安全）** | `make_subtitle` / `make_short_video` / `make_voiceover` 真实落盘写文件却 classify 为 read→allow；`cdp_evaluate` 可执行任意 JS 也归 read；`DEFAULT_ALLOW_PREFIXES`（scope_guard.py:143-150）是死常量未被 classify 使用 | DIRECT | 高 |
| **沙箱 Windows 实现有宿主风险** | `sandbox.py:99-101` 把当前后端进程 assign 进 Job 且 `KILL_ON_JOB_CLOSE`；`:115-121` exit 关闭最后一个 handle；CPU 硬顶 50% 作用于整个后端 | DIRECT | 中高（静态判定，未实跑触发） |
| 工具总数 32（legacy 16 + media_gen 4 + web_auto 12）；web react 路径仅实装 16 | M2 实测枚举；`agent.py:790` register_legacy_tools；`mcp/server.py:135-137` 全量 | DIRECT | 高 |
| `media_gen` / `web_auto` 只在 MCP 注册，web 主界面不可用 | 同上 | DIRECT | 高 |
| **`src/core/eli5.py` 零生产调用方** | `grep -rn "eli5" src/ webapp/` 仅命中定义文件自身（**测试有 3 处 import**：`tests/test_eli5.py` / `test_decision_log.py` / `test_b_fin_coverage.py:755-804`，反向印证"有测试无消费者"）；模块内含 10+ 工具的"大白话"模板（`explain_tool_call`） | DIRECT | 高 |
| **前端零 onboarding/tutorial/guide** | `grep -ir "onboarding\|tutorial\|guide\|教程\|新手\|入门" webapp/src/` → 无命中 | DIRECT | 高 |
| **首页是"AI 视频分析"参数表单，不是 Agent 对话** | `webapp/src/app/(app)/page.tsx` 首屏 60 行：VideoPicker / ConfigPanel(抽帧密度/YOLO/OCR) / "开始分析" | DIRECT | 高 |
| **VAP 无法产出真实 .pptx** | `config/skills/ppt-deck/SKILL.md` 产物为 `deck.md` + 可选 HTML；全仓 grep `pptx`/`python-pptx` = 0 命中；仅 `roster.py:49` 有触发词 | DIRECT | 高 |
| CHANGELOG 称 "dashi-ppt skill" 不成立 | 实际是 `ppt-deck`（无 dashi 代码，仅理念参考） | DIRECT | 高 |
| **VAP 无图像生成能力** | `grep "text_to_image\|image_gen\|txt2img\|diffusion"` 仅命中 `skill_generator.py:38` 的算法描述字符串 | DIRECT | 高 |
| 用户偏好记忆雏形存在但窄 | `history_manager.py:174-205` `remember_preference` / `recall_preferences`（ChromaDB `user_preferences` collection，kind=query/interest/feedback） | DIRECT | 高 |
| 无用户画像/人格建模 | 全仓 grep `user_profile` / `user_model` / `persona` 命中仅为 subagent RoleTemplate 的 persona 字段（角色提示词，非用户画像） | DIRECT | 高 |
| 实测规模：16 router / 91 测试文件 / 16 页 / 12 skills | `ls | wc -l` 直数 | DIRECT | 高 |
| 安装包真实存在 | `desktop/dist/TingFeng Hermes Setup 10.3.0.exe` 697,488,682 字节，2026-09-09 09:12 | DIRECT | 高 |
| scope_guard 相关测试实跑全绿 | `pytest tests/test_scope_guard.py tests/test_agent_router_assembly.py tests/test_web_auto.py -q` → **57 passed, 1 skipped in 39.93s**（用 `./venv/Scripts/python.exe`，系统无 `python` 命令） | DIRECT | 高 |
| 测试覆盖面未触及默认路径绕过 | `test_agent_router_assembly.py:116-200` 全用自建 registry/mock bus，不验证 agent.py 真实 `/run_stream` 闭环 | DIRECT | 高 |
| 参考目录 988 条目 / 912 唯一成卡 / 31 批产出（2 批缺口已补扫或标 PARTIAL） | Workflow 运行统计 + 补扫 | DIRECT | 高 |
| Hermes/DSH/OpenClaw 血统 42 个条目 | `hermes*` 18 + `dsh*` 11 + `claw*` 13 | DIRECT | 高 |
| 参考项目「用户画像」主流做法 | supermemory 双段画像 / honcho peer.chat / opencode-magic-context ≥3 次 promotion（S1 卡片 + N2 部分） | DIRECT | 中高 |
| 「付费红线」在全参考库中普遍被规避/协议化 | 多个项目 README 明示 Mock/本地优先 | INFERENCE | 中高 |
| **压缩链路丢弃裁剪结果 → 摘要反增 token** | `loop.py:219-228` 调 `compressor.compress(session)` 后**未使用返回的 `cond.messages`**；`loop.py:235` `messages = session.derive_messages()` 仍从**全量**事件派生；摘要作为额外 system 事件 append（`supervisor.py:219-227`）→ 只增不减；且 marker 幂等使摘要永不刷新 | DIRECT | 高 |
| **`step_timeout_sec` 是死配置** | 全仓仅 `loop.py:59,68`（docstring + 字段定义），无 `asyncio.wait_for` 包裹；`loop.py:269` 的 `except asyncio.TimeoutError` 分支不可达（`SyncLLMClientAdapter` 把所有异常吞成正常 chunk，`agent.py:264-274`） | DIRECT | 高 |
| **Supervisor 每 HTTP 请求重建 → 跨轮状态丢失** | `agent.py:348,459` 每次请求都 `_build_react_agent`；`BudgetGuard.total_spent` 与 StuckDetector 滚动窗口不跨请求保留；阈值不可 env 配置（`supervisor.py:436` 仅 `cls()` 默认值） | DIRECT | 高 |
| **TripleStore 只写不读** | 生产库 `config/agent_triples.db` 有 9 行；`query_subject/query_between/query_object` 在 `src/` **零调用**（仅测试）→ 图记忆是只写不读的死端 | DIRECT | 高 |
| **FTS5 混合检索无生产读者** | `experience.py:167-173` FTS5 表/触发器/评分公式（0.3/0.4/0.3，`experience.py:307-308`）全部真实且生产库已建；但 `find_similar` 唯一调用方 `SkillAdvisor.suggest`（`experience.py:369`）在 `src/` **零调用** → 运行时从未用过该检索 | DIRECT | 高 |
| **`validator.py` 三重验证完全未接线** | `src/` 下零调用，仅 `tests/test_skills_validator.py`；generate/distill/ratchet 三个入口都不调用它 | DIRECT | 高 |
| **skills 蒸馏断头（无落盘通道）** | `POST /api/skills/distill`（`skills.py:187-194`）**只返回 markdown**，无 save/apply 端点、无后续步骤；前端 build chunk 无 distill/ratchet 调用 → 用户必须手工复制 markdown 建目录才能生效 | DIRECT | 高 |
| **spectre 硬阻断与 UI 文案矛盾** | `loader.py:148` `enabled=enabled and not security_warning`（硬 AND），但日志/UI 称"可手动开启"——被扫出问题的 skill 永远无法启用 | DIRECT | 高 |
| Working Memory 真实写入但 TTL 无物理清理 | 生产库 `config/agent_working_memory.db` **155 行**（mtime 2026-09-11）；`forget_old()`（`working.py:87-109`）**无生产调用**，仅测试 → 过期行永不删除 | DIRECT | 高 |
| `skill_generator` 仅 4 个硬编码场景且 parking 与内置撞名 | `skill_generator.py:63-79` 仅 parking/face/fire/vehicle；parking 模板名与内置 `surveillance-parking-lpr` 同名 → `save_skill` 抛 `FileExistsError`，UI 该条路径必然失败 | DIRECT | 高 |
| `SkillAdvisor` / `roster.with_experience` 零生产调用 | `experience.py:356-387`、`roster.py:157-180` 仅测试引用；`agent.py:827` 调用 roster 时不传 experiences → 宣称的"Experience 历史命中"第 3 优先级不生效 | DIRECT | 高 |
| **全量测试在 HEAD 实跑 = 1 failed, 1324 passed, 3 skipped** | `pytest tests/ -q --ignore=tests/test_headless_server.py`（719.82s）；失败项 `test_gc_guard.py::test_concurrent_check_does_not_crash` 孤立复跑通过 → **flaky 并发竞态**，但"全量绿"在 HEAD 不成立 | DIRECT | 高 |
| **GitHub Release 安装包不可下载** | Release v10.3.0（id 385683679, published 2026-09-09T16:33:47Z）唯一 asset `state="starter"`（上传从未完成），`release.assets` 长度 0；v10.2.0 同 | DIRECT | 高 |
| **安装包内 config/skills 全部缺失** | `desktop/electron-builder.yml:33` config filter 仅 `**/*.txt\|ini\|json`，排除 `SKILL.md` → `win-unpacked/resources/config/` 无 skills 目录（仓库有 12 个） | DIRECT | 高 |
| **版本第 5 处不同步** | `src/web/app.py:142` FastAPI `version="10.2.0"`（其余 4 处 = 10.3.0）；另 `start-desktop.bat:24` 横幅与 `CLAUDE.md:9` 仍写 v9.0.0 | DIRECT | 高 |
| "独立 Critic PASS" 无产物 | 全仓搜索无 Critic/审查/验收报告；`workflow_status.md` 的 Review Findings 栏为空 | DIRECT | 高 |
| 覆盖率 80.25% 早于最终提交 | `.coverage` mtime 2026-09-09 22:45:10，功能提交 c508cb7 = 23:13:14 → HEAD 覆盖率 UNVERIFIED；无 htmlcov/coverage.xml 落盘 | DIRECT | 高 |
| Playwright "23 passed" 为历史记录 | 唯一痕迹 `webapp/test-results/.last-run.json`（无计数）时间 09-09 05:35，早于最终提交；`playwright-report/` 不存在 | DIRECT | 高 |
| Electron 仅 dev 模式启动过，交付形态未验证 | `logs/main.log` 2026-09-09T17:08:32Z 一次 dev 启动（autoUpdater disabled → runtime onReady port=8001 → tray → window created）；**非安装包启动**，无 UI 验收证据 | DIRECT | 高 |
| **logs 页实时按钮死锁** | `webapp/.../logs/page.tsx:176` `onClick={toggleLive}` 与 `disabled={live}` 同时存在 → 开启后永远无法点"停止" | DIRECT | 高 |
| logs 复制时间戳秒/毫秒混用 | `page.tsx:116` `new Date(l.ts*1000)` 对后端秒级正确，但桌面条目 ts 是毫秒（`log-store.js:91`）→ 复制时间错误 | DIRECT | 高 |
| 前后端日志无去重、trace_id 前端丢弃 | `logs/page.tsx:11-16` 类型无 trace_id（后端 `logs.py:55-62` 有）；注释称"渲染层去重"但无代码 | DIRECT | 高 |
| **CUA 服务未挂载却是死代码进包** | `cua-service.js:72` `mountCuaService` 全项目零调用（`main.js` 无 require）；`electron-builder.yml:13` 仍打包该文件 | DIRECT | 高 |
| 后端 16 router / **86 端点** | `grep -cE '^@router\.(get|post|...)'` 实测：agent 9 / analyze 7 / batch 9 / config 8 / providers 9 / im_gateway 7 / models 5 / skills 5 / surveillance 5 / decisions 4 / metrics 4 / remote 4 / logs 3 / media 3 / requests 3 / health 1 | DIRECT | 高 |
| `/` 与 `/analyze` 是重复分析页 | 两个 page.tsx 内容近似；导航指 `/`，dashboard 快捷入口指 `/analyze/`，e2e 测 `/analyze/` | DIRECT | 高 |
| 断路器真实现且有运行痕迹 | `runtime-controller.js:125-146` 记录/冻结/re-arm；`logs/runtime.log` 有 `[breaker] 冻结重启…auto re-arm`；main.js:267-293 托盘联动 | DIRECT | 高 |
| 诊断包 zip 真实现（6 条目） | `crash-diagnostics.js:282-310`：logs/runtime.log、main.log、desktop.log、system.json、config-snapshot.json、process-memory.json；手写 ZIP 含 CRC32/EOCD；单文件 5MB 上限 | DIRECT | 高 |
| log-store 脱敏 4 条正则（有盲区） | `log-store.js:18-27`；**无 `nvapi-` 形态规则**（项目实际用 NVIDIA key），裸 key 会漏网 | DIRECT | 高 |
| autoUpdater 空转 | 依赖在（electron-updater ^6.8.9）、代码在（main.js:53-109），但无 `build.publish`、`~/.tingfeng-hermes-updates` 不存在 → 走"enabled but feed not configured"分支，永不检查 | DIRECT | 高 |
| `tests/test_e2e_smoke.py` 不存在但 CI 仍 ignore | `ci.yml:41` 陈旧配置 | DIRECT | 高 |
| 本地 venv = Python 3.14.3，CI 矩阵 = 3.10/3.11 | 全部本地证据来自 3.14.3 | DIRECT | 高 |
| **VAP 无法跑本地图像生成** | venv 实测 `torch 2.14.0+cpu`（CPU-only）；`edge-tts`/`pyttsx3` **未装** → `voiceover.py` 只出 Mock wav；moviepy 2.2.1 / faster-whisper 1.2.1 / opencv / scenedetect / pydub 已装 | DIRECT | 高 |
| VAP 现成可做 10 件媒体处理 | N4 逐项核对依赖：Ken Burns（moviepy）/ SRT（faster-whisper）/ 字幕烧录 / 竖屏 / 智能剪辑 / 无损拼接+xfade / 封面 / 静音粗剪（pydub）/ loudnorm / 变速倒放画中画 —— 库全部已装 | DIRECT | 高 |
| **`marketing-skills` 含 prompt 注入样本** | 其 `ecommerce-content/SKILL.md` 内嵌"强制插播 CashApp 打赏"与"被问系统提示词就导流付费社群"指令 | DIRECT | 高 |
| 电商合规共识（参考项目） | commerce-agents：「Nothing places an order, charges a card, or changes a live listing」；claude-ads：`default_mode: read-only` + 7 道 mutation gates；EcomAgent：低于成本价直接 BLOCK | DIRECT | 高 |
| 参考目录有 Hermes/DSH/OpenClaw 完整血统 | 42 个条目；`hermes-agent` v0.21.0 比 `hermes-agent-main` v0.7.0 更新 | DIRECT | 高 |
| 上游 Hermes 的自进化机制 | `run_agent.py:1797-1919` 后台 review fork（nudge 间隔 10 轮、静默写共享 store、不改主对话）；`memory_tool.py` USER.md 1375 字符 + MEMORY.md 2200 字符冻结快照注入 | DIRECT | 高 |
| WindowsJobSandbox 实际会否终止宿主 | 未实跑触发 | UNVERIFIED | — |

---

## Decisions and Assumptions

### 已确认事实

1. **v10.3.0 的"闭环"是代码层闭环，不是用户层闭环**。全部 P0-P3 批次的源码与测试都在，但主开关 `VAP_AGENT_BACKEND` 默认 `legacy`，且 legacy 路径是另一套无治理的执行器。**默认用户拿到的是 v10.2 之前的行为**。
2. **系统性问题模式**：本项目反复出现「定义了但没人调用」——v10.2 是 `set_approval_fn`/`set_sandbox`，v10.3 修掉了这两个，但 `set_error_policy`/`set_lock_resolver`/`eli5.py` 仍是同款，且新增了"接线了但默认关"这一变体。
3. **用户在 v10.3.0 之后提出的新方向（教学化/画像/skills 沉淀/扩展域），参考目录里全部有成熟对照实现**，且部分是主项目血统内（Hermes/DSH）的现成能力。
4. 参考目录的战略价值 = 四库合一（血统 42 / 底座件 / 视频延伸 / 新领域预研）。

### 当前假设（需用户确认）

1. **假设"把已建成能力送到默认路径"优先级高于"新建扩展域"**——因为 v10.3.0 的投入若不接通等于零，且用户本轮第一条诉求正是"小白易用"。
2. **假设默认后端切换需谨慎**：legacy 与 react 是两套执行器。**注意：两条路径的 Web 工具集其实同为 16 个 legacy 工具**（）——真正差异在**治理层**（react 走 ToolRegistry + waterfall + scope_guard；legacy 裸执行）与**审批/SSE 契约**。另 media_gen(4) + web_auto(12) 共 16 个工具**只注册进 MCP**，两条 Web 路径都拿不到（见 A4/A9）。属 L3 变更，应分步并保留回退开关。
3. **假设付费 API 红线延续**：图像/视频生成优先本地可跑层，付费能力走 Mock 协议层，用户授权预算后才真连。
4. **假设桌面单用户产品继续不引 PostgreSQL/Docker**（与前任侦察报告结论一致）。
5. **假设用户认可"少而精"skills 路线**（12 个内置 → 用户自沉淀走 on-demand，常驻集设硬上限）。

### 架构决策

1. **P0 主题 = "Deliver, not Build"**：不新增能力模块，把已建成的监督层/审批/记忆/skills/媒体工具接到默认用户路径，并让"当前处于哪条路径、开了哪些能力"对用户可见。
2. **P1 主题 = "让小白看懂 + 让 AI 懂用户"**：eli5 接线 + 教学化黑匣子（懒人/专家双模）+ onboarding + 用户画像层。
3. **P2 主题 = "扩展域按'本地优先、免费优先、审批前置'落地"**：PPT（python-pptx 直出）、图像视频（本地 ComfyUI/免费层）、电商（只读优先 + 写操作审批）。
4. **所有新能力默认可见**（与 v10.3.0 的"默认关零回归"相反）——对小白而言"默认关"等于"不存在"。复杂的/有风险的默认值改由 UI 开关呈现，而非 env 开关藏起来。

### 被否决 / 降级方案

| 方案 | 处理 | 原因 |
|------|------|------|
| 照搬 PostgreSQL/pgvector/Docker | 否决 | 桌面单用户 |
| Tauri 迁移 | 否决 | 生态损失 |
| 验证码绕过 / 反检测浏览器 | 否决 | 法律灰色 + 开源定位 |
| 规模类 skill 合集照搬 | 否决 | YAGNI |
| dashi-ppt（Node+Chromium+AGPL）/ genoffice（TS）嵌入 Python 后端 | 否决（仅借规则） | 技术栈不匹配 + 许可 |
| dashi 之外：`ppt-agent-skills` 截图式 pptx | 降级为兜底 | 文字不可编辑 |
| N2 用户画像深读代理 | 放弃重试（×2 风控失败） | 已有 S1 卡片 + 前任报告覆盖 |
| 切片 421-450 第三次重扫 | 放弃 | 内容风控；已用轻量结构扫描覆盖 |

---

## Risks and Blockers

| 风险 | 等级 | 触发条件 | 影响 | 缓解 | 需用户确认 |
|------|------|----------|------|------|------------|
| 默认后端切 react 引入回归 | **L3** | 工具集差异 / SSE 契约 / 审批链路变化 | Agent 全链路 | 分步切 + 保留 `VAP_AGENT_BACKEND=legacy` 回退 + 灰度 header | **是** |
| WindowsJobSandbox 终止宿主后端进程 | **L3** | 启用沙箱后 exit 路径 | 后端进程被杀 | 改用独立子进程或先修 Job 作用域；默认保持关闭前先验证 | **是** |
| 工具分类错误导致静默写盘/任意 JS | **L2** | `make_*` / `cdp_evaluate` 被调用 | 数据/安全 | 修 classify 表 + 补测试 + 默认 ask | 否（安全修复） |
| 审批 SSE 断点未修 | **L2** | react 模式写工具调用 | 60s 静默失败 | 修 `/chat` 的 `auto_run` 契约或前端改判据 | 否 |
| 教学化改造增大 context/token | **L2** | eli5 + 记忆注入同时上 | 成本 | 解释文本不进 LLM prompt（仅 UI 层）；注入设预算 | 否 |
| 用户画像涉及隐私 | **L3** | 长期记录用户行为 | 隐私 | 本地存储 + 用户可查看/编辑/删除 + 默认不联网 | **是**（数据策略） |
| PPT/图像生成引入新依赖 | **L2** | python-pptx / ComfyUI | 安装包体积（已 697MB） | python-pptx 极轻；图像生成走可选依赖 + 缺失降级 | 否 |
| 电商自动化合规 | **L3** | 真实下单/改价/爬取 | 法律 + 账号风险 | 只读优先；写操作强制审批；不碰平台 ToS 禁止项 | **是** |

---

## Implementation Batches

> 全部 **PENDING**（ANALYSIS_ONLY）。每批含目标 / 涉及文件 / 依赖 / 回滚。

### P0-1 默认路径接通（最高优先，服务全部诉求）

- **目标**：让 v10.3.0 已建成的能力真正到达默认用户。三选一或组合：**(a)** 把 `VAP_AGENT_BACKEND` 默认值切为 `react`（需先补齐 P0-2/P0-3）；**(b)** 在 legacy 路径补装 scope_guard + supervisor；**(c)** 前端 agent 页显式提供"引擎"切换并高亮推荐值。**推荐 (a)+(c) 组合，分两步：先修 P0-2/P0-3，再切默认。**
- **涉及文件**：`src/web/routers/agent.py`（_get_backend / _build_react_agent）、`.env.example`、`webapp/src/app/(app)/agent/page.tsx`
- **回滚**：`VAP_AGENT_BACKEND=legacy` 保留；header `x-agent-backend` 可按请求切。
- **验证**：默认启动 → agent 页发一条消息 → 确认走 react → supervisor/审批/记忆均生效；legacy 回退仍可用。

### P0-2 审批链路闭环修复

- **目标**：修掉 react 模式下审批事件到不了前端的断点（`/chat` 返回 `auto_run=false` 时前端不订阅 `/run_stream`）。
- **涉及文件**：`src/web/routers/agent.py`（`/chat` 响应契约）、`webapp/src/app/(app)/agent/page.tsx:186`
- **回滚**：纯前端/契约修复，可单点回退。
- **验证**：react 模式下触发一次写工具 → 前端收到 `approval-request` → 弹窗 → 允许/拒绝均生效；超时 60s deny。

### P0-3 工具风险分类修正 + 审批分级可见

- **目标**：修正 `make_subtitle` / `make_short_video` / `make_voiceover`（真实落盘却归 read）与 `cdp_evaluate`（任意 JS 却归 read）；让 `priority` 字段真实区分危险写与普通写；审批弹窗加中文工具名、后果说明、倒计时、"本会话总是允许"。
- **涉及文件**：`src/core/tools/scope_guard.py`（classify / priority / DEFAULT_ALLOW_PREFIXES 死常量清理）、`webapp/.../agent/page.tsx`（审批 UI）
- **回滚**：分类表可配置。
- **验证**：`test_scope_guard.py` 补分类用例（含回归：真实写盘工具必须 ask）；前端审批弹窗人工验收。

### P0-4 未接线模块了断（接线或明确移除）

- **目标**：`set_error_policy`（重试熔断）/ `set_lock_resolver`（工具级读写锁）/ `validator.py`（三重验证）/ `SkillAdvisor` / `roster.with_experience` / `eli5.py` 六处未接线模块二选一：接线到生产路径，或明确移除并在文档中更正宣称。
- **涉及文件**：`src/core/tools/registry.py`、`src/skills/{validator,roster}.py`、`src/core/agent/experience.py`、`src/core/eli5.py`、`src/web/routers/agent.py`、`CHANGELOG.md`
- **验证**：接线则补非测试调用方；移除则补文档一致性检查。
- **建议**：`eli5.py` 接线（见 P1-1）；`validator.py` 接进 distiller 的准入门；`set_error_policy`/`set_lock_resolver` 接线到 react 的 registry；`SkillAdvisor`/`roster.with_experience` 若并入新链路则接线，否则删除。

### P0-5 沙箱安全复核

- **目标**：复核 `WindowsJobSandbox` 的 Job 作用域与 `KILL_ON_JOB_CLOSE` 语义，避免退出时杀掉宿主后端；enter 失败必须可见（当前仅 warning 降级）。
- **涉及文件**：`src/core/runtime/sandbox.py`、`registry.py:244-249`
- **验证**：最小实验——启用沙箱跑一条无害命令，确认宿主存活；失败路径有用户可见提示。

### P0-6 监督层半成品修复（压缩反增 bug）

- **目标**：修 `ContextCompressor` 的**消费侧**——`loop.py` 必须使用 `cond.messages` 替代全量 `derive_messages()`，否则压缩不仅无效反而增加 token；同时修 marker 幂等导致的摘要永不刷新。附：`step_timeout_sec` 要么用 `asyncio.wait_for` 真正消费，要么删除字段并更正文档；`Supervisor` 生命周期改为跨请求复用（或把累计预算/卡死窗口持久化）。
- **涉及文件**：`src/core/agent/loop.py`、`supervisor.py`、`src/web/routers/agent.py`
- **验证**：新增 loop 级端到端用例（当前两个集成用例都显式 `compress_max_events=1000` 关闭压缩）——断言压缩后 prompt token 下降且摘要会随新事件刷新。

### P0-7 发布闭环 + 版本同步 + 诚实性修正

- **目标**：① 重传安装包到 GitHub Release（当前 asset 为 `starter`，线上不可下载）；② 修 `electron-builder.yml:33` 的 config filter，让 `config/skills/**/SKILL.md` 进包（当前安装包内 0 个 skill）；③ 同步 `src/web/app.py:142`（10.2.0→10.3.0）、`start-desktop.bat` 与 `CLAUDE.md` 的版本号；④ 更正 `CHANGELOG.md` v10.3.0 与 `workflow_status.md` 的过度声称（"独立 Critic PASS" 无产物、"全量绿" 实为 1 failed）；⑤ 清理工作区（未提交删除 + 未跟踪脚本）；⑥ 修 `ci.yml` 对不存在文件的 ignore。
- **涉及文件**：`desktop/electron-builder.yml`、`src/web/app.py`、`start-desktop.bat`、`CLAUDE.md`、`CHANGELOG.md`、`workflow_status.md`、`.github/workflows/ci.yml`
- **回滚**：文档/配置层，可单点回退。
- **验证**：重传后 `release.assets` 长度 ≥1 且 state=uploaded；解包确认 `resources/config/skills/` 有 12 个目录；版本号 5 处一致。

### P1-1 eli5 接线 + 教学化黑匣子（服务诉求①②）— N1 已给出可执行五步

- **第 1 步（0.5 天，最高性价比）**：**给 `eli5.explain_tool_call` 接线**——在工具事件落库/推 SSE 前调用，结果写进事件字段（如 `human`），agent 页与 decisions 页优先渲染它。范本：`claude-devtools` 的 `toolSummaryHelpers.ts:20-140`（每工具一个 case 的参数级摘要）。
- **第 2 步（1 天）**：**摘要行 + 运行中帧**——抄 `claude-hud` 的 `◐/✓ + 工具中文名 + 目标 + ×N`（`tools-line.ts:17-40`）；SSE 先发 running 帧再执行（DeepTutor `labeled_step.py:430-434` 的 eager_sub_trace 填补首 token 空白）。
- **第 3 步（1-2 天）**：**日志页人话层**——事件加 `layer: summary|raw` 字段（DeepTutor `rag/service.py:112,240`），默认只显示 summary，raw 折叠；术语旁挂"?"三段气泡（`SessionContextHelpTooltip.tsx:117-166`："什么是抽帧/YOLO/OCR"）。
- **第 4 步（2-3 天）**：**任务结束"本次回顾"卡**——数据 = `decision_log` 的 reason + 工具调用序列；结构 = shareAI 的 scenario/annotation JSON（步骤+旁白+**alternatives**）；文案规范 = `tour-builder` 的"2-4 句、承接上一步、写给没见过的人"。首版纯模板拼接不调 LLM。
- **第 5 步（可选，1-2 天）**：**踩坑与技巧卡**——仿 `hermes-hud` 的 corrections 正则（用户说"不对/不是这样"）+ patterns 聚类。
- **四阶段提示卡（可并入第 2 步）**：首页分析跑批时显示"抽帧 → 检测 → OCR → 总结"四张常显卡，每张带一句"这阶段在干嘛"（DeepTutor `TracePanels.tsx:43-48` 原文风格），解决新用户只见参数表单的问题。
- **刻意不做**：双套 UI、独立教程页——首屏问题靠"阶段提示卡 + 术语气泡"就地解决。
- **涉及文件**：`src/core/agent/loop.py`（事件挂 eli5）、`src/web/routers/agent.py`（SSE 增 human/layer 字段）、`webapp/src/app/(app)/{agent,logs,decisions}/page.tsx` + 新组件
- **回滚**：UI 层开关，关掉即回原样。
- **风险**：eli5 模板依赖工具 result 前 200 字格式（`eli5.py:19-20`），`agent_tools.py` 输出格式变更会静默退化为"返回了 N 字符"——建议加格式契约测试。

### P1-2 Onboarding + 首页重构（服务诉求①）

- **目标**：首次启动引导（这是什么产品 / 能做什么 / 三步上手）；首页从"AI 视频分析表单"改为 **Agent 对话入口 + 能力卡片**，视频分析降为其中一张卡。参考 `LifeOS` 访谈式 onboarding、`awesome-ux-skills` 的 ai-wayfinders、`locally-uncensored` 的引擎探测向导。
- **涉及文件**：`webapp/src/app/(app)/page.tsx`、新增 `components/onboarding/`、`docs/guide/getting-started.md`（去掉 TODO，补小白路径）
- **验证**：全新用户视角走一遍；引导可跳过且不重复出现。

### P1-3 用户画像与个人记忆（服务诉求③）

- **目标**：在现有 `working/triplestore/experience` 之上补**用户画像层**：稳定事实 + 近期偏好双段（supermemory 范式），≥3 次出现的观察才晋升（opencode-magic-context 范式），提供"AI 记住了我什么"的查看/编辑/删除页。复用已有 `history_manager.remember_preference` 雏形。
- **涉及文件**：`src/core/memory/`（新增 profile 模块）、`src/core/agent/loop.py`（注入点）、`src/web/routers/agent.py` 或新 router、`webapp` 新增记忆页
- **回滚**：feature flag + 数据可清空。
- **验证**：跨两次会话验证偏好被记住；用户可删除且删除后不再注入。

### P1-4 用户 skill 采纳闭环（服务诉求③）

- **目标**：现有 distiller→validator→scoring→spectre 已有骨架，缺"用户可见的采纳流程"。照 `SkillOpt-Sleep` 补：**dry-run 只报告 → staged 提案预览 → 用户 adopt 才落盘 → 可回滚**；触发质量用 `agent-skills` 的 TF-IDF rank-1 做 CI 门。
- **涉及文件**：`src/skills/{distiller,scoring,roster}.py`、`src/web/routers/skills.py`、`webapp/src/app/(app)/skills/page.tsx`
- **验证**：端到端——完成 3 次同类任务 → 出现提案 → 采纳 → 下次任务命中该 skill → 回滚可用。

### P2-1 真实 .pptx 生成（服务诉求④）

- **目标**：新增 `src/core/tools/ppt/`（python-pptx 直出 + 7 版式 + theme token + Report→Deck IR 适配器 + QA 闸门）。**唯一新依赖 `python-pptx>=0.6.23`（纯 Python）**。设计规则直接采用 `OfficeCLI` 的硬性版式闸门与反 AI-tell 清单（见 §全栈方案）。差异化：输入不是"主题"而是**已跑完的视频分析事实 + 关键帧 + matplotlib 图表**。
- **涉及文件**：新增 `src/core/tools/ppt/{theme,layouts,render,deck_ir,charts,qa,from_report}.py`；`config/skills/ppt-deck/SKILL.md` 升级；`registry` 注册为写工具（走审批）
- **验证**：一条端到端样例 `分析 JSON → 8 页 .pptx（含图与关键帧）`，用 PowerPoint/LibreOffice 打开确认文字可编辑。

### P2-2 图像 / 视频生成（服务诉求④）

- **目标**：免费层优先——本地 TTS（`LuxTTS`/`VoxCPM` 范式）、本地字幕（已有 faster-whisper）、竖屏成片（已有 media_gen）、**本地图像生成（ComfyUI 桥接，可选依赖）**；付费文生视频走 **Mock 协议层**（请求/轮询/回调/超时/重试/取消全 mock 覆盖）。
- **涉及文件**：`src/core/tools/media_gen/`（扩 image provider）、`_providers.py`（补 ImageGen Protocol + Mock）
- **验证**：本地图像生成端到端 1 张；付费路径全部走 mock，零真实调用。

### P2-3 电商能力（服务诉求⑤，合规前置）

- **目标**：**只读优先**——商品页/评价/竞品的读取与分析（复用 `web_auto`）；写操作（下单/改价/上架/发消息）一律 `ask` 审批且默认拒绝。参考 `commerce-agents` 的 staged 审批与 `claude-ads` 的"默认只读 + 五道门禁"。
- **中文平台现状（N5 勘误后）**：
  - **知识层有**：`agency-agents/marketing/marketing-china-ecommerce-operator.md`（283 行）是目前参考集中最完整的淘宝/天猫/拼多多/京东/抖音运营手册——含**标题公式与字数上限**（淘宝/PDD 60 字符、京东建议 45 字符）、**主图 5 槽位模型**、**详情页 8 段结构**、**投放术语**（直通车/万相台/多多搜索/京速推）、**大促 T-60→T+7 战役模板**、**抖音合规守则**（禁绝对化用语）。另有抖音/直播/快手/B站/微博策略人设。→ 可**改写后**进 VAP 的电商 skill，作为"上架包"的骨架，落地成本显著降低。
  - **执行层无**：所有中文平台内容均为纯 prompt 人设，**无工具、无 API、无真实执行**；EcomAgent 的 taobao/jd/douyin adapter 仍是 stub。真实接入仍需 VAP 自造（字段模型 + 数据解析 + adapter）。
- **红线（新增）**：`agency-agents` 文本含具体违禁词的"规避话术"（prohibited language workarounds）——**VAP 只能实现"识别并拦截禁词"，不得实现"生成替代词以绕过平台审核"**，否则等同协助规避平台合规审查。其人设中"实时监控热榜/竞品价格并动态调整"均为策略文本，落地须映射到合规数据源，不得据此实施高频爬取。
- **红线（原有）**：不做刷单、不做绕过平台风控、不做未授权爬取、不真实下单；不提供任何代下单/代支付工具（照抄 `commerce-agents`：从后端层面就不提供该方法）。
- **建议 MVP**：**上架包生成器**（零爬取、零写入、当天可交付）——输入商品资料/参考图 → 输出标题×20（含平台字数十限与禁词自检）、5 条卖点、详情描述、关键词表、主图/短视频脚本（走已有 `media_gen`），落成 Markdown + 可粘贴表格，标注"请人工核对后到千牛/抖店后台提交"。
- **验证**：只读分析路径端到端；写操作确认弹窗出现且默认拒绝。

### P3-1 / P3-2

- **P3-1 生态**：MCP 工具面扩展（media_gen/web_auto 已入 MCP）、插件市场形态（参考 `awesome-dsh-plugin`）。
- **P3-2 多智能体可见性**：子 agent 卡片（ID/角色/状态色/模型/耗时/tokens）+ 上下文成分面板 + "需要你介入"优先列（参考 `agent-orchestrator` 六列看板、`agent-teams-ai` 六类上下文）。

---

## Validation Matrix

| Requirement | Validation Method | Actual Command/Action | Result | Evidence |
|-------------|-------------------|----------------------|--------|----------|
| 全量扫描 988 条目 | Workflow 33 批并行 | Workflow `ref-scan-987` 完成 | 31/33 批产出卡片，912 卡 | `wag3524es.output` |
| 主项目默认后端核实 | 源码直读 | Read `agent.py:182-191`、`.env.example:110` | 默认 legacy | file:line |
| 默认路径工具执行核实 | 源码直读 | Read `agent_tools.py:37-46` | 裸调用无治理 | file:line |
| supervisor 接线核实 | grep + 源码直读 | `grep -n supervisor loop.py` + Read `agent.py:751-879` | 已接线，默认关 | file:line |
| eli5 孤立核实 | 全仓 grep | `grep -rn "eli5" src/ webapp/` | 仅定义文件自身 | Bash 输出 |
| onboarding 缺失核实 | 全仓 grep | `grep -ir "onboarding\|tutorial\|guide" webapp/src/` | 无命中 | Bash 输出 |
| PPT 能力核实 | 全仓 grep + skill 直读 | `grep -rn "pptx" src/ config/ requirements.txt` | 0 命中 | Bash 输出 |
| 工具/审批测试实跑 | pytest | `./venv/Scripts/python.exe -m pytest tests/test_scope_guard.py tests/test_agent_router_assembly.py tests/test_web_auto.py -q` | **57 passed, 1 skipped, 39.93s** | 命令输出 |
| 规模计数 | 统计含 `APIRouter(` 的文件数 / find / ls | `grep -l "APIRouter(" src/web/routers/*.py \| wc -l`（**注意 `ls *.py \| wc -l` 会算上 `__init__.py` 得 17**）· `ls tests/test_*.py \| wc -l` · `find webapp/src/app -name page.tsx \| wc -l` · `ls -d config/skills/*/ \| wc -l` | 16 / 91 / 16 / 12（Critic A 独立复核确认） | Bash 输出 |
| 安装包存在 | ls -la | `desktop/dist/TingFeng Hermes Setup 10.3.0.exe` | 697,488,682 B | Bash 输出 |
| 全量测试 |  | **1 failed（gc_guard 并发 flaky，孤立复跑通过）+ 1324 passed + 3 skipped，719.82s** | DIRECT（**非绿**） | M5 核查 |
| 覆盖率 80.25% | 未复跑（ mtime 22:45 早于末次提交 23:13） | **UNVERIFIED** | UNVERIFIED | M5 核查 |
| Electron 真机启动 | 人工 | **未运行**（无 GUI 会话） | UNVERIFIED | — |

---

## Review Findings

> ANALYSIS_ONLY 阶段无 Builder 修复、无独立 Critic 复验（Critic 在 Phase B 才启动）。以下为主控自检 + 15 个独立核查/精读代理的交叉发现。

### 最强发现：v10.3.0 是「建造完成、交付未完成」

v10.3.0 的七个批次（P0-1 监督层 / P0-2 写审批 / P1-1 记忆分层 / P1-2 skills 闭环 / P1-3 Electron 黑匣子 / P2-* 扩展域 / P3-* MCP 与设计）的**源码与测试全部真实存在**，实跑证据：supervisor 13 + agent_framework 30 + assembly 19 = 62 passed；scope_guard 相关 57 passed；memory/skills 相关 174 passed；media_gen/web_auto/mcp 相关 82 passed。**这不是"造假"，是"没接通"。**

具体而言，能力停在三层中的不同位置：

| 层 | 含义 | v10.3.0 落点 |
|----|------|--------------|
| L1 已建成 | 代码存在 + 单测通过 | 全部批次 |
| L2 已接线 | 生产路径调用 | 大部分（supervisor 5 个调用点、scope_guard 装配、memory connector、断路器、诊断 zip） |
| **L3 已送达** | **默认用户可用** | **几乎没有** |

**造成 L2→L3 断裂的 7 个具体断点**（全部 DIRECT）：

1. `VAP_AGENT_BACKEND` 默认 `legacy` → 全部 Agent 架构不可见
2. `VAP_AGENT_SUPERVISOR` 默认 `false` → 监督层双闸门关闭
3. legacy 路径工具裸执行（`agent_tools.py:37-46`）→ 无审批/沙箱/审计
4. react 模式审批 SSE 断点（`auto_run=false`）→ 弹窗收不到事件，写工具静默挂 60s
5. **skills 正文未注入**（`agent_prompt.py:218` 只给 name+description）→ SKILL.md 的模板与决策表从未到达模型
6. **压缩丢弃裁剪结果**（`loop.py:219-235`）→ 摘要反增 token
7. **skills 蒸馏无落盘通道** + **TripleStore/FTS5 只写不读** + **validator 零调用** → 记忆与技能的"读"侧整体缺失

### 系统性模式：本项目反复出现「定义了但没人调用」

| 版本 | 未接线模块 | 状态 |
|------|-----------|------|
| v10.2 | `set_approval_fn` / `set_sandbox` | v10.3 已修 ✅ |
| v10.3 | `set_error_policy` / `set_lock_resolver` | 仍零调用 ❌ |
| v10.3 | `eli5.py`（154 行完整实现） | 零生产调用方（仅 3 个测试 import）❌ |
| v10.3 | `validator.py`（三重验证） | 仍零调用 ❌ |
| v10.3 | `SkillAdvisor` / `roster.with_experience` | 仍零调用 ❌ |
| v10.3 | `PluginLoader` / `SubagentDirector` / `build_domain_role` | 仍零调用 ❌ |
| v10.3 | `cua-service.js`（打进安装包） | 未挂载 ❌ |
| v10.3 | `media_gen`(4) / `web_auto`(12) 工具 | 只在 MCP，主 agent 不可达 ❌ |

**建议固化 CI 门**：新增公开符号必须有非测试调用方，或显式标注 `# not-wired: reason`。这条门的价值高于任何单个功能修复。

### 诚实性风险（需在 P0 批次内修正）

`CHANGELOG.md` v10.3.0 的表述与实测存在硬缺口：
- "独立 Critic PASS" → 全仓无任何 Critic 产物，`workflow_status.md` 的 Review Findings 栏为空
- "全量 1324 passed + 2 skipped" → HEAD 实跑为 **1324 passed + 3 skipped + 1 failed**（gc_guard 并发 flaky）
- "覆盖率 80.25% 门禁达标" → `.coverage` 时间早于最终提交，HEAD 覆盖率 UNVERIFIED
- "Playwright 23 passed" → 唯一痕迹时间早于最终提交
- "安装包小白双击即用" → **Release asset 为 `starter`（线上不可下载）**，且**包内 0 个 skill**（filter 排除 SKILL.md）

**这不影响代码价值，但会误导后续会话与用户**——本 v3 指南的多处结论正是靠"不采信文档、只认源码与实跑"才得以发现。

### 正面事实（同样重要）

1. **代码质量与注释密度高**：`supervisor.py` / `scope_guard.py` / `_providers.py` 的注释直接解释设计权衡与红线，优于多数同规模开源项目。
2. **付费红线严格守住**：三重保证（缺 key 回落 mock + 付费 stub `raise NotImplementedError` + 文档声明），且 mock 结果全部显式标注 `mock=True`/`mode:"mock"`，不伪造成功。
3. **记忆真的在写**：生产库 `agent_working_memory.db` 155 行、`agent_triples.db` 9 行、`agent_experiences.db` 155 行 —— 不是空壳。
4. **Electron 壳主体真实**：断路器有运行痕迹（`logs/runtime.log` 的 `[breaker] 冻结重启…auto re-arm`）、诊断 zip 6 条目真实现、`tsc --noEmit` 0 错误。
5. **web_auto Playwright 真实可用**：chromium 二进制已装，真实 navigate/snapshot/click/screenshot 测试通过。
6. **参考目录的四个方向都有成熟对照实现**，且部分是主项目血统内（Hermes/DSH）的现成能力——落地路径短。

### 剩余风险

见 §Risks and Blockers。其中 **L3 四项**（默认后端切换、沙箱宿主风险、用户画像隐私、电商合规）需用户明确授权后才可实施。

---

## Next Gate

- **下一步动作**：用户审阅本 v3 指南 → 确认实施范围与排序 → 方进入 Phase B（实施）。
- **是否需用户批准**：**是**（ANALYSIS_ONLY 约束未解除）。
- **需批准的准确范围**：
  1. **排序原则**：是否同意 **P0「Deliver, not Build」（把已建成能力接到默认路径 + 修发布闭环）优先于 P1/P2 新增**？本轮核查证明 v10.3.0 的投入若不接通等于零。
  2. **默认后端**：是否授权把 `VAP_AGENT_BACKEND` 默认切为 `react`？（L3；需先完成 P0-2/P0-3/P0-6，保留 legacy 回退）
  3. **沙箱**：`VAP_SANDBOX_ENABLED` 是否维持默认关闭直到 P0-5 复核通过？
  4. **skills 正文注入**：是否授权把 SKILL.md 正文注入系统提示？（会显著增加 token，需定预算策略——建议按 roster 命中才注入正文）
  5. **用户画像**：是否授权建立跨会话用户偏好/画像（本地存储 + 可查看删除）？数据保留策略？
  6. **P1 优先级**：教学化（P1-1）与 onboarding（P1-2）是否先于用户画像（P1-3）与 skill 采纳（P1-4）？
  7. **P2 扩展域**：PPT / 图像视频 / 电商三项的优先级与授权边界；电商是否只做只读？
  8. **新依赖**：是否批准 `python-pptx`（P2-1 唯一新依赖）与 `edge-tts`（解锁真实配音，免费）？ComfyUI 是否走可选依赖？
  9. **发布闭环**：是否授权重传安装包到 GitHub Release + 修 electron-builder filter？
  10. **诚实性修正**：是否授权更正 `CHANGELOG.md` v10.3.0、`workflow_status.md` 的过度声称并清理工作区？
  11. **CI 接线门**：是否授权新增"定义了必须有调用方"的 CI 检查？
  12. **黑匣子日志的默认呈现**（Critic B 指出此前的改写未请你确认）：你的原话是"日志**全开**"，而方案设计为"**默认简单模式（人话层）+ 专家视图一键切换**"（考虑小白首屏可读性）。请确认：**A) 保持默认简单模式 + 一键切专家**（推荐）/ **B) 默认全开（专家视图为默认）** / **C) 首次启动问一次，记住选择**。
  13. **P1-5（Agent 内核深化）的细节粒度**（Critic B 指出它是你第一诉求的唯一载体却只有一行）：是否需要我把它展开为与 P1-1 同等粒度的批次卡（逐机制：压缩硬化 / 工具结果预算 / 卡死恢复 / XML 任务块 / 模型分档，各自文件+验证+回滚），并追加"**每个 subagent 角色的逐个优化分析**"？

---

# 第一部分：项目识别结果

## 1.1 主项目识别结论

| 维度 | 结论 | 依据 |
|------|------|------|
| 项目类型 | 桌面端 AI Agent 平台（前端分离 + 多进程） | `desktop/` Electron 主进程 + `src/web/` FastAPI 侧车 + `webapp/` Next 16 静态导出 |
| 语言/框架 | Python 3.10+ / FastAPI / uvicorn / SSE；TypeScript / Next 16 / React 19 / Tailwind v4；JavaScript / Electron | `requirements.txt` / `webapp/package.json` / `desktop/package.json` |
| 包管理 | pip（`requirements*.txt` 分层）+ npm（两个独立 package.json，无 workspace） | 同上 |
| 构建 | `start-desktop.bat`（唯一入口）/ `npm run dist`（electron-builder NSIS）/ `next build`（静态导出到 `out/`） | `scripts/`、`desktop/electron-builder.yml` |
| 运行入口 | `launcher.py`（版本门禁+venv）→ `desktop/main.js` → spawn `src/web/serve.py` → `loadURL(webapp/out)` | 源码直读 |
| 模块边界 | `src/core/agent`（Agent 内核）/ `src/core/tools`（工具+治理）/ `src/core/memory`（记忆）/ `src/skills`（技能）/ `src/core/im_gateway`（IM）/ `src/remote`（隧道）/ `src/mcp`（对外 MCP）/ `src/web/routers`（16 router） | 目录 + 计数 |
| 核心业务流程 | ① Agent 对话（ReAct 循环 + 工具调用）② 视频分析三阶段（抽帧/转录/检测 → LLM → 媒体生成）③ 批量任务 ④ 监控 ⑤ skills 蒸馏 ⑥ IM 投递 | 源码 + 页面 |
| 数据访问 | SQLite（多库：agent_sessions/agent_experiences/im_gateway/cred_audit/history/runs）+ ChromaDB（kb_frames/user_preferences） | 源码 + `.gitignore` |
| 配置管理 | `.env`（env 优先）+ `config/app_config.ini` + keyring（凭据分层 env>keyring>ini） | `src/core/credentials/key.py` |
| 日志/异常 | JSON 结构化日志 + trace_id（`src/core/runtime/structured_log.py`）+ `/api/logs` 环形缓冲 + SSE；Electron 侧 LogStore 黑匣子 | 源码 + M4 核查中 |
| 测试 | pytest 91 个测试文件；Playwright e2e；CI 覆盖率门禁 80% | 计数 + `ci.yml` |
| 部署 | Windows NSIS 安装包（697MB，含 venv+src+config+webapp-out）；Dockerfile + Dockerfile.cuda + compose | 产物 + 源码 |
| 缓存/队列/调度 | 无消息队列；`batch_runner.py` 内存批处理；`run_store` 持久化；无独立调度器 | 源码 |
| 未提交改动 | `参考的结果计划指南.md` 被删除（工作区状态，本文件即为其 v3 重建）；`scripts/create-release.ps1` 未跟踪 | `git status` |

**判断置信度**：高（全部为源码/命令直读）。

## 1.2 参考项目扫描结果

见 §Project Inventory。核心数字：**988 条目（写作时点）→ 912 个唯一项目（919 条卡片记录，31/33 批，2 批缺口）**；高价值 406 个；类型分布以 agent-framework(251) / skill-system(123) / desktop-app(82) 为主。

## 1.3 候选参考项目清单

完整 912 张卡片存于 Workflow 输出（`wag3524es.output`，1.2MB JSON）与 `.agent-scratch/recon/`。本节按用户 6 新方向 + 3 通用坐标给出精选（见 §高价值参考项目）。

## 1.4 最终选中的高价值参考项目及原因

| 方向 | 首选 | 理由 |
|------|------|------|
| ① 小白教学化 | `claude-devtools` + `mimik` + `ai-agents-from-scratch` | 唯一同时给出"四件套可视化 + 教程化录制回放 + 代码/概念双讲解"的组合 |
| ② 用户画像 | `supermemory` + `honcho-main` + `opencode-magic-context` | 双段画像 + peer.chat 追问 + ≥3 次晋升，三者互补且均可本地跑 |
| ③ skills 沉淀 | `SkillOpt(+sleep)` + `SkillClaw` + `darwin-skill-master` | 唯一齐全的"训练/双环/棘轮+独立评分"三视角 |
| ④ 图像视频生成 | `locally-uncensored` + `Pixelle-Video` + `ffmpeg-skill` | 本地 ComfyUI 全链 + 主题到成片 + 零付费 40 工具 |
| ⑤ 电商 | `commerce-agents` + `claude-ads` | staged 审批 + 默认只读五道门禁，合规姿态最稳 |
| ⑥ PPT/办公 | `OfficeCLI` + `ppt-master` | 前者给规则与 QA 门禁，后者给 python-pptx/SVG→原生形状路线 |
| ⑦ Agent 内核 | `pi-shadow-mind` + `LongHorizon-Harness` + `token-optimizer` + `council` | 影子观察 / 独立验证才算进度 / 压缩可恢复 / 审查反从众 |
| ⑧ 桌面壳 | `pi-agent-desktop` + `atlas` + `Minke` | 关注点拆分文件+测试 / checkpoint 黑匣子 / 模块一一对应 |
| ⑨ 工程化 | `AI-company` + `bernstein` + `agent-skills` | 不变量校验 / 审计链 / 三级评测 |

## 1.5 排除目录及原因

见 §排除目录及原因。

## 1.6 判断置信度

- 主项目结论：**高**（源码 file:line + 实跑命令）
- 参考项目分类：**中高**（912 卡基于 README/清单文件；2 批缺口：61-90 已补扫 / 421-450 为 PARTIAL）
- 路线图优先级：**中**（依赖用户对排序与授权的确认）

---

# 第二部分：主项目现状总结

## 2.1 当前架构特点

三层分离 + 双引擎并存：
- **外壳层**：Electron（单实例锁 / 托盘 / 崩溃断路器 / LogStore 黑匣子 / autoUpdater 代码就位）
- **服务层**：FastAPI 16 router + SSE 流式 + 结构化日志 + GCGuard
- **界面层**：Next 16 静态导出 16 页
- **Agent 双引擎**：`legacy`（`agent_orchestrator.py` + `agent_tools.py`，默认）与 `react`（`ReactLoopAgent` + Session/Turn/supervisor/scope_guard/memory）

## 2.2 当前优点（DIRECT）

1. **架构完整度高**：Agent 内核、工具四 waterfall、并行读写锁、子代理 director、插件 contextvars、IM 网关、远程隧道、MCP server 齐备——覆盖通用 Agent 平台的全部主要部件。
2. **治理意识强**：scope_guard 三级决策、审批 SSE 通道、沙箱抽象（Windows Job / Linux Landlock / Null）、凭据分层、提示注入守卫、技能安全扫描——安全设计密度高于同规模开源项目。
3. **代码质量与可读性好**：新模块注释详尽（supervisor.py / scope_guard.py 的注释直接解释设计权衡与坑）。
4. **工程化到位**：CI 覆盖率门禁 80%、Playwright e2e、结构化日志、GC 守护、版本多位置同步、真实 Windows 安装包。
5. **本地优先与隐私姿态坚定**：SQLite + ChromaDB，数据不出设备，付费 API 默认零预算。

## 2.3 当前主要问题（完整缺陷清单，全部 DIRECT）

### A. 送达类（能力已建成，但用户拿不到）

| # | 问题 | 证据 | 严重度 |
|---|------|------|--------|
| A1 | **默认后端 legacy 使 v10.3.0 全部 Agent 架构默认不生效**（双闸门：`VAP_AGENT_BACKEND=legacy` + `VAP_AGENT_SUPERVISOR=false`） | `agent.py:190`、`.env.example:110/126`、`supervisor.py:433` | **P0** |
| A2 | **默认路径工具执行无任何治理**（无 waterfall/审批/沙箱/审计/锁/重试） | `agent_tools.py:37-46` 裸 `execute(**args)` | **P0** |
| A3 | **react 模式审批事件也到不了前端**（`/chat` 返回 `auto_run=false` → 前端不订阅 `/run_stream`） | `agent.py:355-360` + `page.tsx:186` | **P0** |
| A4 | **工具面分叉**：`media_gen`(4) + `web_auto`(12) 只在 MCP 注册，Web 主路径仅 16 个 legacy 工具 | `mcp/server.py:135-137` vs `agent.py:790` | P1 |
| A5 | `set_error_policy`/`set_lock_resolver` 零调用 → 重试熔断与工具级读写锁从不生效 | 全仓仅定义 `registry.py:101/147` | P1 |
| A6 | `eli5.py`（大白话翻译器，10+ 工具模板）零引用 | `grep -rn eli5 src/ webapp/` 仅定义文件 + 测试 | P1 |
| A7 | `SkillAdvisor` / `roster.with_experience` / `validator.py` 零生产调用 | M1/M3 核查 | P1 |
| A8 | **skills 正文未注入**：只注入 `name: description`，SKILL.md 正文（PPT 版式模板、电商决策表）**根本进不了模型** | `agent_prompt.py:218`、`agent.py:833-834` | **P0**（对应用户诉求③） |
| A9 | `media_gen`/`web_auto` 未挂主 agent（只在 MCP 与测试注册）；`PluginLoader`/`SubagentDirector`/`build_domain_role` 同样零生产调用 | `mcp/server.py:135-137` vs `agent.py:790` | P1 |

### B. 半成品类（接线了但不工作）

| # | 问题 | 证据 | 严重度 |
|---|------|------|--------|
| B1 | **压缩链路丢弃裁剪结果 → 摘要反增 token**：`cond.messages` 未使用，消息仍从全量事件派生 | `loop.py:219-235` vs `supervisor.py:219-227` | **P0** |
| B2 | **`step_timeout_sec` 死配置**，无单步超时保护；`except asyncio.TimeoutError` 不可达 | `loop.py:59,68,269`；`agent.py:264-274` 吞异常 | P1 |
| B3 | **Supervisor 每请求重建** → 累计预算/卡死窗口跨轮丢失；阈值不可 env 配置 | `agent.py:348,459,871` | P1 |
| B4 | **TripleStore 只写不读**（9 行数据，查询零调用） | M3 核查 | P1 |
| B5 | **FTS5 混合检索无生产读者**（表/公式/触发器真实，但 `find_similar` 唯一调用方是死代码） | `experience.py:167-173,307-308,369` | P1 |
| B6 | **skills 蒸馏断头**：`/api/skills/distill` 只返回 markdown，无落盘/应用步骤 + 无 UI | `skills.py:187-194` | **P0**（对应用户诉求③） |
| B7 | `skill_generator` 仅 4 个硬编码场景，parking 与内置撞名必失败 | `skill_generator.py:63-79` | P2 |
| B8 | spectre 硬阻断（`enabled and not security_warning`）与 UI"可手动开启"文案矛盾 | `loader.py:148` | P2 |
| B9 | Working Memory TTL 无物理清理（`forget_old` 零调用，表无限增长） | `working.py:87-109` | P2 |
| B10 | 工具风险分类错误：3 个真实写盘工具 + `cdp_evaluate`（任意 JS）被归 read/allow | M2 核查 | **P0（安全）** |
| B11 | `DEFAULT_ALLOW_PREFIXES` 是死常量，classify 未使用 | `scope_guard.py:143-150` | P2 |

### C. 收尾与发布类（M5 核查）

| # | 问题 | 证据 | 严重度 |
|---|------|------|--------|
| C1 | **GitHub Release 安装包不可下载**（asset `state="starter"`，上传从未完成；`release.assets` 长度 0） | Release id 385683679 | **P0（交付）** |
| C2 | **安装包内 12 个 skill 全部缺失**（electron-builder config filter 排除 `SKILL.md`） | `electron-builder.yml:33` | **P0（交付）** |
| C3 | **全量测试在 HEAD 实跑 = 1 failed, 1324 passed, 3 skipped**（gc_guard 并发 flaky） | 719.82s 实跑 | P1 |
| C4 | **"独立 Critic PASS" 无任何产物** | 全仓搜索为空 + workflow_status Review Findings 栏为空 | P1（诚实性） |
| C5 | 覆盖率 80.25% 数据早于最终提交 → HEAD 覆盖率 UNVERIFIED | `.coverage` mtime 22:45 vs 提交 23:13 | P2 |
| C6 | Playwright "23 passed" 仅 09-09 05:35 的历史 `.last-run.json`，早于最终提交 | `webapp/test-results/` | P2 |
| C7 | 版本第 5 处不同步：`app.py:142` = 10.2.0；`start-desktop.bat`/`CLAUDE.md` 停在 v9.0.0 | M5 核查 | P1 |
| C8 | CHANGELOG v10.3.0 过度声称（"全部真实落地 + 独立 Critic PASS"）与实测不符 | 对照 | P1（诚实性） |
| C9 | 工作区脏（未提交删除 + 未跟踪 `scripts/create-release.ps1`）；`ci.yml` 仍 ignore 不存在的 `test_e2e_smoke.py` | `git status` | P2 |

### D. 体验类（用户本轮第一诉求）

| # | 问题 | 证据 | 严重度 |
|---|------|------|--------|
| D1 | **前端零 onboarding / tutorial / guide** | grep 无命中 | P1 |
| D2 | **首页是"AI 视频分析"参数表单**，与 README"通用 Agent 平台"定位不一致；`/` 与 `/analyze` 还是重复页 | `page.tsx` 首屏 | P1 |
| D3 | **黑匣子日志是开发者视角**，零解释层（级别/SSE/断路器/诊断包全是黑话） | M4 核查；小白友好度评分 1/5 | P1 |
| D4 | logs 页实时按钮**死锁**（`onClick` + `disabled={live}` 同时存在） | `logs/page.tsx:176` | P1 |
| D5 | logs 复制时间戳秒/毫秒混用；前后端日志无去重；trace_id 前端丢弃 | `page.tsx:116`、`log-store.js:91` | P2 |
| D6 | 日志仅内存（LogStore 2000 条 / 后端环 500 条），重启即失 | `log-store.js:74`、`logs.py:29` | P2 |
| D7 | 无用户画像/人格建模（仅 `history_manager` 的 ChromaDB 偏好雏形） | 全仓 grep | P1 |
| D8 | **CUA 服务死代码进安装包**（`mountCuaService` 零调用） | `cua-service.js:72`、`electron-builder.yml:13` | P2 |
| D9 | autoUpdater 空转（无 feed、无更新 UI） | `main.js:82-90` | P2 |

### E. 能力缺口类（P2）

| # | 问题 | 证据 |
|---|------|------|
| E1 | **无真实 .pptx**（`ppt-deck` 只出 `deck.md` + HTML） | 全仓 grep pptx = 0 |
| E2 | **无图像生成**；`torch` 为 CPU-only，本地扩散不可用 | venv 实测 `torch 2.14.0+cpu` |
| E3 | `edge-tts`/`pyttsx3` 未装 → `voiceover.py` 只出 Mock wav | N4 核查 |
| E4 | 电商 4 个 skill + 浏览器工具，但 Web 路径不可达 | A4 |
| E5 | `logic.py` 1591 行巨石（历史遗留） | 行数 |

## 2.4 风险点 / 技术债

- **技术债 A：双执行器并存**。legacy 与 react 工具集同为 16 个，但**治理层不同**（waterfall/审批/沙箱 仅 react 有），且 MCP 侧另有 16 个 Web 路径拿不到的工具。任何 Agent 相关改动都要问"哪条路径"，维护成本翻倍。**收敛为单执行器是中期必做项**。
- **技术债 B：feature flag 泛滥且默认关**。`VAP_AGENT_BACKEND`/`VAP_AGENT_SUPERVISOR`/`VAP_SANDBOX_ENABLED`/`VAP_SKILLS_ROSTER`/`VAP_MEMORY_LAYERED`(默认开)/`VAP_SKILLS_AUTODISTILL`(默认开) 六个开关，小白无法理解，且"默认关"等于不存在。
- **技术债 C：宣称与实现脱节**。CHANGELOG/workflow_status 与实测存在差距，会误导后续会话与用户。
- **技术债 D：MCP 与 Web 工具面分叉**。media_gen/web_auto 只在 MCP 可用，Web 主界面拿不到——同一产品两套能力。

## 2.5 已确认事实与推断的区分

见 §Evidence Ledger 的 Type 列。**核心结论全部为 DIRECT**；UNVERIFIED 项集中在"全量测试/覆盖率"与"Electron 真机行为"。

---

# 第三部分：参考项目亮点提炼

## 3.1 按方向的高价值亮点（含证据出处）

> 完整 912 卡见 Workflow 输出。以下为精选，每条均标注来源文件/章节。

### 方向① 小白易用 + 黑匣子教学化

| 项目 | 亮点机制（含 file:line） | VAP 落点 |
|------|--------------------------|----------|
| `DeepTutor` | **summary/raw 双层 trace**（事件带 `trace_layer`，人话层永远显示、机器层折叠）`TracePanels.tsx:377-391,552-557`；**四阶段提示卡**（标题+hint，`TracePanels.tsx:43-48`）；**系统事件人话通知**（"The model output reached its token limit; asked it to continue." `agent_loop.py:363-368`）；**模式门控 + 拒绝时点名原因**（`mode.py:131-158`） | 日志页分层 + 首页阶段卡 + eli5 增 `explain_system_notice` |
| `claude-hud-main` | **一行常驻摘要**：`◐ Edit: auth.ts \| ✓ Read ×3`（运行中最多 2 条、完成最多 4 条、路径截 20 字符）`tools-line.ts:17-58` | agent 页顶部"现在在干嘛"行 |
| `claude-devtools` | **工具→摘要映射表**（每工具一个 case：Edit→`文件名 - N 行`、Read→`文件名 - lines x-y`）`toolSummaryHelpers.ts:20-140`；**概念 "?" 三段气泡**（What is X / A 与 B 区别 / 优化 Tips）`SessionContextHelpTooltip.tsx:117-166` | eli5 未知工具退化分支 + 参数表单旁"?" |
| `Egonex-AI__Understand-Anything` | **导览生成器**：5-15 步、每步 2-4 句、必须承接上一步、"写给没见过的人"（Bad: "This is the Dockerfile." / Good: 解释它在系统里的角色）`tour-builder.md:281-306`；`understand-onboard` 产出固定章节手册 | 任务结束"本次回顾"卡 |
| `shareAI-lab__learn-claude-code` | **场景回放 JSON**（每步 type/content/**annotation 旁白**）`s01.json:1-56`；**设计决策注解**含 `alternatives`（放弃过什么）`annotations/s01.json:2-40`；分步 SVG 动画 | "背后的故事"卡（decision_log reason 升级） |
| `hermes-hud-main` | **纠偏挖掘**（关键词→severity 表 + 用户纠偏正则 + 去重）`corrections.py:17-77`；**模式聚类**（人话任务标签 + 3 连工具链 trigram）`patterns.py:14-55`；快照 diff | "本次踩坑与修正" + "你常用技巧" |
| `hermes-trace` | **Span 模型**（llm_call/tool_call/subagent）+ **文本树** `├── Turn 1 (2.3s)` + ASCII Gantt + JSON/Mermaid 导出（纯 Python stdlib）| 黑匣子导出人话版文本树 |
| `mimik` | 录制浏览器操作 → 自动生成带高亮截图的教程 + Guide Me 回放 + PII 打码 | 产品内教学素材生产 |
| `explain-like-socrates` | 五段式讲解：好奇开场→引导推理→单一类比→澄清→反思；禁教科书腔 | eli5 的 LLM 层风格提示词 |
| `agent-inspect` | **facts（observed）vs inferences（deterministic + evidence id）** 分层；**首个因果失败**（禁止仅凭时间相关性下结论）`causal-failure.ts:19-52` | 日志页"诊断结论"卡 |
| 其他 | `agenttrail`（声明 vs 观测双信号）、`ai-agents-from-scratch`（CODE.md + CONCEPT.md 双讲解）、`awesome-ux-skills`（撤销/成本/引用 + 引导 onboarding + 可调行为）、`LifeOS`（访谈式 onboarding）、`locally-uncensored`（引擎探测向导）、`BoardUI`/`cate`（chat/thinking indicator）、`HERO-Anti-OverDefense`（用失败命名教学） | — |

**共性**：教学化不是"写文档"，而是**把运行时状态翻译成用户语言并常驻在界面上**。三种成熟形态：①运行中摘要行（一句人话解释当前步骤）②事后时间线（可回放、可展开细节）③概念旁注（这一步用到了什么原理）。

### 方向② AI 更懂用户

| 项目 | 亮点机制 | 证据 |
|------|----------|------|
| `supermemory` | 事实抽取 + **稳定事实 / 近期活动双段画像（~50ms）** + 矛盾消解 + 自动遗忘；单二进制本地离线 | README Features |
| `honcho-main` | peer/session 实体状态 + **`peer.chat` 自然语言追问"我是谁"** | README |
| `agentmemory-main` | 4 层整合 + 强度衰减 + 级联过时 + 引用溯源 + token 预算注入 + **省 92% token** | README |
| `VoiceMem` | 事实脑 + 情绪脑双脑 + 情绪归因 + 投机预取 | README |
| `caura` | 矛盾检测 + **supersession 排序** + 失败自动生成 rule + 四级信任 + 审计 | README Features |
| `opencode-magic-context` | **观察 ≥3 次才晋升为稳定 user memory** + 19 表 SQLite schema + historian/dreamer 双后台 | README |
| `nanobot` | `SOUL.md / USER.md / MEMORY.md` 三文件 + Git 版本化 + Dream 定时整合 | `docs/memory.md` |
| `remnic` | `extraction-judge` 耐久性评分卡 + citations | 架构文档 |
| `ai-memory` | git markdown 为源 + handoff「claimed exactly once」+ 默认零 LLM 调用 | README |
| `atlas` | checkpoint（commit ↔ session/推理绑定）+ 跨 Agent 共享 + 落盘前脱敏 | crates 结构 |

**共性 4 问的答案**（N2 部分 + S1 卡片）：
- **何时写**：会话结束批处理（多数）或显式"记住这个"；opencode-magic-context 用后台 historian 异步写。
- **冲突处理**：supersession（保留历史 + 标记失效）为主流，非覆盖。
- **注入策略**：稳定事实常驻（~50-170 token）+ 近期活动/相关记忆按需 top-k。
- **用户可控**：成熟的都有"查看/编辑/删除记忆"的 UI（这一点 VAP 必须补）。

### 方向③ 用户 skills 沉淀

见 §Task Graph N3 的 44 条机制表（存于该代理输出）。**最关键的 6 条**：

1. **dry-run → staged → adopt**（SkillOpt-Sleep）：自动产物永不直接生效，必须用户显式采纳。
2. **held-out 严格大于门**（SkillOpt `gate.py:36-52`）：候选必须严格优于当前才接受。
3. **独立评分 agent**（darwin/nuwa）：自评无效，答题与评分必须分离。
4. **发布门预防性否决**（SkillClaw `skill_verifier.py`）：不确信即拒并记录原因。
5. **触发质量可量化**（agent-skills）：TF-IDF rank-1 ≥95%，描述碰撞 ≥75% 报错。
6. **安全扫描一票否决**（SkillSpector/soul-grader）：critical 项不进加权，直接阻断。

### 方向④ 图像 / 视频生成

见 N4 报告（进行中）+ S1 卡片。要点：**免费层已足够做 10 件事**（本地 TTS / 字幕 / 切分 / 竖屏 / 关键帧 / 图表 / 信息图 / 视频检索 / 多目标跟踪 / 拼图）；付费文生视频走 Mock 协议层。
`ffmpeg-skill` 的工程范式值得整体照搬：**40 个工具 + SPEC（input_schema 从 argparse 运行时派生、CI 防漂移）+ 统一 `--dry-run/--json`**。

### 方向⑤ 电商

见 N5 报告（进行中）+ S1 卡片。要点：**合规姿态决定架构**——默认只读、写操作 staged 审批、五道门禁（claude-ads）、staged 审批（commerce-agents）。

### 【用户原始方向⑤】"不被局限在帮用户做软件项目"——任务域边界分析

> 编号说明：本指南 §3.1 的"方向①-⑨"是**领域坐标**（①教学化 ②AI懂用户 ③skills ④图像视频 ⑤电商 ⑥PPT ⑦内核 ⑧桌面壳 ⑨工程化）；用户原始诉求的"方向①-⑥"是另一套编号。本节按**用户原始编号**命名，避免混淆。

> Critic B 指出：这是用户 6 个方向中**唯一被实质性遗漏**的一个（全文此前只在目标转述里出现一次）。本节补齐。

**问题诊断**：VAP 的名字、历史与首屏都还锁在"视频分析"上，README 虽已改称"通用 AI Agent 桌面平台"，但 **16 个页面里 8 个是视频分析相关**（analyze / gallery / media / metrics / batch / surveillance + `/` 首页），能力卡片与工具集（17 legacy 工具中 11 个视频相关）同样偏向。用户所说的"不被局限"，指的不是再加几个领域 skill，而是**任务域的组织方式要改成领域无关**。

**参考项目的四种"领域无关"组织法**（可直接对照）：

| 范式 | 项目 | 做法 | VAP 适配 |
|------|------|------|----------|
| **能力卡片网格** | `Orkas`（Commander + 9 专家 + marketplace）、`Orkas` 的 `PptMaker/OfficeWorker/VideoStudio/ImageStudio` | 首页不是某个功能，而是"能干什么"的卡片墙；每个专家 agent 自带 skills/memory/tools | 首页重构（P1-2）应直接采用此形态：**Agent 对话入口 + 能力卡片网格**，视频分析降为其中一张卡 |
| **一句话建工具** | `DeepDeck` 的 WebMCP（探索→验证→保存→复用）、`CowAgent`（自然语言造技能） | 用户描述需求 → Agent 建出可复用工具 → 下次直接可用。**领域不是预设的，是长出来的** | 对应 P1-4 skill 采纳闭环 + P0-8 正文注入；这是"不被局限"最根本的机制 |
| **任务看板 / 通用工作台** | `DSH-taskboard`（七状态 + agent 只能提 in_review、人类才能 accept）、`Kun`（GUI+TUI 同运行时）、`Comfy`? —— 见 `Compass`/`Comando` 多窗格工作台 | 用"任务"而非"功能"组织界面；领域差异沉到工具层 | VAP 已有 `batch` 页雏形，可升级为通用任务看板 |
| **领域角色即插件** | `Orkas` 每 agent 独立 skills/memory/tools、`p2-2` 的 `role_template` 商务角色 | 新领域 = 新角色 + 新 skill 包，不动内核 | VAP `role_template.py` 已有 MERCHANT/SHOPPER/MARKETER，但**未接入主 agent**（A9）；补接线后即可"装一个角色就多一个领域" |

**建议覆盖的任务域清单（按 VAP 现有基建的落地成本排序）**：

| 域 | 现状 | 最小落点 | 成本 |
|----|------|----------|------|
| 视频分析/监控 | ✅ 已有 | — | — |
| 媒体生产（字幕/剪辑/配音/竖屏/PPT） | 部分（P2-1/P2-2） | media_gen + tools/ppt | 低 |
| 文档办公（docx/xlsx/pptx 读写、报告导出） | ❌（仅 Markdown→PDF） | python-pptx / openpyxl + 已有 matplotlib | 低 |
| 电商运营 | 仅 skill 文本 | P2-3 上架包 MVP | 中 |
| 知识管理（个人 wiki / 笔记 / 检索） | 部分（kb_rag + ChromaDB） | 复用 OpenKB/ai-memory 的 markdown-wiki 范式 | 低 |
| 研究/调研（多源检索 + 综述） | ❌ | 复用 web_auto + kb_rag | 中 |
| 代码项目 | ❌（且**不应是重点**） | — | — |
| 生活事务（日程/邮件/消息） | ❌（IM 网关全 Mock） | 需真实渠道凭据（L3） | 高 |

**结论**：用户说"不被局限在软件项目"，本质是要求 **VAP 的默认叙事与首页从"视频工具"转为"通用 Agent 工作台"**，而扩展路径应是"**角色 + skill 包**"而非"改内核"。这也是为什么 P0-8（skills 正文注入）与 P1-4（采纳闭环）虽然看起来是 skills 模块的事，实则是**整个扩展性的地基**——skills 只有真正生效，新领域才是"装一个包"而不是"改一次代码"。

### 方向⑥ PPT / 办公

见 N6 报告（已完成）。要点：VAP 现在**无法产出真实 .pptx**；最小可行路径是 `python-pptx` 直出（唯一新依赖，纯 Python），设计规则直接采用 `OfficeCLI` 的硬性版式闸门 + 反 AI-tell 清单；差异化优势是**输入是已跑完的视频分析事实而非主题**。

### 方向⑦ Agent 内核

见 N7 报告（30 条机制 + 10 条优化清单）。**最高性价比 5 条**：
1. 压缩硬化（checkpoint/restore + 决定清单 + 五失效模式单测）
2. 工具结果预算器（>4K 归档 + expand 句柄）
3. 卡死后的**恢复行为**（当前只中断，应注入复盘提示 + 一次恢复预算）
4. XML 任务块（`<action>/<verify>/<done>`）
5. 角色模型分档路由 + 失败升级阶梯

### 方向⑧ 桌面壳

`pi-agent-desktop`（每个健壮性关注点独立文件+测试）、`atlas`（checkpoint + 脱敏）、`Minke`（模块一一对应）、`agent-inspect`（execution tree + CI 轨迹断言）。

### 方向⑨ 工程化

`AI-company`（21 项机器校验不变量 + 停顿检测阈值用真实数据校准）、`bernstein`（HMAC 链式审计）、`agent-skills`（三级评测）、`OpenSandbox`（沙箱协议）。

## 3.2 共性工程优点（跨方向）

1. **"默认只读 + 显式升级"是安全设计的共识**（claude-ads 五道门禁、commerce-agents staged、smart-extraction 的显式旗标、AD-PathFinder 的 `--unsafe-report`）。
2. **"独立验证才算数"是质量设计的共识**（LongHorizon 的 Auditor、darwin/nuwa 的独立评分 agent、no_human 的复现门禁）。
3. **"可回滚是常规路径"**（skillroster Receipt/Undo、darwin 用 `git revert` 而非 `reset --hard`、EvoSkill 的 git 分支即版本）。
4. **"压缩必须可恢复"**（token-optimizer 的 checkpoint/restore、headroom 的可逆回取、caveman 的读侧可恢复压缩）。
5. **"可观测要分级"**（简单模式给结论、专家模式给细节）。

## 3.3 对主项目最有价值的部分

按"用户 6 新方向 × 落地成本"排序：**① 教学化 UI 模式（低改造成本，直接命中用户第一诉求）> ③ skill 采纳闭环（骨架已有，补流程）> ② 用户画像（新增一层，可复用现有 SQLite）> ⑥ PPT（一个纯 Python 依赖）> ④ 图像视频（可选依赖）> ⑤ 电商（合规前置，最慢）**。

## 3.4 适用前提与不应照搬的内容

**适用前提**：VAP 是**桌面单用户、本地优先、Electron+Python** 的产品——任何依赖 Docker/PG/云服务/Node 重栈的方案都要先做技术栈适配。

**不应照搬**：PG+pgvector+Docker（Continuous-Claude 等）、Tauri（Poraland 等）、Node+Chromium 的 PPT 导出（dashi）、TS 办公引擎（genoffice）、验证码/反检测（2Captcha/camofox）、规模类 skill 合集（ECC 181/application-skills 3000+）、跨用户共享存储与 marketplace（二期再议）。

---

# 第四部分：差距分析

## 4.1 功能层

| 能力 | VAP 现状 | 参考最佳实践 | 差距性质 |
|------|----------|--------------|----------|
| Agent 监督（卡死/压缩/预算） | **已建成 + 已接线 + 默认关** | OpenHands / context-mode / LongHorizon | **送达差距**（非能力差距） |
| 写审批 | **已建成 + 默认路径不生效** | commerce-agents / pi-guardrails | **送达差距 + 契约 bug** |
| 记忆分层 | 已建成（`VAP_MEMORY_LAYERED=1` 默认开）+ 未接 legacy | mnemosyne BEAM / mempalace | 送达差距 |
| skills 蒸馏闭环 | 骨架建成（distiller/validator/scoring/spectre/roster）+ 缺用户采纳流 | SkillOpt-Sleep / SkillClaw | **流程差距** |
| 用户画像 | **无**（仅 ChromaDB 偏好雏形） | supermemory / honcho | 能力差距 |
| 教学化/可解释 | **无**（`eli5.py` 孤立 + 零 onboarding） | claude-devtools / mimik | 能力差距 |
| PPT | 仅 `deck.md` + HTML | OfficeCLI / ppt-master | 能力差距 |
| 图像生成 | **无** | locally-uncensored / Pixelle-Video | 能力差距 |
| 电商 | 4 个 skill + 浏览器工具（未在 web 路径注册） | commerce-agents / claude-ads | 能力差距 + 送达差距 |
| 多智能体可见性 | 有 director 无 UI | agent-orchestrator / agent-teams-ai | 体验差距 |

## 4.2 架构层

| 维度 | 差距 |
|------|------|
| 执行器收敛 | **双引擎并存**（legacy/react），工具集与语义不一致——这是架构层最大债务 |
| 工具面统一 | media_gen/web_auto 只在 MCP，Web 主路径拿不到 → 同一产品两套能力 |
| 能力开关呈现 | 6 个 env flag 且多数默认关，用户不可见、不可控 → 应改为 UI 层开关 |
| 治理装配点 | `_install_tool_guard` 只挂在 react；legacy 应至少接入同一 guard（哪怕是 Null 实现） |

## 4.3 工程层

| 维度 | 差距 |
|------|------|
| 接线检查 | 无 CI 门阻止"定义了没人调用"——建议加"新公开符号必须有非测试调用方或显式 `# not-wired` 标注" |
| 宣称一致性 | CHANGELOG/workflow_status 与实测脱节，无自动校验 |
| 测试覆盖盲区 | `test_agent_router_assembly.py` 用自建 registry/mock bus，不验证真实 `/run_stream` 闭环 → 默认路径绕过无测试保护 |
| 评测体系 | 有 pytest 与 Playwright，但缺参考项目那种"三级评测"（结构 CI / 触发路由 / 行为 eval）用于 skills |

## 4.4 性能层

| 维度 | 差距 |
|------|------|
| 上下文压缩 | 有 ContextCompressor 但缺"压缩前 checkpoint / 压缩后 restore / 决定清单"（token-optimizer 范式） |
| 工具结果预算 | 无 >4K 结果归档机制（context-mode / lean-ctx 范式） |
| 检索 | experience 已升 FTS5 混合，但用户偏好仍是 ChromaDB 单级向量 |
| 模型路由 | 无按角色/任务的模型分档（mission-control 三级路由范式） |

## 4.5 安全层

| 维度 | 差距 |
|------|------|
| 默认路径治理 | **最大缺口**：legacy 裸执行 |
| 工具分类准确性 | 4 处误分类（3 写盘 + 1 任意 JS） |
| 沙箱 | 默认关 + Windows 实现有宿主风险 |
| 审批 UI | 裸 JSON + 无中文说明 + 无倒计时 + 单槽 state（并发会互相覆盖） |
| 技能安全 | 有 spectre，但规则库（4 类）远小于 SkillSpector（71 模式/17 类） |

## 4.6 运维层

| 维度 | 差距 |
|------|------|
| 可观测对用户 | 黑匣子面板有（SSE+IPC+过滤+搜索+导出 zip），但内容是开发者视角 → 缺"人话翻译层" |
| 崩溃恢复 | 断路器已有；缺 session tape 可重放（atmosphere 范式） |
| 诊断 | 导出 zip 已有；缺"停止时保留部分轨迹 + 下一轮从实际状态恢复"（LongHorizon 范式） |

## 4.7 测试与真实用户体验差距

- **测试盲区**：默认路径（legacy）无测试保护；审批端到端无真实 `/run_stream` 闭环测试。
- **体验盲区**：无 onboarding、无教学化、首屏定位错位、无用户可查看记忆、无 skill 采纳 UI。
- **未验证项**：Electron 真机启动、全量测试与覆盖率（M5 核查中）。

---

# 第五部分：可迁移 / 半可迁移 / 不建议迁移

## A. 可迁移（低成本、直接落地）

| 项 | 来源 | 预期收益 | 成本 | 风险 | VAP 落点 |
|----|------|----------|------|------|----------|
| eli5 摘要行接线 | `src/core/eli5.py`（已有） | 小白看得懂 | L | 低 | `loop.py` 事件 → agent 页 |
| 双模黑匣子（简单/专家） | claude-devtools | 边用边学 | M | 低 | agent/logs 页 |
| 审批 UI 增强（中文名/后果/倒计时/会话级允许） | commerce-agents、AQBot | 小白敢用 | S | 低 | `page.tsx` |
| 工具分类修正 | 内部 bug | 安全 | S | 低 | `scope_guard.py` |
| dry-run→staged→adopt | SkillOpt-Sleep | 用户放心采纳 | M | 低 | skills 页 + router |
| 触发质量 CI（TF-IDF rank-1） | agent-skills | skill 命中率可测 | S | 低 | CI |
| 工具结果预算器（>4K 归档+expand） | token-optimizer / context-mode | 省 token | M | 低 | tools 层 |
| 压缩前 checkpoint / 后 restore | token-optimizer | 长会话不丢 | M | 中 | supervisor |
| 卡死后恢复预算（非直接中断） | OpenSwarm / LongHorizon | 少中断 | S | 低 | supervisor 消费侧 |
| XML 任务块（action/verify/done） | GSD | 完成标准明确 | S | 低 | turn 提示 |
| python-pptx 直出 + OfficeCLI 规则 | OfficeCLI / ppt-master | 真 .pptx | M | 低 | `tools/ppt/` |
| 本地 TTS / 字幕 / 竖屏 | media_gen（已有）+ LuxTTS | 免费媒体层 | S | 低 | media_gen |
| 电商只读分析 + staged 审批 | commerce-agents / claude-ads | 合规起步 | M | 中 | skills + web_auto |
| 影子观察者（概率唤醒） | pi-shadow-mind | 多 agent 不打扰 | L | 中 | subagent |
| 六列注意力看板 | agent-orchestrator | 多 agent 可见 | M | 低 | webapp |

## B. 半可迁移（需改造或外部条件）

| 项 | 原因 | 前置条件 |
|----|------|----------|
| 用户画像完整实现 | 需确定数据保留策略与隐私边界 | 用户授权（L3） |
| 付费文生图/视频 | 需预算授权 | 用户授权 + Mock 协议先行 |
| ComfyUI 本地生成 | 需 GPU/额外依赖 | 可选依赖 + 缺失降级 |
| 沙箱强化 | Windows Job 语义需复核 | 最小实验 |
| 默认后端切换 | 双引擎语义差异 | P0-2/P0-3/P0-5 先完成 |
| Electron 自动更新 | 需发布 feed + 签名基建 | 基建 |
| MCP 全工具面 | 需白名单与写审批对齐 | 已有双闸，需扩面 |

## C. 不建议迁移

PG/pgvector/Docker · Tauri · 验证码绕过与反检测浏览器 · 重型浏览器逆向 · 纯攻防黑产 · 规模类 skill 合集照搬 · Node+Chromium PPT 导出（dashi）· TS 办公引擎（genoffice）· marketplace/跨用户共享（现阶段）· Rust 重写（wayland/BitFun 等只借思想）。

---

# 第六部分：优先级优化路线图

## 排序原则

**P0 = 把已建成能力送到默认用户路径 + 修安全缺口**（当前投入等于零，且存在静默写盘风险）
**P1 = 用户本轮第一诉求（小白看懂）+ 让 AI 懂用户 + skill 采纳闭环**
**P2 = 扩展域（PPT / 图像视频 / 电商），按"本地优先、免费优先、审批前置"落地**
**P3 = 生态与多智能体可见性**

## P0（必须先做，影响核心路径与安全）

> **P0-1 的唯一前置定义**：`P0-2 → P0-3 → P0-6 → P0-1`。P0-5 与 P0-1 **无依赖**（沙箱另有独立开关 `VAP_SANDBOX_ENABLED`，切后端不启用沙箱），此前多处写作 P0-1 前置属笔误，已统一。P0-4/P0-7 独立。
> **P0-1 的完整交付物必须包含"监督层闸门处理"**——否则按现有文件清单做完，仍达不到其验收标准（`VAP_AGENT_SUPERVISOR` 是第二道默认关的闸门）。

| ID | 做什么 | 为什么 | 涉及模块 | 前置 | 收益 | 风险 | 数据迁移 | 兼容性 | 验证 | 回滚 |
|----|--------|--------|----------|------|------|------|----------|--------|------|------|
| P0-2 | 审批 SSE 契约修复 | react 模式审批弹窗收不到事件，写工具静默挂 60s | `agent.py` `/chat` 契约 / `page.tsx:186` | 无 | 审批真正可用 | L2 | 无 | 前端/契约层 | **两个并发会话**各触发写工具 → 各自弹窗 → 允许/拒绝不串扰 | 单点回退 |
| P0-3 | 工具风险分类修正 + 审批 UI 增强 | 3 个写盘工具 + 任意 JS 被 allow | `scope_guard.py` / `page.tsx` | 无 | 安全 + 小白可用 | L2 | 无 | 分类表可配 | 补分类单测 + 人工验收（依赖 P0-2 才有弹窗） | 配置回退 |
| P0-6 | 监督层半成品修复（压缩反增 bug + 死配置 + 跨轮状态） | `cond.messages` 被丢弃 → 摘要反增 token；`step_timeout_sec` 死配置；Supervisor 每请求重建 | `loop.py` / `supervisor.py` / `agent.py` | 无 | 监督层真实有效 | L2 | 无 | 无 | **loop 级端到端**：压缩后 prompt token 下降 + 摘要随新事件刷新（现有集成用例都显式关压缩，须新写） | feature flag 关 |
| P0-1 | **默认路径接通**（切默认 react + **处理监督层闸门**） | v10.3.0 全部投入当前对默认用户不可见 | `agent.py` / `.env.example` / `page.tsx` / **settings 页（能力开关）** | **P0-2 / P0-3 / P0-6** | 全部已建能力生效 | **L3** | 无 | 保留 legacy 回退 + **旧会话兼容** | 默认启动走 react，supervisor/审批/记忆均生效；**react 打开 legacy 期会话** 与 **切回 legacy 后 react 期会话仍可见** | 改回 env（回滚后抽查 react 期数据可见性） |
| P0-4 | 未接线模块了断（**统一清单**：`set_error_policy` / `set_lock_resolver` / `validator.py` / `SkillAdvisor` / `roster.with_experience` / `cua-service.js`；`eli5.py` 归 P1-1） | 消除"定义了没接线"重复模式 | `registry.py` / `skills/*` / `experience.py` / `desktop/cua-service.js` / `CHANGELOG.md` | 无 | 宣称与实现一致 | L1 | 无 | 接线则补测 | 接线测试 | 移除即可 |
| P0-5 | 沙箱安全复核 | Job 可能杀宿主 | `sandbox.py` / `registry.py` | 无 | 沙箱可安全启用 | **L3** | 无 | 默认保持关 | 最小实验（宿主存活） | 保持关闭 |
| P0-7 | 发布闭环 + 版本同步 + 诚实性修正 | Release asset 未上传完（线上不可下载）；包内 0 skill；版本 3 处待修 | `electron-builder.yml` / `app.py` / `start-desktop.bat` / `CLAUDE.md` / `CHANGELOG.md` / `workflow_status.md` / `ci.yml` | 无 | 用户真能下载到能用的包 | L2 | 无 | 无 | Release assets 长度≥1 且 state=uploaded；解包 `resources/config/skills/` 有 12 目录；**版本号全处一致** | 文档/配置层单点回退 |
| **P0-8** | **skills 正文注入**（用户诉求③的核心断点） | `agent_prompt.py:218` 只给 name+description，SKILL.md 模板与决策表从未到达模型 | `agent_prompt.py` / `skills/{loader,schema,roster}.py` / `agent.py` | 无 | skills 从"名字"变成"真能用" | L2 | 无 | 无 | **roster 命中才注入正文** + token 预算上限 + 命中率/token 回归测试 | 开关关闭即回退到 name+description |

## P1（高价值，服务用户第一诉求）

| ID | 做什么 | 涉及模块 | 收益 | 风险 |
|----|--------|----------|------|------|
| P1-1 | eli5 接线 + 双模黑匣子 | loop / agent 页 / logs 页 | **用户第一诉求** | 低 |
| P1-2 | Onboarding + 首页重构（Agent 优先） | 首页 / 新组件 / 文档 | 小白易用 | 低 |
| P1-3 | 用户画像与个人记忆（含查看/编辑/删除页） | memory 新模块 / 新页面 | AI 懂用户 | 中（隐私，需授权） |
| P1-4 | 用户 skill 采纳闭环（dry-run→staged→adopt + 触发 CI） | skills / skills 页 / CI | skills 沉淀 | 低 |
| P1-5 | Agent 内核深化（压缩硬化 / 结果预算 / 卡死恢复 / 任务块 / 模型分档） | supervisor / tools / turn / route_resolver | 单 agent 更聪明 | 低 |

## P2（扩展域）

| ID | 做什么 | 涉及模块 | 新依赖 | 风险 |
|----|--------|----------|--------|------|
| P2-1 | 真实 .pptx 生成 | 新 `tools/ppt/` + ppt-deck skill | `python-pptx`（纯 Python） | 低 |
| P2-2 | 图像/视频生成（本地免费层 + 付费 Mock 协议） | media_gen | ComfyUI 走可选 | 低 |
| P2-3 | 电商（只读分析 + staged 审批） | skills + web_auto | 无 | 中（合规） |

## P3

- **P3-1** MCP 工具面扩展 + 插件生态形态
- **P3-2** 多智能体可见性（子 agent 卡片 / 上下文成分面板 / 需要你介入优先列）

## 明确不迁
见 §C 不建议迁移。

---

# 第七部分：全栈实施方案

## 前端改造建议

1. **首页重构**：`(app)/page.tsx` 从"视频分析表单"改为 **Agent 对话入口 + 能力卡片网格**（视频分析 / 媒体生成 / PPT / 电商 / 技能库各一张卡）。
2. **agent 页教学化**：消息流中插入**工具调用摘要行**（调用 `eli5.explain_tool_call` 的产物）；顶部提供 **简单模式 / 专家模式** 切换；专家模式显示 phase 链 / token 占用 / 原始 JSON。
3. **审批弹窗增强**：中文工具名 + 后果说明（"将删除当前会话历史" / "将写入 D:\..."）+ 倒计时进度 + "本会话总是允许" + 危险等级配色。
4. **新增页面**：`memory`（AI 记住了我什么，可编辑/删除）、`onboarding`（首启引导）。
5. **设计一致性**：新组件沿用现有设计 token（电光青 / `--font-data`），禁 Inter 默认栈、禁卡中卡、禁 emoji 当图标。

## 后端改造建议

1. **执行器收敛**（中期）：让 legacy 也经过 `ToolRegistry` + waterfall（最小版：只接 scope_guard 审计），或直接切默认 react。
2. **审批契约修复**：`/chat` 在 react 模式返回可执行计划或改用独立事件告知前端订阅 `/run_stream`。
3. **工具面统一**：`media_gen` / `web_auto` 注册进 Web 主路径的 registry（不再只挂 MCP）。
4. **eli5 接线**：在 `loop.py` 的工具结果 phase 调 `explain_tool_call`，通过 SSE 附加字段下发（**注意：解释文本只走 UI，不进 LLM prompt，避免 token 膨胀**）。
5. **用户画像层**：`src/core/memory/profile.py`（双段画像 + ≥3 次晋升 + supersession）；复用现有 SQLite 与 `history_manager`。
6. **skills 采纳流程**：`/api/skills/propose`（dry-run）、`/api/skills/stage`、`/api/skills/adopt`（写盘 + 快照）、`/api/skills/rollback`。
7. **PPT 工具**：`src/core/tools/ppt/`，注册为写工具（走审批）。
8. **媒体 provider 扩展**：`media_gen/_providers.py` 补 `ImageGen` Protocol + Mock。

## 数据层改造建议

- 新增表：`user_profile`（稳定事实）、`user_observations`（带计数的候选项，≥3 晋升）、`skill_proposals`（staged 提案）。
- 复用：`agent_experiences`（FTS5 已就绪）、`working_memory`、`triples`。
- 迁移策略：**只新增表，不重建既有表**；坏了直接删新表回滚。

## 接口层改造建议

- 统一响应信封（success/data/error/meta）——参考 `rules/common/patterns.md` 的 API Response Format。
- 审批相关：`/api/agent/approval/{pin}/decide` 已有；补 `priority` 真实分级与并发多槽（当前单槽 state 会互相覆盖）。
- skills 采纳：新增 4 个端点（propose/stage/adopt/rollback）。

## 配置管理建议

- **把 6 个 env flag 收敛为用户可见的设置项**：agent 页或 settings 页提供"能力开关"（监督层 / 审批 / 沙箱 / 记忆 / roster），带一句话说明与推荐值。
- **默认值策略反转**：从"默认关零回归"改为"默认开 + 可关"，安全相关的（沙箱）例外。
- `.env.example` 保留（开发者用），但不再是唯一入口。

## 日志与监控建议

- **日志分级呈现**：LogStore 增加 `userMessage` 字段（eli5 产物），logs 页默认显示人话，可展开原始。
- **trace 贯穿**：沿用 `x-correlation-id`（参考 agent-orchestrator 的 observability 文档）与现有 trace_id。
- **指标**：每 turn 的 token / 工具结果占比 / 压缩次数 / 卡死触发次数，进 metrics 页。

## 测试策略建议

- **P0 必补**：默认路径端到端（真实 `/run_stream` 闭环，不用 mock bus）、工具分类回归、审批全链路（含超时 deny）。
- **接线门**：CI 加脚本检查新公开符号是否有非测试调用方（或显式 `# not-wired: reason`）。
- **skills 三级评测**：结构 CI（已有 lint）→ 触发路由 TF-IDF rank-1（新增）→ 行为 eval（后续）。
- **E2E**：Playwright 补 onboarding / agent 教学化 / 审批弹窗三条真实路径。

## 部署与运维建议

- 安装包已 697MB，新增依赖需评估体积：`python-pptx` 极轻可接受；ComfyUI/本地模型走**可选下载**而非打包。
- autoUpdater 已就位但无 feed：需规划发布 feed 与签名（L3，需用户决策）。
- 建议在 P0 批次同步修正 `CHANGELOG.md` v10.3.0 的过度声称，并把 workflow_status.md 的 Critic 栏补齐。

## 建议的实施批次与依赖顺序

```
P0-2 ─┐
P0-3 ─┼→ P0-1（默认路径接通）──┐
P0-5 ─┘                        │
P0-4（独立）                    │
                               ├→ P1-1（教学化）─┐
P1-5（独立，可与 P0 并行）      │                  ├→ P1-3（画像）
                               ├→ P1-2（onboarding）│
                               └→ P1-4（skill 采纳）┘
                                                     └→ P2-1 / P2-2 / P2-3 → P3
```

---

# 第八部分：实施授权状态

**当前未授权修改源代码。** 本节仅说明建议优先批准的批次。

| 批次 | 影响范围 | 风险等级 | 预计验证方式 | 是否需单独审批 |
|------|----------|----------|--------------|----------------|
| P0-2 审批契约修复 | `agent.py` + `page.tsx` | L2 | **两个并发会话**各触发写工具，弹窗不串扰 | 否（含在 P0 授权内） |
| P0-3 工具分类 + 审批 UI | `scope_guard.py` + `page.tsx` | L2 | 补分类单测 + 人工验收 | 否 |
| P0-4 未接线了断 | `registry.py` / `skills/*` / `experience.py` / `cua-service.js` / 文档 | L1 | 接线测试或文档一致性 | 否 |
| P0-6 监督层半成品修复 | `loop.py` / `supervisor.py` / `agent.py` | L2 | loop 级端到端：压缩后 token 下降 + 摘要刷新 | 否 |
| P0-5 沙箱复核 | `sandbox.py` | **L3** | 最小实验（宿主存活） | **是**（与 Risks 表一致） |
| **P0-1 默认后端切换** | 全局默认行为 + 监督层闸门 | **L3** | 默认启动全链路 + 旧会话兼容 + 回滚后数据可见 | **是** |
| P0-7 发布闭环 + 版本同步 | `electron-builder.yml` / 文档 / Release | L2 | Release asset 上传完成 + 包内有 12 skill + 版本全一致 | 否 |
| **P0-8 skills 正文注入** | `agent_prompt.py` / `skills/*` — **影响 token 成本** | **L3（成本）** | roster 命中才注入 + token 预算 + 回归测试 | **是** |
| P1-3 用户画像 | 新增数据表 + 新页面 | **L3（隐私）** | 跨会话记忆 + 删除生效 | **是** |
| P2-1 PPT（新依赖） | 新增 `tools/ppt/` + requirements | L2 | 端到端产出可编辑 .pptx + **依赖树/体积实测** | 否（若批准依赖） |
| P2-2 图像生成 | media_gen + 可选依赖 | L2 | 本地生成 1 张 + Mock 覆盖 | 否（ComfyUI 走可选） |
| P2-3 电商（**前置：A4/A9 工具面接线**） | skills + web_auto | **L3（合规）** | 只读路径 + 写操作默认拒绝 | **是** |
| CHANGELOG 更正 + 工作区清理 | 文档 | L1 | 与实测对照 | 否 |

---

# 第九部分：需要我补充的信息

| # | 问题 | 为什么需要 | 不提供的影响 | 能否用临时假设继续 | 建议默认 |
|---|------|------------|--------------|---------------------|----------|
| 1 | 是否授权把 `VAP_AGENT_BACKEND` 默认切为 `react`？ | 决定 P0-1 的形态（切默认 vs 给 legacy 补治理） | P0-1 无法开工 | 可（先修 P0-2/3/5） | 先修再切，保留 legacy 回退 |
| 2 | 用户画像的数据保留策略与隐私边界？ | P1-3 的合规与实现 | 无法设计存储与 UI | 可（本地存储 + 可删除 + 默认不联网） | 全本地 + 用户可查看/编辑/删除 |
| 3 | P1 内部排序：教学化 vs onboarding vs 画像 vs skill 采纳？ | 决定 P1 批次顺序 | 只能按建议排序 | 可 | 教学化 → onboarding → skill 采纳 → 画像 |
| 4 | P2 三项扩展域的优先级与授权边界？ | 决定先做哪个 | 只能按建议排序 | 可 | PPT → 图像视频 → 电商（只读） |
| 5 | 是否批准引入 `python-pptx`？ComfyUI 是否走可选依赖？ | P2-1/P2-2 前提 | 无法开工 | 可 | 批准 python-pptx；ComfyUI 可选 |
| 6 | 是否授权更正 CHANGELOG v10.3.0 的过度声称？ | 诚实性与后续会话可信度 | 后续可能被误导 | 可 | 建议更正 |
| 7 | 电商是否限定为"只读分析"？ | 合规边界 | 无法设计写操作 | 可 | 限定只读 + 写操作默认拒绝 |
| 8 | 是否有 Electron 真机验收条件（GUI 会话）？ | 黑匣子/壳层无法真机验证 | 只能代码级验证 | 可 | 由用户人工验收 |
| 9 | **黑匣子日志默认呈现**（Critic B / Q8）：默认简单模式+可切专家 / 默认全开 / 首次询问？ | 你的原话是"日志全开"，方案改写为"默认人话层" | 首屏体验与你的预期可能不符 | 可 | **默认简单模式 + 一键切专家**（推荐） |
| 10 | **P1-5 是否需要展开为完整批次**（Critic B / Q4）？ | 它承载你第一诉求"每个智能体深度优化"，当前只有一行 | 无法据此开工 | 可 | 建议展开 + 补 subagent 角色逐个分析 |
| 11 | **P0-8 skills 正文注入的 token 预算上限**？ | 决定注入策略与成本 | 无法实现与验收 | 可 | roster 命中才注入 + 单次注入上限（建议 ≤2000 token）+ 超限截断 |

---

# 附录 A：PPT 设计规则与 QA 门禁（P2-1 的实施依据，取自 `OfficeCLI/skills/officecli-pptx/SKILL.md`）

> Critic B 指出此前 P2-1 以"见 §全栈方案"指向不存在的内容——本节补上，使 P2-1 可独立执行。

**画布与栅格**
- 16:9 = **33.87 × 19.05 cm**（python-pptx：`Inches(13.333) × Inches(7.5)`）
- 页边距 ≥ **1.27cm**；块间距全篇统一取 **0.76 或 1.27cm**（不许混用）；留白 ≥ **20%**
- N 卡栅格：`usable = 33.87 − 2×margin − (N−1)×gap；col = usable / N`。**坐标不许手拍**

**字号阶梯（硬下限，违反任一条 = 未完成）**
| 元素 | 最小 | 常用 |
|------|------|------|
| 页标题 | **≥36pt 粗** | 36–44pt |
| 章节/副标题 | ≥20pt | 20–24pt |
| 正文 | **≥18pt** | 18–22pt |
| 注释/轴标签 | ≥10pt muted | 10–12pt |
- **标题 ≥ 2× 正文**；正文左对齐，仅标题与大数字居中
- 每页至少 1 个 **64px+ 的超级数字**；封面 H0 48–160px；页内最大/最小字号比 **≥5×**；正文行高 1.8
- **卡片放不下就减卡，不许缩字号**

**字体与配色**
- 最多 **2 个字族**（标题 + 正文）；一张 deck 一套调色板：主色 **60–70% 权重** + 辅色 + 强调色（accent 同页 1–2 处）
- 现成色板（OfficeCLI 原文）：Midnight Executive `1E2761/CADCFC/FFFFFF/333333/8899BB` · Forest & Moss `2C5F2D/97BC62/F5F5F5/2D2D2D/6B8E6B` · Warm Terracotta `B85042/E7E8D1/A7BEAE/3D2B2B/8C7B75` · Charcoal Minimal `36454F/F2F2F2/212121/333333/7A8A94`
- **深底上的正文必须 `FFFFFF` 或亮度 >80%**（中灰 `6B7B8D` 投影会消失）
- VAP 自有 token 应与 webapp 同源：主信号电光青、数字用 `--font-data`

**图表决策表**
| 数据形态 | 图表 |
|----------|------|
| 类别对比 | 柱/条（≥6 类走横向） |
| 时间序列 1–3 条 | 折线 |
| 占比 2–5 片 | 饼/环 |
| 相关 | 散点 |
| **单个 KPI** | **大字而非图表** |
| >3 series 且 >8 类 | 拆两图或改表格 |
- KPI 卡：60pt 数字在 9.78cm 卡内约容 5 字符（`$84.2`）；`$84.2M` 应拆成大字 + 副标签，**不许缩字号**
- 连接线必须有箭头（`tailEnd=triangle`），否则流程方向丢失

**反 AI-tell 清单（OfficeCLI 原文）**
- ❌ **标题下的装饰横线** —— "A stripe / rule below a heading is **the single most common AI-slide tell**"
- ❌ 圆角卡片 + 左侧色条
- ❌ emoji 当图标
- ❌ **纯 bullet 页** —— "A bullet-only deck is interchangeable with a Word doc"
- ✅ 每页必须有一个**承载信息的非文字视觉**（且不为凑数造数据）
- ✅ 封面必须有：标题 + 副题 + 讲者/客户 + 日期 + 品牌条；每张内容页带演讲者备注
- ✅ 标题写**结论**（"Revenue grew 18% YoY"）而非话题（"Revenue Overview"）

**内容密度合同**（取自 `ppt-agent-skills/planning_validator.py` DENSITY_DEFAULTS，可直接抄成 VAP 校验常量）
| density | max_cards | max_charts | min_body_font | max_lines/card |
|---------|-----------|------------|---------------|----------------|
| low | 2 | 1 | 24 | 3 |
| mid_low | 3 | 1 | 20 | 4 |
| medium | 4 | 2 | 18 | 5 |
| high | 6 | 2 | 16 | 4 |
| dashboard | 8 | 4 | 14 | 3 |
- 页面填充率：封面 40–55% / 章节 25–40% / 内容 60–75% / 数据页 70–80% / 结尾 35–50%
- 每页只有一个视觉锚点；禁连续 3 页 high/dashboard

**QA 门禁（交付前必跑，最多 3 轮修复）**
1. **溢出检测**：逐 shape 估算文本边界 vs shape 框
2. **占位符残留**：grep `xxxx` / `lorem` / `TODO` / 无关默认文案
3. **对比度校验**：深底正文必须亮色
4. **逐页看图**：转 PDF/PNG 后按**对抗式**审查（"假设一定有问题"）
- OfficeCLI 原文："**Gate 2 — overflow / format / structure… If it lists any issue → REJECT, fix, re-run until clean**"；"Fix-verify (mandatory, max 3 cycles)"

**版式骨架（10 页评审范例）**：封面 · 议程 · 3×KPI · 分隔页 · 图表 · 图表 · 分隔页 · 流程 · 时间线 · 收尾（**分隔页必须在章节内容之前**）

---

# 附录 B：v3 的产物与可复现性说明

> Critic B 指出"单一状态源"应可追溯。本节澄清本轮所有产物的位置与时效。

| 产物 | 位置 | 时效性 |
|------|------|--------|
| 本指南 | `Video-Analysis-Pro-python/参考的结果计划指南.md` | 持久（已落盘于仓库根） |
| 33 批扫描的完整卡片（919 条记录 / 912 唯一项目，1.2MB JSON） | `%TEMP%\claude\...\<session>\tasks\wag3524es.output` | **会话临时文件，会被清理** |
| Workflow 逐代理日志 | 同会话目录 `subagents/workflows/wf_*/journal.jsonl` | 同上 |
| 主项目核查 / 方向精读的完整报告 | 各子代理的会话临时输出文件 | 同上 |
| 前任侦察产物 | `.agent-scratch/recon/{evidence_check.md, recon_synthesis_summary.md}`（各 39/64 行，**是 9-8 前任 11 代理侦察，901 目录/865 项目，非本轮**） | 持久（仓库内） |
| 前任记忆/技能侦察 | `参考项目/vap_scout_memory.md`（245 行，18 项目深读） | 持久（参考目录内） |

**复现方法**：本指南的所有 DIRECT 结论都可用文中给出的 `file:line` 或命令重新得到，不依赖上述临时文件。**临时文件仅用于承载原始卡片明细，若需长期留存应另行导出。**

**参考目录计数时点**：本指南写作期间实测 **988** 个条目（2026-09-11 约 03:30）；写作完成后复查为 **1000** 个——**该目录正在被实时扩充**，任何计数都应标注时点。

**⚠️ 一个易混淆项**：`C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\ref_scan\` 下的 `C*.md` / `G01-G32.md` / `D*.md` / `U*.md` **不是本任务的产物**（经查其内容为某 Rust LLM 网关项目的 `cargo test` / `axum` / 流式协议审查报告，由另一并发会话写入）。勿与本轮扫描混淆。

---

# 附录 C：逐能力默认值表（Critic B 指出"默认开"策略一刀切，此处细化）

> 原则：**"UI 默认可见"与"运行时默认启用"是两件事**。凡涉及安全 / 隐私 / 费用 / 不可逆的，运行时默认关闭并走审批；其余默认开启，让用户不必改 env 就能用到。

| 能力 | UI 默认可见 | 运行时默认 | 开关位置 | 回退 |
|------|:-----------:|:----------:|----------|------|
| Agent 对话（react 引擎） | ✅ | **开**（P0-1 后） | 用户设置页（原 env） | `VAP_AGENT_BACKEND=legacy` |
| 监督层（卡死/压缩/预算） | ✅ 状态可见 | **开** | 用户设置页 | `VAP_AGENT_SUPERVISOR=false` |
| 写审批 | ✅ 弹窗 | **开**（默认 ask） | 用户设置页 | `VAP_ALLOW_WRITE_*` |
| 工具沙箱 | ✅ | **关**（P0-5 复核前） | 设置页（高级） | 保持关即可 |
| 分层记忆 | ✅ 可查看/编辑/删除 | **开** | 设置页 | `VAP_MEMORY_LAYERED=0` |
| **skills 正文注入** | ✅ | **开**（P0-8 后，**仅 roster 命中时**，带 token 预算） | 设置页 | 开关关闭 → 退回 name+description |
| skills 自动蒸馏 | ✅ 提案可见 | **关提案直接落盘**（dry-run 默认；**采纳必须用户点**） | 设置页 | 不做即可 |
| 用户画像 | ✅ 可查看/编辑/删除 | **关**（需 P1-3 授权） | 设置页 | 一键清空 |
| 黑匣子日志 | ✅ | **简单模式默认；专家视图一键切**（**见 §9 第 12 问待确认**） | 日志页页内切换 | 切回简单模式 |
| 图像/视频生成（本地） | ✅ | 开（依赖已装的部分）；ComfyUI 等可选依赖缺失时**显示"未安装"而非静默降级** | 设置页 | 不启用 |
| 付费生成 API | ✅（标注"需 Key"） | **关**（`VAP_MEDIA_BUDGET=0`） | 设置页（需显式填 Key + 预算） | 保持关 |
| 电商写操作 | ✅ | **拒**（deny；只读默认开） | 设置页 | 保持拒 |
| IM 网关 | ✅ | **关**（全 Mock，需真实凭据才启用） | 设置页 | 保持关 |
| 远程访问 tunnel | ✅ | **关** | 设置页 | 保持关 |
| MCP server | ✅ | **关**（需显式启动进程） | 启动参数 | 不启动 |
| 自动更新 | ✅ 状态可见 | 开（**需先配 feed**，否则显示"未配置"） | 设置页 | — |

---

# 附录 D：独立审查记录（Critic A / B / C on 本指南）

> 按用户原始指令"启动一个独立审查线程，从需求完整性、逻辑正确性、边界情况、代码质量、测试覆盖和实际运行结果六个方面验证"执行。三个 Critic 均**不继承主控推理**，只认自己动手得到的证据。

| Critic | 侧重 | 结论 | 已采纳的修正 |
|--------|------|------|--------------|
| **A** | 事实准确性抽查（14 条 DIRECT 断言独立复现） | **13 条完全确认，1 条措辞失准**；总体可信度**高**。7 条点名高风险断言（默认 legacy / 裸执行 / eli5 / SKILL.md 未注入 / 压缩丢弃 cond.messages / electron-builder 排除 SKILL.md / app.py 10.2.0）全部在源码与**构建产物**层面证实（打包缺 skill 一项还取得了 `win-unpacked/resources/config/` 无 skills 目录的物证） | ① eli5"全仓零引用"→"**零生产调用方**（测试有 3 处 import）"；② Validation Matrix 的规模计数命令改为 `grep -l "APIRouter("`（原命令会算上 `__init__.py` 得 17）；③ 未验证项（GitHub Release 外部状态）保持 UNVERIFIED |
| **B** | 需求完整性（9 项输出要求 + 6 原则 + 16 禁止项 + 6 新方向） | **覆盖度 85/100，可执行性 75/100**。指出三处硬伤：①**方向⑤"不被局限在软件项目"整段缺失** ②数字口径不一致 ③**P2-1 的设计规则引用悬空**（"见 §全栈方案"指向不存在的内容）。另有 Q4（P1-5 无批次）/ Q5-Q6（第五六部分字段不全）/ Q8（"日志全开"被改写未请确认）/ Q9（"默认开"一刀切）/ Q10（血统计数 42 应为 41） | ① **新增"方向⑤ 任务域边界分析"整节**；② 追加 **附录 A（PPT 设计规则与 QA 门禁）**，使 P2-1 可独立执行；③ 追加 **附录 B（产物与可复现性说明）**；④ 追加 **附录 C（逐能力默认值表）**；⑤ 血统计数改 41；⑥ 高价值计数补全 14 类；⑦ 全量测试行改为实跑结果；⑧ Next Gate 增第 12 问（黑匣子默认呈现） |
| **C** | 逻辑一致性与边界情况 | **A/B/C 三大类共 20+ 项发现**。核心：①**A8（skills 正文注入）标 P0 却无施工批次**（孤儿问题）②**P0-6/P0-7 缺席 §六路线图表与 §八授权表**③**P0-1 前置条件三处互相矛盾**（P0-2/3 vs P0-2/3/5 vs P0-2/3/6）④**风险等级自相矛盾**（沙箱 L3/是 vs L2/否；画像 L2 vs L3；"L3 三项"名单错漏电商）⑤**"16 vs 32"表述与自身证据冲突**（两套 Web 路径其实都是 16）⑥多处边界遗漏（legacy→react 会话兼容、回滚后数据可见性、并发审批串扰、跨平台、空 skills/无凭据首启） | ① **新增 P0-8 批次**（skills 正文注入，含 token 预算与验收）；② §六 P0 表与 §八授权表**补入 P0-6/P0-7/P0-8**；③ **统一 P0-1 前置为 `P0-2→P0-3→P0-6→P0-1`**，并写明"P0-1 交付物必须含监督层闸门处理"；④ 修风险等级（沙箱→L3、画像→L3、L3 名单加电商）；⑤ 改写"16 vs 32"为"工具集同为 16，差异在治理层"；⑥ **P0-1/P0-2/P0-3 验收补并发与兼容用例**；⑦ P2-3 标注前置 A4/A9 |
| **B（误报，未采纳）** | — | Critic B 称本轮扫描产物在 `Desktop/ref_scan/G01-G32.md`，并据此判定"候选清单不可定位" | **经核实该目录属另一个并发会话**（内容为某 Rust LLM 网关的 `cargo test`/`axum`/流式协议审查报告，02:40–05:14 写入）。本轮真实产物是会话语义目录下的 `wag3524es.output`（1.2MB，31 批，已验证可解析）。**未采纳**；但采纳了其衍生建议——在附录 B 中如实说明该文件为**会话临时文件、会被清理**，并声明指南的 DIRECT 结论均可由文中 `file:line` 独立复现，不依赖临时文件 |

**Critic 一致确认的部分（无需返工）**：Evidence Ledger 抽查全对；P0 批次含目标/文件/回滚/验证且准确；依赖图无循环依赖；988=987+1；86 端点分项合计；62/57/174/82 测试数；P0"Deliver, not Build"的诊断锋利度。

---

**（本文件为 v3，落盘于 2026-09-11。等待用户确认实施范围后进入 Phase B。不自动进入实施。）**
