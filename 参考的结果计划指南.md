# 参考的结果计划指南（v2 — 以 v10.2.0 真实现状为基准）

> 本文件由 SynthesisArchitect 综合分析子代理在 ANALYSIS_ONLY 阶段产出（2026-09-08）。
> 输入：`.agent-scratch/recon/recon_synthesis_summary.md`（11 簇收敛）+ `.agent-scratch/recon/evidence_check.md`（DIRECT 证据核查清单）+ 主项目 v10.2.0 关键源码直读（loop.py / session.py / turn.py / prompt_guard.py / registry.py / waterfall.py / error_policy.py / provider_router_client.py / experience.py / skill_generator.py / agent.py router / app.py / ci.yml / cipher.py / director.py / inprocess.py / desktop main.js + runtime-controller.js / skills loader.py / credentials key.py / remote manager.py + adapters）。
> 约束：只读，不改源码；只创建本指南一份文件；证据区分 DIRECT / INFERENCE / UNVERIFIED；不自动进入实施。
> v1 已归档至 `docs/archive/参考的结果计划指南.md`，其中多数 P0 已于 v10.0-v10.2 闭环，本 v2 不重复列为待办，仅在 Decisions 注明"已闭环"。

---

## Task Contract

- **原始目标**：综合 9 份参考侦察（11 簇收敛摘要）+ 主项目 v10.2.0 真实现状，产出对标差距分析与优先级路线图，服务用户 5 条原始意图（①Agent 更完美 ②小白易用 ③AI 懂用户+skills 沉淀 ④真干活扩领域 ⑤扩展延伸性）。
- **当前阶段**：ANALYSIS_ONLY（只读分析，不改源码）。
- **成功标准**：六阶段分析（跨簇交叉验证 / 六层差距 / 可迁移分类 / P0-P3 路线图 / Red Team / 全栈方案）+ 9 节模板，证据可追溯，孤证降权，以 v10.2.0 实际代码为准，P0 短板（监督层/写审批接线）优先于扩展。
- **停止条件**：指南落盘后回传 ≤500 字摘要，不自动进入实施。

---

## Task Graph

| ID | Agent | Goal | Dependencies | Deliverable | Validation | Status |
|----|-------|------|--------------|-------------|------------|--------|
| R1 | Recon-A..K | 11 簇参考项目侦察（901 目录→865 项目） | 无 | `.agent-scratch/recon/recon_*.md`（每簇 Top8 + 3-5 行卡片） | README/清单 file:line 证据 | DONE |
| R2 | Recon-Synthesis | 11 簇收敛为主摘要 | R1 | `recon_synthesis_summary.md`（跨簇收敛表 + 用户战略意图解读） | 多簇独立 Top8 重合 | DONE |
| R3 | EvidenceCheck | v10.2.0 真现状核查 | 无 | `.agent-scratch/recon/evidence_check.md`（A 已闭环 / B 真缺口） | 源码直读（grep/Read） | DONE |
| T1 | main-mapper | 主项目基线文件直读 | 无 | 本指南引用的 v10.2.0 现状（loop/session/registry/desktop 等） | file:line 证据 | DONE |
| T2 | SynthesisArchitect | 综合分析 + 产出 v2 指南 | R2+R3+T1 | 本指南（参考的结果计划指南.md v2） | 输入全部 Read 确认 + 证据表 | DONE |
| T3 | team-lead/用户 | 审阅指南、确认排序 | T2 | 决策确认（见 Next Gate） | 待用户 | PENDING |

---

## Project Inventory

### 主项目识别摘要（v10.2.0，全部 DIRECT）

TingFeng Hermes v10.2.0 = Electron 桌面壳 + Python FastAPI 后端（**16 router**，app.py:158-176 实测）+ Next.js 16 前端（**16 页**，webapp/src/app 下 page.tsx 实测，玻璃拟态 + v10.2 反 AI 美学 电光青）+ DSH 式 async Agent 框架。

**Agent 框架核心**（DIRECT，已直读）：
- `src/core/agent/loop.py` ReactLoopAgent（280 行）：`run_turn` 主循环 166-260 行 = 单 while 用 `turn.step < max_steps`（默认 8）限步；`AgentConfig.step_timeout_sec` **字段从未被消费**（全仓 grep 仅 loop.py:46 定义行，无 `wait_for`/使用点，`except asyncio.TimeoutError` 在无 wait_for 包裹下不可触发）；**无 stuck/卡死检测、无上下文压缩、无成本预算、无 supervisor**。
- `session.py`：SessionEvent append-only + `derive_messages()` 投影 + SessionStore（SQLite WAL 持久化，`config/agent_sessions.db` 已 gitignore）；`search_sessions` 用 `LIKE %q%`（无 FTS）。崩溃恢复入口 `load_session`。
- `turn.py`：15 phase 事件链 + `TurnHooks`（每 phase 多钩子）+ `TurnStopReason`（STOP/MAX_STEPS/ERROR/CANCELLED）。Phase 序列无 condensation/approval 专用 phase。
- `prompt_guard.py`：5 类 19 规则 + base64 启发式 + 零宽字符 + system-spoof；`guard_messages` **已在 run_turn 内调用**（loop.py:179）。
- `provider_router_client.py`：`ProviderRouterClient.stream` async 包装 `post_nvidia`（to_thread 防阻塞 + tools 透传到 `build_nvidia_payload`），补了 loop docstring 曾提及的缺口。

**工具系统**（DIRECT）：
- `definition.py` `to_llm_schema` → OpenAI 兼容嵌套 `{type:"function", function:{name,description,parameters}}`（真实 NVIDIA 200）。
- `registry.py`：`set_approval_fn`（ApprovalFn（call, Ask）→ bool）、`set_sandbox`、`set_error_policy`、`set_lock_resolver` **已定义，但全仓 grep 无任何调用方**（`.set_approval_fn(` / `.set_sandbox(` 零命中，仅定义行）；`execute` 直接走 waterfall，Ask 信号在 `_approval is None` 时抛 `ToolNeedsApproval` 归一成 error。
- `waterfall.py`：pre_execute 支持 Allow/Deny/Ask/Replace/Timeout 五信号；post_execute/result 钩子。
- `error_policy.py`：TRANSIENT/INVALID_INPUT/FATAL/UNKNOWN 分级 + 指数退避重试 + FATAL 标记不可用。
- `parallel.py`：ParallelExecutor 读锁共享/写锁独占。
- `adapter.py`：17 个 legacy 工具桥接（get_video_meta/search_web/kb_search/VLM 等）。

**记忆/skills**（DIRECT）：
- `experience.py`：ExperienceExtractor → ExperienceStore（SQLite，`config/agent_experiences.db`）→ SkillAdvisor。**未接入主控**（grep: 除 experience.py 自身外 src/ 零引用）。
- `skill_generator.py`：单向规则模板（parking/face/fire/vehicle 四场景）→ SKILL.md；无验证/无棘轮/无评分。
- `config/skills/`：仅 8 内置 skill（builtin-video-summary / epic-infographics / funclip-clip / luxtts-voiceover / surveillance×4）。
- `src/skills/loader.py`：frontmatter 解析 + enabled 状态（config/skills_state.json）。

**Web/部署**（DIRECT）：
- `app.py`：**16 router 装配**（app.py:158-176：health/analyze/metrics/media/models/agent/config/providers/logs/decisions/skills/batch/surveillance/im_gateway/remote/requests）+ lifespan：JSON 结构化日志（回退 basicConfig）、SessionStore 单例、install_log_bridge（/api/logs 环形缓冲+SSE）、**GCGuard 已接入**（`install_gc_guard`，app.py:107-124）。
- `ci.yml`：**已去 `|| true`** + `--cov-fail-under=80`（ci.yml:42-45）+ e2e job（Playwright 6 spec）+ build-windows job。**注：e2e "23 passed" 来自 CHANGELOG 声称，本会话未复跑无运行留档 → 证据类型为 UNVERIFIED**。
- `.gitignore`：im_gateway.db / agent_sessions.db / agent_experiences.db（*+shm+wal）均已忽略。
- `cipher.py`：`cryptography` 缺失降级 XOR 流密码 + HMAC（**非真 AES**，需真实凭据时才升级）。
- IM adapters（wechat/telegram/discord）全 Mock（telegram.py 用内存队列 + inject_inbound，零真实 API）。
- remote adapters（tailscale-serve/direct/cloudflare）全 Mock（tailscale_serve.py 状态机，无 real subprocess）。
- `desktop/main.js`：单实例锁 + 托盘 + before-quit kill；**无 autoUpdater**（desktop/package.json 无 electron-updater 依赖）。
- `desktop/runtime-controller.js`：spawn serve.py + stdout 端口就绪信号 + 健康探活 + loadURL；**无崩溃断路器/无黑匣子实时面板/无诊断导出/无启动屏**。
- `logic.py`：1591 行视频分析巨石（Phase1 抽帧/转录/检测 → Phase2 LLM → Phase3 媒体生成）。
- CHANGELOG v10.2.0：Agent 主线闭环 + OpenAI 兼容 schema + 断线续传基座；`APP_VERSION="10.2.0"` 五位置同步。

### 参考项目候选清单（跨簇收敛 Top 20，含簇 A-K 命中）

> 依据 recon_synthesis_summary.md 第 2 节"≥2 簇独立 Top8 重合"收敛；本 v2 只采纳跨簇共识，不留单簇 Top8 孤证项目。

| # | 项目 | 簇命中 | 核心可提炼机制 | 落点 |
|---|------|--------|----------------|------|
| 1 | OpenHands | F/I | 卡死检测 stuck.py / 事件流总线 / 确认模式 | P0-1 监督层 |
| 2 | context-mode | D/G/J | 上下文压缩 / 分块摘要 / 会话寿命 | P0-1 压缩 |
| 3 | LongHorizon-Harness | I | 长时任务 harness / task_state 蒸馏 | P0-1 监督扩展 |
| 4 | commerce-agents | 电商簇 | 写操作 staged-approval / shopping+merchant 双角色 | P0-2 审批 / P2-2 电商 skill |
| 5 | pi-guardrails | 安全/桌面 | Agent 行为护栏 / 最小权限 | P0-2 scope_guard |
| 6 | pigeon | 安全 | 最小权限工具分发 / 危险操作拦截 | P0-2 scope_guard |
| 7 | okf-agent-memory | D/G/J | 工作记忆 + 手动/自动记忆 + 召回 | P1-1 记忆分层 |
| 8 | claude-mem | D/G/J | 用户偏好 / 长时记忆分层 | P1-1 记忆分层 |
| 9 | plur | D | 分层记忆 + 长期长期关系抽取 | P1-1 记忆分层 |
| 10 | graphiti | J | 时序知识图谱 / 实体关系更新 | P1-1 时序/图记忆 |
| 11 | nuwa-skill | D | 技能蒸馏 / 三重验证 / 保真度评分卡 | P1-2 skills 闭环 |
| 12 | EvoSkill / SkillOpt / AREX-Skill | J | 技能进化优化 / 选择性 skill 分配 / 技能验证 | P1-2 skills 闭环 |
| 13 | SkillSpector | J | 技能安全扫描（frontmatter/指令注入） | P1-2 安全扫描 |
| 14 | skills-best-practices | MCP/J | SKILL.md 结构规范 / progressive disclosure | P1-2 结构 + 按需分配 |
| 15 | nodeterm | I | 黑匣子日志面板（实时流+过滤+搜索+Copy/Clear） | P1-3 Electron |
| 16 | OpenDesign | I | 崩溃断路器 / crash-diagnostics | P1-3 Electron |
| 17 | DSH 全家（deepseek-harness） | D/I/F | 四段式 runtime 托管 / 三层记忆 dsh-mnemon / 子代理 director | P1-3 Electron / P1-1 记忆 |
| 18 | clawdeck / ClawX | D | OpenClaw 桌面壳 / 技能集成托盘 | P1-3 Electron |
| 19 | video-use / VideoCaptioner / funclip | K/K/G | 对话式剪辑 / 可插拔 ASR / 三形态交付 | P2-1 视频工具集 |
| 20 | MoneyPrinterTurbo | H/B/G | 主题→成片全自动流水线 / Web+CLI+Docker 三形态 | P2-1 视频工具集 |
| 21 | OpenMontage / PreenCut | I/I | agent-first pipeline / 7 维评分 + 质量门 / 智能剪辑 | P2-1 视频工具集 |
| 22 | browser-use / chrome-cdp / TuriX-CUA | MCP/J | 浏览器自动化 / CDP / 桌面 Computer Use | P2-3 GUI 自动化 |
| 23 | dashi-ppt / claude-ads | 电商簇 | PPT 版式组合 / 广告多平台分发 | P2-2 营销 skills |
| 24 | wayland | J/K | 本地 Rust 桌面 agent 中枢 + 2100 skills | P3 生态参考（Rust 栈不迁代码） |
| 25 | cc-switch / chops | C | 多 Agent provider 配置管理桌面 | 已参照（v10.0 providers 页） |

### 排除目录及原因

| 排除项 | 原因 |
|--------|------|
| PostgreSQL + pgvector + Docker | 桌面单用户产品不匹配，坚持 SQLite + ChromaDB 本地优先；7 簇报告均建议本地优先 |
| Tauri 迁移 | 丢失 Node 托管 Python 便利，VAP 已选 Electron，迁移成本 > 收益 |
| 验证码 bypass（geetest/hcaptcha 系） | 法律灰色边界 + GPL-3.0 企业用户定位不符 |
| 纯攻防黑产（AutoSploit/arsenal/hackingtool/pentagi 进攻面） | 定位不符；只借编排/治理/沙箱/记忆/技能通用抽象，不迁进攻代码 |
| 重型浏览器逆向 | 高合规风险 + 维护成本，浏览器自动化用 browser-use/CDP 合法路径 |
| ECC 181 skills 规模照搬 | YAGNI，规模是结果非起点，先 8→20 高质量 skill |
| baoyu-skills 全量安装 | README 自警上下文开销，应按需选 |
| open-mercato/genoffice 完整产品移植 | 定位不同（ERP/Office 套件 vs Agent 平台），只借架构思想 |

### 高价值参考项目（供精读，优先级排序）

1. **OpenHands stuck.py + context-mode**（P0-1，监督层参照）
2. **commerce-agents + pi-guardrails + pigeon**（P0-2，写审批 + scope_guard 参照）
3. **okf-agent-memory + claude-mem + graphiti**（P1-1，记忆分层 + 时序图谱参照）
4. **nuwa-skill + SkillSpector + skills-best-practices**（P1-2，蒸馏/安全扫描/按需分配参照）
5. **nodeterm + OpenDesign + DSH desktop**（P1-3，Electron 壳参照）
6. **MoneyPrinterTurbo + VideoCaptioner + PreenCut**（P2-1，视频工具集参照）
7. **browser-use + TuriX-CUA**（P2-3，GUI 自动化参照）
8. **dashi-ppt + claude-ads**（P2-2，营销 skills 参照）

---

## Evidence Ledger

> 每条 claim 附 DIRECT（本会话已直读源码/配置）/ INFERENCE（间接推断）/ UNVERIFIED（缺环境未验证）+ 置信度。

| Claim | Evidence | Type | Confidence |
|-------|----------|------|------------|
| CI `\|\| true` 已去 + `--cov-fail-under=80` | ci.yml:42-45（本会话直读） | DIRECT | 高 |
| GCGuard 已接入 app.py lifespan | app.py:107-124（install_gc_guard + VAP_GC_THRESHOLD_MB） | DIRECT | 高 |
| ReactLoopAgent 已接 prod | agent.py `_get_backend`（header>env>默认 legacy）+ `_build_react_agent` 构造 ReactLoopAgent+SessionStore；`/api/agent/sessions` API | DIRECT | 高 |
| ProviderRouterClient 已实现 | provider_router_client.py 全文件（async stream 包装 post_nvidia） | DIRECT | 高 |
| OpenAI 兼容 to_llm_schema | definition.py:40-60 嵌套 `{type:function,...}`（备注真实 NVIDIA 200） | DIRECT | 高 |
| PromptGuard 已在 run_turn 内调用 | loop.py:179 `messages = guard_messages(messages)` | DIRECT | 高 |
| config/im_gateway.db 已 gitignore | .gitignore:69-77（im_gateway/agent_sessions/agent_experiences/cred_audit 全列） | DIRECT | 高 |
| **监督层零实现** | loop.py 166-260 主循环仅 `while turn.step < max_steps` + 单个 `except asyncio.TimeoutError`（205 行）；`AgentConfig.step_timeout_sec`（loop.py:46）**全仓 grep 仅定义行、从未被消费**（无 `asyncio.wait_for` 使用点），即 timeout 字段从未生效；无 stuck/压缩/预算/supervisor | DIRECT（字段未被消费为源码直读；"timeout 从未生效"为其必然推断） | 高 |
| **写审批/sandbox 零调用方** | grep `\.set_approval_fn(` / `\.set_sandbox(` → 仅 registry.py 定义行，src/ 零调用；loop.py `self.tools.execute(call)` 直连 | DIRECT | 高 |
| **记忆扁平无混合检索** | experience.py 仅 LIKE 模糊 + quality_score 降序；无 FTS5/热层/时序图；grep src/ 除 experience.py 外零引用 | DIRECT | 高 |
| **skills 单向** | skill_generator.py 全文件规则模板（parking/face/fire/vehicle）；config/skills/ 仅 8 目录 | DIRECT | 高 |
| **Electron 无 autoUpdater** | desktop/package.json devDependencies 仅 electron/electron-builder；main.js grep 无 autoUpdater（node_modules 内 app-builder-lib 引用不算应用代码） | DIRECT | 高 |
| **Electron 无崩溃断路器/黑匣子面板/诊断导出/启动屏** | runtime-controller.js 全文件：spawn+stdout READY_RE+waitForHealth+loadURL，无状态机重试/断路器；main.js 无 log-store/crash-diagnostics | DIRECT | 高 |
| IM 三 adapter 全 Mock | telegram.py 内存队列 + inject_inbound；wechat/discord 同构 | DIRECT | 高 |
| remote 三 adapter 全 Mock | tailscale_serve.py 状态机（prepare/start/stop 模拟，无 subprocess）；manager.py"Adapter 全 Mock"docstring | DIRECT | 高 |
| GatewayCipher 降级 XOR（非真 AES） | cipher.py `_CRYPTOGRAPHY_AVAILABLE` 探测 + `_encrypt_fallback` XOR+HMAC；真实凭据才需升级 | DIRECT | 高 |
| config/skills 仅 8 内置 | Glob config/skills/*/SKILL.md → 8 命中 | DIRECT | 高 |
| 视频分析巨石 logic.py 1591 行 | wc -l src/core/logic.py → 1591 | DIRECT | 高 |
| 前端 **16 页路由** + e2e 6 spec | Glob webapp/src/app/page.tsx → 16 个（app 根 + (app) 下 15）；webapp/e2e/ 6 spec 文件存在；**"23 passed" 来自 CHANGELOG 声称、无本会话运行留档 → e2e 通过数标记 UNVERIFIED** | DIRECT（页面/文件计数）/ UNVERIFIED（运行数） | 高 / — |
| 记忆层"热层/混合评分/时序图"是跨簇共识 | recon_synthesis_summary.md 第 2 节（context-mode/memory/okf/plur/remnic 多簇命中） | DIRECT | 高 |
| skills"蒸馏→验证→棘轮"是跨簇共识 | recon_synthesis_summary.md 第 2 节（nuwa/EvoSkill/SkillOpt/AREX 多簇命中） | DIRECT | 高 |
| 写审批是跨簇共识 | recon_synthesis_summary.md 第 2 节 + evidence_check.md（commerce-agents/agent-guardrails/pi-guardrails/pigeon） | DIRECT | 高 |
| 参考目录战略意图三维 | recon_synthesis_summary.md 第 3 节（用户原话归纳：底座件/视频生态延伸/新领域预研） | INFERENCE | 中高 |
| 本地优先不引 PG | 多簇报告一致建议 SQLite 本地优先 | INFERENCE | 中高 |
| 视频生成工具付费 API 需求 | 参考视频生成项目多接付费生成 API；主项目守付费红线 | INFERENCE | 中（需用户预算授权） |
| 记忆混合评分召回率收益 | 参考项目 README 声称，未在 VAP 实测 | UNVERIFIED | — |
| MIB/向量二值化（存储 32x） | 参考 mnesosyne 声称，未在 VAP 验证召回率影响 | UNVERIFIED | — |

---

## Decisions and Assumptions

### 已闭环事实（旧指南 v9 P0 已实现于 v10.0-v10.2，**不再列出为待办**）

1. **CI 门禁**（旧 P0-1）：`|| true` 已去 + `--cov-fail-under=80`（ci.yml:42-45）→ CHANGELOG v10.1.0 注明。
2. **GCGuard 接入**（旧 P0-1）：app.py:107-124 已接入 lifespan。
3. **ReactLoopAgent 接通 prod**（旧 P0-2）：feature flag `VAP_AGENT_BACKEND`（header>env，默认 legacy 零回归），`react` 走 `_build_react_agent` 全链路（ReactLoopAgent + SessionStore + SyncLLMClientAdapter/ProviderRouterClient）；`/api/agent/sessions` + Turn 时间轴 API 已落地。
4. **ProviderRouterClient 补全**（旧 P0-2）：provider_router_client.py 已实现 async stream 协议。
5. **OpenAI 兼容 schema**（旧 P0-2 关联）：to_llm_schema 已改嵌套 `{type:function,...}`，真实 NVIDIA 200。
6. **config/im_gateway.db 误入库**（旧 P0-1）：.gitignore:69-77 已覆盖全部运行时 db。
7. **PromptGuard 注入守卫**（旧 P0-3 关联）：已在 run_turn 内调用（loop.py:179），含 5 类 19 规则。
8. **SessionStore 持久化**（旧 P0-2 关联）：agent.py /sessions + 崩溃恢复 `load_session` 已落地。

### 当前假设（INFERENCE）

1. **假设"监督层/审批接线是当前最高优先级"**——基于 loop.py 无 supervisor、registry 审批/sandbox 定义未接线（均为 DIRECT），是"已实现未接通"的重复模式。需用户确认排序。
2. **假设桌面单用户产品不引 PostgreSQL/Docker**——延续 v1 判定，7 簇报告一致 SQLite 本地优先。需用户确认无多端/多租户诉求。
3. **假设付费 API 红线延续**：视频生成（Seedance/image 等）与 IM/remote 真实凭据，默认预算 0，Mock 协议验证，用户授权后才真连。
4. **假设写审批首版默认 allow（零回归）**——react 路径先注入 approval handler（SSE Ask 事件 + 环境变量分级），默认放行真实视频分析任务，写危险工具（delete/写盘）才弹审批。需用户确认默认策略。
5. **假设监督层 feature flag 默认关**——先实现低误判的 2-3 场景，观察后再全开。

### 架构决策（基于跨簇共识 + v10.2 现状）

1. **监督层走 OpenHands stuck 场景裁剪 + context-mode 压缩 + 预算 guard**，纯 asyncio 无新依赖，feature flag 控制。
2. **记忆分层走"Working Memory 热层 + 经验 SQLite 升 FTS5 混合检索 + 时序/图记忆"，SQLite 本地优先**（graphiti/plur/okf-agent-memory 范式）。
3. **skills 走"蒸馏→验证→棘轮"闭环 + SkillSpector 安全扫描 + roster 按需分配**（nuwa/EvoSkill/SkillOpt/AREX 范式），复刻到 src/skills/ 现有 loader 之上。
4. **Electron 壳走 DSH 四段式 + nodeterm 黑匣子 + OpenDesign 断路器**，全部落地为独立 JS 模块（log-store.js / crash-diagnostics.js），可单测。
5. **写审批 + scope_guard + sandbox 接线**：registry 已定义 API 但从未接线——本轮把 approval/sandbox 从"已实现未接通"变为"默认安全+"，不改 loop 直连回归。

### 被否决 / 降级方案

1. **PG/pgvector 升级（否决）**：桌面本地优先。
2. **Tauri 迁移（否决）**：丢 Node 托管便利。
3. **验证码绕过 / 纯攻防黑产（否决）**：法律灰色 + GPL-3.0 定位。
4. **ECC 181 skills 照搬（否决）**：YAGNI。
5. **媒体生成付费链路全做（缩小范围）**：先免费层（ASR/字幕/TTS/竖屏），付费生成 API 走 Mock 协议直到用户授权。
6. **OpenHands 五场景全照搬（缩小范围）**：先 2-3 低误判场景 + feature flag 默认关。
7. **MCP server 双形态（降 P3）**：基础件齐备（监督/审批/记忆/skills 闭环）后再对外暴露。

---

## Risks and Blockers

| 风险 | 等级 | 触发条件 | 影响范围 | 缓解方式 | 需用户确认 |
|------|------|----------|----------|----------|------------|
| 监督层误判正常长任务为卡死(长视频多步工具调用) | L2 | 卡死检测阈值过严 | agent 用户路径被打断 | 先 2-3 场景 + feature flag 默认关 + 阈值可配置 | 否(可回滚) |
| 写审批接线破坏 react 路径零回归 | L2 | approval handler 误拒真实分析工具 | agent 全链路 | 默认 allow 白名单 + 仅危险写工具弹审批 + header/env 分级开关 | 是(默认策略) |
| sandbox 接线在部分环境(无 pywin32/landlock)降级 | L2 | 沙箱 enter 失败 | 工具执行 | registry 已有 enter 失败降级直跑 + 告警 | 否 |
| 经验 SQLite 升 FTS5 迁移 | L2 | 现有 experience 表数据 | 记忆质量 | 新增表/索引不重建；坏则回滚 | 否 |
| skills 棘轮产生坏 skill | L2 | 蒸馏/验证不严 | agent 行为漂移 | nuwa 三重验证 + 评分卡 + 棘轮回滚 | 否(机制内含) |
| 记忆热层注入增大 context | L2 | TTL/注入策略不当 | token 成本 | TTL 白名单 + 注入上限 | 否 |
| Electron 断路器误判 | L2 | 阈值过严 | 正常重载被阻断 | 5 次/60s + 5min auto re-arm + 配置 | 否 |
| electron-updater 落地需发布 feed+签名 | L3 | 无 update server | 自动更新不可用 | 先落地代码 + 文档修正声明，feed 待发布基建 | 是(发布基建) |
| 视频生成/IM/remote 真实凭据 | L3 | 用户授权前真连 | 付费 API 红线 | Mock 协议优先，授权后才真连 | 是(凭据+预算) |

---

## Implementation Batches

> 全部 PENDING（ANALYSIS_ONLY）。每批含目标/文件/依赖/回滚。

### Batch P0-1：Agent 监督层（服务意图①，最高优先）

- **目标**：为 ReactLoopAgent 补齐监督层——①卡死检测（OpenHands stuck 场景裁剪：同 action 同 observation 重复 / 相同 tool_args 循环无进展 / 连续 error 循环 → 触发 recovery 提示 + 中断）②上下文压缩（context-mode 范式：Session 超阈值时用历史 user 事件生成压缩摘要作为新 system 段，保留 system 与最近 N 轮）③成本预算（per-turn token 上限 + 累计预算 guard，超限 stop_reason=BUDGET）。
- **涉及文件**：新增 `src/core/agent/supervisor.py`（StuckDetector + ContextCompressor + BudgetGuard）、`loop.py`（run_turn 内接 supervisor，feature flag `VAP_AGENT_SUPERVISOR`）、`turn.py`（TurnStopReason 加 BUDGET / STUCK，可选加 condensation phase）、`agent.py`（装配）。
- **依赖**：无（纯 asyncio，无新依赖）。
- **回滚**：feature flag 默认关，零回归。
- **验证**：新增 `test_supervisor.py`（2 卡死场景 + 压缩触发 + 预算超限）。最小实验：1 场景先验证。
- **参考**：OpenHands stuck.py / context-mode / LongHorizon-Harness。

### Batch P0-2：写审批 + scope_guard + sandbox 接线（服务意图④⑤，安全姿态）

- **目标**：把 registry 已定义未接线的 `set_approval_fn` / `set_sandbox` 真正装配到 Agent 运行路径——①staged-approval：waterfall pre_execute 的 Ask 信号升级为"分级审批"（读操作放行 / 写操作弹审批 / 危险写操作（delete_* / highlight_cut 落盘 / IM send）必须审批），approval handler 经 agent router SSE 投递 + 用户确认回调，超时默认 deny；②scope_guard chokepoint（pi-guardrails/pigeon 最小权限）：工具名+args → allow/deny/ask 三级决策表 + 环境变量分级开关（`VAP_ALLOW_WRITE_CUT` 等）；③sandbox 接线：`set_sandbox(build_sandbox(), enabled=VAP_SANDBOX_ENABLED)`。
- **涉及文件**：新增 `src/core/tools/scope_guard.py`、`waterfall.py`（Ask→分级信号）、`registry.py`（装配入口）、`agent.py` / `loop.py`（注入 approval + sandbox）、`deps.py`（SSE approval 通道）。
- **依赖**：无。
- **回滚**：approval handler 默认 allow（白名单）时 behavior 与现状一致，feature flag 可关。
- **验证**：`test_agent_framework.py` 扩 approval 用例 + 新 `test_scope_guard.py`（ask/deny/allow 三分支 + 环境变量覆盖）。
- **参考**：commerce-agents staged-approval / agent-guardrails / pi-guardrails / pigeon。

### Batch P1-1：记忆分层（服务意图③）

- **目标**：现有扁平记忆升级为三层——①Working Memory 热层（最近会话关键事实 TTL 注入，SQLite，turn 开始时注入）：②Episodic/语义层（ExperienceStore 加 FTS5 全文索引 + 混合评分（SQLite FTS5 关键词分 + quality_score + recency 加权），替换 LIKE 模糊）；③时序/图记忆（decision_log + experience 事件 → 主体/关系/时间三元组图，graphiti/plur 范式，SQLite 表）。**接入主控**：run_turn end（SessionEnd）钩子 → ExperienceExtractor → ExperienceStore → SkillAdvisor；turn start 注入 Working Memory。
- **涉及文件**：新增 `src/core/memory/working.py` + `triplestore.py`、`experience.py`（FTS5 迁移 + 混合评分）、`loop.py`（SessionEnd/TurnStart 钩子）、`skills/router` 消费。
- **依赖**：无（纯 stdlib + 已有 SQLite）。
- **回滚**：新增表不重建，热层可关。
- **验证**：`test_memory_working.py` + `test_experience_search.py`（FTS5 召回 vs LIKE）。
- **参考**：okf-agent-memory / claude-mem / plur / graphiti / mnemosyne。

### Batch P1-2：skills 蒸馏→验证→棘轮闭环 + 安全扫描 + 按需分配（服务意图③）

- **目标**：skill_generator 单向生成升为闭环——①蒸馏（nuwa/EvoSkill 范式：Experience 聚合 tool_chain → skill 草稿 → 概念验证）；②三重验证（Validator：跨 2+ 领域 / 有预测力 / 排他性）+ **SkillSpector 安全扫描**（frontmatter 注入 / 指令泄露 / 恶意命令检测）；③棘轮机制（达尔文棘轮：评分卡持续评测，只留改进回滚退步，demo 用技能自测脚本）；④skillroster 按需分配（skills-best-practices progressive disclosure：按 intent 实时加载相关 skill 子集而非全量注入，降低 context 开销）。
- **涉及文件**：新增 `src/skills/distiller.py` / `validator.py` / `scoring.py` / `spectre.py` / `roster.py`、`skills/loader.py`（结构规范 + 安全前端）。
- **依赖**：P1-1（Experience 接主控后提供原料）。
- **回滚**：单向生成保留，棘轮 feature flag。
- **验证**：`test_distiller.py`（蒸馏 1 个 skill 端到端）+ `test_spectre.py`（注入样本扫描）+ `test_roster.py`（按需加载子集）。
- **参考**：nuwa-skill / EvoSkill / SkillOpt / AREX-Skill / SkillSpector / skills-best-practices。

### Batch P1-3：Electron 壳健壮性 + 黑匣子（服务意图②）

- **目标**：runtime-controller.js 升级（DSH 四段式 + 状态机 starting/ready/crashed/restarting）+ 崩溃断路器（OpenDesign：renderer crash-loop 检测，5 次/60s + 5min auto re-arm）+ 黑匣子实时面板（nodeterm：主进程 console ring buffer + 渲染层实时流 + level 过滤 + 搜索 + Copy/Clear）+ 诊断导出（一键打包 logs + config + gc_guard 快照）+ 启动屏（DSH startup 5 步修复流程）+ autoUpdater 代码落地（electron-updater 依赖 + 更新检查，feed 待发布基建）。
- **涉及文件**：desktop/main.js、runtime-controller.js、新增 `desktop/log-store.js` + `crash-diagnostics.js` + `startup.html`、desktop/package.json（+electron-updater）、webapp logs 页（黑匣子 UI）。
- **依赖**：无（独立于后端）。
- **回滚**：壳层 feature flag / 独立模块可切。
- **验证**：desktop e2e（崩溃恢复 + 日志面板 + 诊断导出 + 断路器 re-arm）。
- **参考**：nodeterm LogPanel / OpenDesign crash-loop / DSH 四段式 / clawdeck。

### Batch P2-1：视频生成/剪辑 Agent 工具集（服务意图④）

- **目标**：三阶段流水线之上加"内容生产"工具集——①对话式剪辑（video-use 范式：自然语言选段+字幕+转场）；②字幕/配音全流程（VideoCaptioner 范式：免费 ASR + Edge TTS + 字幕生成，付费 ASR/TTS 走 Mock 协议）；③智能剪辑（PreenCut/OpenMontage：自动高光、智能成片）；④短视频"主题→成片"流水线（MoneyPrinterTurbo；竖屏 + 字幕 + BGM，接已有 funclip-clip skill）。
- **涉及文件**：新增 `src/core/tools/media_gen/`（subtitle.py / clip.py / voiceover.py / shortvideo.py）、`adapter.py` 注册新工具、`config/prompts/` 加媒体模板。
- **依赖**：P0-2（写审批，落盘工具需审批）+ P1-2（skill 框架）。
- **回滚**：新工具独立注册，可 disposer。
- **验证**：`test_media_gen.py` + 免费层端到端（1 个字幕 + 1 个竖屏成片）。
- **参考**：video-use / VideoCaptioner / PreenCut / MoneyPrinterTurbo / OpenMontage / funclip(已装)。

### Batch P2-2：电商 / PPT / 营销 skills（服务意图④）

- **目标**：非软件领域 extension——commerce-agents 双 Agent（shopping/merchant 检索+下单审批）、dashi-ppt 版式组合（PPT 生成）、claude-ads 多平台分发（营销文案，写操作审批前置）。
- **涉及文件**：`plugins/` 声明式 patch、`src/skills/`、`src/core/subagent/role_template.py`（加商务角色）。
- **依赖**：P1-2（skill 框架+安全扫描）+ P0-2（写审批）。
- **回滚**：skill 独立可卸载。
- **验证**：端到端 1 个 PPT + 1 个广告文案（Mock 生成，守付费红线）。
- **参考**：commerce-agents / dashi-ppt / claude-ads。

### Batch P2-3：浏览器 / 桌面 GUI 自动化工具（服务意图④⑤）

- **目标**：把"看/点/读网页 + 桌面操作"变成 Agent 工具——browser-use 浏览器自动化（Playwright 桥接，可复用现有 e2e 基建）、chrome-cdp 调试协议（实时页面状态）、TuriX-CUA 桌面 Computer Use（截图 + 坐标点击，Windows 后端）。危险性分级入 scope_guard。
- **涉及文件**：`src/core/tools/web_auto/`（browser.py / cdp.py）、`desktop/`（CUA 服务，Electron 壳内 IPC）。
- **依赖**：P0-2（GUI/浏览器写操作审批）+ P1-2。
- **回滚**：工具独立注册。
- **验证**：`test_web_auto.py`（Playwright 桥 1 个流程）+ CUA 单截图操作。
- **参考**：browser-use / chrome-cdp / TuriX-CUA。

### Batch P3-1：MCP server 双形态（服务意图⑤）

- **目标**：视频分析 Agent 工具对内仍是 adapter 桥接，对外发成 MCP server（browser-use/video-use 双形态范式），Claude Code / 其它 MCP 客户端可调。
- **涉及文件**：新增 `src/mcp/server.py`（fastmcp 条件依赖或 stdio 手写）、`config/mcp/` 工具白名单。
- **依赖**：P0-P1 基础件齐备 + P2-1 工具集存在。
- **回滚**：MCP server 独立进程可关。
- **验证**：1 个工具 MCP 暴露 + 外部客户端调用（Mock）。
- **参考**：browser-use 双形态 / claude-plugins-official 决策树。

### Batch P3-2：反 AI-slop 设计升级（服务意图②）

- **目标**：v10.2 电光青美学深化——对照 impeccable/OJO 反模式清单（禁 Inter 默认栈 + 紫蓝渐变 + 卡中卡 + 均匀圆角阴影）自检，数据可视化（metrics/requests 页）当作设计系统一部分（web 设计质量 10 品质清单）。
- **涉及文件**：webapp/src/components/ 设计 tokens。
- **依赖**：无。
- **回滚**：样式回滚。
- **验证**：设计自检 + 视觉回归截图。
- **参考**：impeccable / OJO-Design-Skills / v10.2 反 AI 美学已打底。

---

## Validation Matrix

| Requirement | Validation Method | Actual Command-Action | Result | Evidence |
|-------------|-------------------|----------------------|--------|----------|
| 11 簇收敛摘要读取 | Read `recon_synthesis_summary.md` | 完整 Read | 收敛表 + 用户意图解读 | .agent-scratch/ |
| DIRECT 证据核查读取 | Read `evidence_check.md` | 完整 Read | A 已闭环 / B 真缺口 | .agent-scratch/ |
| 主项目关键源码直读 | Read loop/session/turn/prompt_guard/registry/waterfall/error_policy/definition/provider_router_client/experience/skill_generator/agent/app/ci/cipher/director/inprocess/main/runtime-controller/skills loader/remote adapters | 全部读取成功 | 关键缺口 5 项 DIRECT 确认 | 本会话 Read 记录 |
| set_approval_fn/set_sandbox 零调用方 | Bash grep src/ `\.set_approval_fn(\|\.set_sandbox(` | 仅 registry.py 定义行命中 | 未接线确认（DIRECT） | Bash 输出 |
| autoUpdater 缺失 | Bash grep desktop/ autoUpdater/electron-updater | 仅 node_modules app-builder-lib 引用 | 应用代码无 autoUpdater | Bash 输出 |
| config/skills 8 内置 | Glob config/skills/*/SKILL.md | 8 命中 | skills 生态窄 | Glob 输出 |
| CI 已去 `\|\| true` + 覆盖率门禁 | Read ci.yml | ci.yml:42-45 | 已闭环 | DIRECT |
| 路线图 P0 优先扩展 | 检查排序 | P0-1/P0-2 在 P1/P2 前 | 监督层+审批接线优先 | 本指南段四 |
| 指南落盘 | Write 主项目根目录 | 已创建 | 待 final | 本文件存在 |

---

## Review Findings

> ANALYSIS_ONLY 阶段，无 Builder 修复，无 Critic 复验。以下为 SynthesisArchitect 自检。

- **已发现**：v10.2.0 与旧 v9 指南的差距已大幅收敛——旧 P0 中 CI/GCGuard/ReactLoopAgent 接通/ProviderRouterClient/schema 兼容/im_gateway.db 均闭环。真正剩余的 P0 是"**监督层零实现**"与"**审批/sandbox 定义未接线**"两类"已实现未接通"；P1 是记忆/skills/Electron 三块生态。
- **Builder 修复状态**：N/A（本阶段不改源码）。
- **复验结果**：本会话 9 份 input（1 份收敛摘要 + 1 份证据核查 + 7+ 轮源码直读）全部 Read 成功；关键缺口（监督层零实现、审批接线零调用方、记忆未接主控、skills 单向、Electron 无 autoUpdater/断路器）均有 DIRECT 证据。
- **剩余风险**：
  1. webapp 页面统计 16 页文件（Glob 实测）；任务卡曾称 18 页，Critic 复核为计数口径误写（非别名/版本差异），以 16 页为准；不影响结论。
  2. 记忆"热层/混合评分/时序图"收益为参考项目声称，未在 VAP 实测（UNVERIFIED）——先最小实验。
  3. 视频生成付费 API、IM/remote 真实化均需用户凭据+预算（L3，阻塞但非当前批次）。
  4. electron-updater 需发布 feed+签名（L3），先落地代码修正声明。
  5. 旧指南 v1 已在 docs/archive/，本次直接新建根目录 v2，未删除 archive。

---

## Next Gate

- **下一步动作**：将本指南交付 team-lead 转呈用户审阅，等待确认后进入实施。
- **是否需用户批准**：是（ANALYSIS_ONLY）。
- **需批准项**：
  1. 排序原则：是否同意"监督层+审批接线（P0）→ 记忆/skills/Electron（P1）→ 视频/电商/浏览器（P2）→ MCP/设计（P3）"？是否想把某 P1/P2 提前？
  2. 写审批默认策略：是否同意"默认 allow 白名单 + 仅危险写工具弹审批"（零回归首版）？还是要默认全部写操作审批？
  3. 监督层 feature flag 默认关 + 先 2-3 卡死场景：是否接受？
  4. 记忆层 SQLite 本地优先（不引 PG）：是否继续认可？
  5. 视频生成/IM/remote 真实凭据：当前阶段是否推进（需预算）还是保持 Mock？
  6. electron-updater 发布基建：是否授权配置 update server / 签名，还是先只落地代码+文档？

---

# 第一部分：阶段一 跨簇交叉验证

## 方法

对 recon_synthesis_summary.md 的"跨簇高价值候选收敛"逐项核对：≥2 独立簇 Top8 重合 = 强信号（⭐⭐）；1 簇 = 孤证（降权）。本 v2 只采 ⭐⭐。

## 强信号收敛（多簇独立 Top8 重合）

| 收敛主题 | 命中项目（簇） | 主项目缺口（DIRECT） | 落点 |
|---------|----------------|----------------------|------|
| Agent 卡死检测 + 上下文压缩 | OpenHands(F/I)、context-mode(D/G/J)、LongHorizon-Harness(I) | loop.py 主循环无 stuck/压缩/预算 | P0-1 |
| 桌面 Agent 平台多形态 | DSH 全家(D/I/F)、LeAgent(G)、VetarAI(J)、wayland(J/K)、autoshorts/VibeNVR(B/K) | Electron 壳 + Agent 框架已具雏形，缺监督/记忆/生态 | P1-3 / P3 生态 |
| 三层记忆 + 上下文续航 | context-mode/memory(okf/plur/remnic)(D/G/J)、CowAgent(D)、graphiti(J) | 仅 experience.py 扁平 LIKE，未接主控 | P1-1 |
| 视频生成/剪辑全流水线 | MoneyPrinterTurbo(H/B/G)、video-use/VideoCaptioner/funclip(K/K/G)、OpenMontage/PreenCut(I)、Lingji-cut(G) | 仅三阶段分析 + funclip skill，无内容生产工具集 | P2-1 |
| 写审批 / 安全护栏 | commerce-agents(电商)、agent-guardrails、pi-guardrails、pigeon(安全) | registry set_approval_fn/set_sandbox 零调用方 | P0-2 |
| skills 蒸馏 / 技能生态 | nuwa(D)、ClawX(D)、wayland(2100 skills)(J/K) | skill_generator 单向 + 8 内置 | P1-2 |
| 本地优先桌面 Agent 中枢 | VetarAI(J)、Lingji-cut(G)、wayland(J/K) | SQLite+ChromaDB 本地优先已一致 | 延续 |

> 孤证降权示例：swarms 拓扑（单簇）、OpenMontage typed IR（单簇）不纳入 v2 主线，仅在 P2-1 参考思想。

## 用户参考目录战略意图（三维坐标）

1. **通用 Agent 平台底座件**（记忆/上下文/编排/治理/桌面壳/审批/证据）→ 可复刻为 VAP 架构件（P0-1/P0-2/P1-1/P1-3）。
2. **视频生态横向延伸**（剪辑/字幕/生成/竖屏/NVR）→ VAP 视频能力产品化（P2-1）。
3. **未来新领域预研**（电商/PPT/办公/营销/浏览器/知识图谱）→ VAP 扩展性 D2C（P2-2/P2-3/P3）。

> 评价结论：参考目录不是"当前功能 1:1 对照库"，而是"底座件对照 + 视频延伸 + 新领域预研"三库合一——v2 路线图按此三维设计。

---

# 第二部分：阶段二 六层差距（以 v10.2.0 为准）

## 功能层

| 维度 | v10.2.0 现状（DIRECT） | 参考最佳实践 | 差距 |
|------|----------------------|--------------|------|
| Agent 健壮性 | max_steps=8 + try/except TimeoutError，无 stuck/压缩/预算 | OpenHands stuck.py + context-mode + LongHorizon | 缺监督层（P0-1） |
| 写操作治理 | waterfall Ask 信号存在但无 handler 接线 | commerce-agents staged-approval + pi-guardrails | 缺接线（P0-2） |
| 跨会话记忆 | experience.py LIKE + quality_score | okf/claude-mem/graphiti 三层 + 混合评分 | 缺热层/时序/混合（P1-1） |
| skills 生态 | skill_generator 单向 + 8 内置 | nuwa/EvoSkill 蒸馏闭环 + SkillSpector 扫描 | 缺闭环（P1-2） |
| 媒体生成 | 三阶段分析，无内容生产工具 | MoneyPrinterTurbo/VideoCaptioner 全流程 | 缺工具集（P2-1） |
| 横向 skill | 无电商/PPT/营销 | commerce-agents/dashi-ppt/claude-ads | 缺 skill（P2-2） |
| GUI 自动化 | 无浏览器/桌面操作工具 | browser-use/chrome-cdp/TuriX-CUA | 缺工具（P2-3） |

## 架构层

| 维度 | v10.2.0 现状 | 参考最佳实践 | 差距 |
|------|-------------|--------------|------|
| Agent 监督架构 | loop.py 单 while，无 supervisor 注入点 | OpenHands supervisor + event_stream | 需 supervisor 模块（P0-1） |
| 审批/沙箱架构 | registry API 完备但零装配 | commerce-agents approval chain + sandbox | 需装配层（P0-2） |
| 记忆架构 | 扁平 SQLite 经验 + ChromaDB | 热层/语义层/时序图三层 | 需分层（P1-1） |
| skills 架构 | 单向生成 + 静态 8 内置 | 蒸馏→验证→棘轮 + roster 分配 + 安全扫描 | 需闭环（P1-2） |
| Electron 壳架构 | spawn+探活+loadURL | DSH 状态机 + 断路器 + 黑匣子 + 诊断 | 需健壮化（P1-3） |
| 工具扩展 | adapter 桥接 17 工具 | browser/video/GUI 多域工具 + MCP 双形态 | 需扩展域（P2/P3） |

## 工程层

| 维度 | v10.2.0 现状 | 差距 |
|------|-------------|------|
| CI | 已门禁 80% + e2e job（已闭环） | 无 |
| 测试 | 952 用例，标准子集 804 passed + 2 skip | supervisor/scope_guard/memory/distiller 补测 |
| 文档 | CLAUDE.md 计数需随 **16 router** 同步 | 收尾时同步 |
| logic.py 巨石 | 1591 行 | P2-1 媒体工具集独立模块化时顺带分拆（不提前重构） |

## 性能层

| 维度 | v10.2.0 现状 | 参考最佳实践 | 差距 |
|------|-------------|--------------|------|
| context 管理 | SessionEvent 全量回放，无压缩 | context-mode 分块压缩 | P0-1 压缩 |
| skills 注入 | match_skills 全量前置 | roster 按需分配 progressive disclosure | P1-2 |
| 记忆检索 | SQLite LIKE 模糊 | FTS5 混合评分 | P1-1 |

## 安全层

| 维度 | v10.2.0 现状 | 参考最佳实践 | 差距 |
|------|-------------|--------------|------|
| 提示注入 | guard_messages 已接入（已闭环） | OpenHands confirmation_mode | 无（已闭环） |
| 写操作 | 无审批 handler（未接线） | staged-approval + scope_guard | P0-2 |
| 沙箱 | sandbox 已实现未接线 | LinuxLandlock/WindowsJob | P0-2 接线 |
| 凭据 | env>keyring>ini 分层 + 审计/轮换 | per-agent budget（pentestkit） | P1 可选（per-agent 预算） |
| IM 加密 | XOR 降级（非真 AES） | Fernet（cryptography 条件依赖） | 真实凭据时升级（L3） |

## 运维层

| 维度 | v10.2.0 现状 | 参考最佳实践 | 差距 |
|------|-------------|--------------|------|
| 崩溃恢复 | runtime-controller 探活+loadURL，无断路器 | OpenDesign crash-loop + auto re-arm | P1-3 |
| 实时日志 | logs 页只读文件浏览 | nodeterm 实时流+过滤+搜索+Copy/Clear | P1-3 |
| 诊断导出 | 无 | DSH diagnostic-export 一键打包 | P1-3 |
| 自动更新 | 无 autoUpdater 代码 | electron-updater 生命周期 | P1-3（feed 待基建） |
| 启动屏 | 无 | DSH startup 5 步修复 | P1-3 |

---

# 第三部分：阶段三 可迁移分类

## A. 可迁移（设计思想 + 具体落地，低成本）

| 项 | 证据 | 预期收益 | 成本 | 风险 | 落点 |
|----|------|----------|------|------|------|
| OpenHands stuck 场景裁剪（2-3 场景） | OpenHands stuck.py（多簇） | 防卡死 | L | 低(feature flag) | P0-1 |
| context-mode 分块压缩 + 摘要 | context-mode(D/G/J) | 长会话续航 | M | 中(误压缩) | P0-1 |
| per-turn token/累计预算 guard | LongHorizon-Harness | 防超支 | L | 低 | P0-1 |
| commerce-agents staged-approval（Ask handler 接线） | registry 已有 API，零调用（DIRECT） | 写安全 | M | 低(默认 allow) | P0-2 |
| pi-guardrails/pigeon 最小权限 scope_guard | scope 分级 | 危险写拦截 | L | 低 | P0-2 |
| sandbox 接线（build_sandbox 已实现） | sandbox.py 存在，未接线 | 工具隔离 | L | 低(enter 失败降级) | P0-2 |
| FTS5 混合评分替换 LIKE | experience.py 现状 DIRECT | 记忆召回 | L | 低(新表不重建) | P1-1 |
| Working Memory 热层 TTL 注入 | okf/claude-mem | AI 懂用户 | M | 低 | P1-1 |
| nuwa 蒸馏→验证→棘轮 | 多簇 | skills 可用 | M | 中(坏 skill) | P1-2 |
| SkillSpector 安全扫描 | 技能安全 | 防注入 | L | 低 | P1-2 |
| skillroster 按需分配 | skills-best-practices | 降 context | L | 低 | P1-2 |
| nodeterm 黑匣子面板 | nodeterm | 小白易用 | M | 低(独立模块) | P1-3 |
| OpenDesign 崩溃断路器 | OpenDesign | 壳不黑屏 | L | 低(阈值配置) | P1-3 |
| DSH 四段式状态机 | DSH | 壳健壮 | M | 低 | P1-3 |
| 诊断导出（logs+config+gc 快照） | DSH | 排障 | L | 低 | P1-3 |
| VideoCaptioner 免费字幕全流程 | VideoCaptioner | 字幕能力 | M | 低(免费层) | P2-1 |
| MoneyPrinterTurbo 竖屏成片流水线 | MoneyPrinterTurbo | 短视频 | M | 低(免费 BGM/TTS) | P2-1 |
| browser-use Playwright 桥 | browser-use | 浏览器自动化 | M | 中(写操作审批) | P2-3 |

## B. 半可迁移（需改造/外部条件）

| 项 | 原因 | 落点 |
|----|------|------|
| graphiti 时序图记忆 | SQLite schema 设计 + 与 decision_log 融合 | P1-1（先小规模） |
| 视频生成付费链路 | 需用户预算授权（付费 API 红线） | P2-1（Mock 协议先行） |
| TuriX-CUA 桌面 Computer Use | 需 Windows 桌面窗口控制 + 权限 | P2-3 |
| electron-updater 自动更新 | 需发布 feed + 签名基建 | P1-3（先代码/文档，feed 待基建） |
| IM/remote 真实化 | 需用户 bot token + 预算 | L3 阻塞 |
| cryptography 真加密升级 | 依赖策略决策（可选依赖） | 真实凭据时触发 |
| MCP server 双形态 | 需基础件齐备后对外暴露 | P3-1 |

## C. 不建议迁移

| 项 | 原因 |
|----|------|
| PostgreSQL + pgvector + Docker | 桌面单用户不匹配 |
| Tauri | 丢 Node 托管便利 |
| 验证码绕过 | 法律灰色 |
| AutoSploit/arsenal 等进攻攻击面 | 定位不符 |
| 重型浏览器逆向 | 合规 + 维护成本 |
| ECC 181 skills 照搬 | YAGNI |
| baoyu-skills 全量 | 上下文开销 |

---

# 第四部分：阶段四 优先级路线图（P0/P1/P2/P3）

## 用户 5 条原始意图 → 路线图映射

| 用户意图 | 批次 | 关键内容 |
|---------|------|---------|
| ① Agent 更完美 | P0-1 | 监督层（stuck/压缩/预算） |
| ④⑤ 真干活扩领域 + 安全姿态 | P0-2 | 写审批 + scope_guard + sandbox（跨意图底座） |
| ③ AI 懂用户 + skills 沉淀 | P1-1 + P1-2 | 记忆分层 + skills 蒸馏闭环 |
| ② 小白易用 | P1-3 + P3-2 | Electron 黑匣子/启动屏 + 反 AI-slop 设计 |
| ④ 真干活扩领域 | P2-1/P2-2/P2-3 | 视频工具集 / 电商PPT营销 / 浏览器GUI |
| ⑤ 扩展延伸性 | P3-1 | MCP server 双形态 + 插件生态 |

## 排序原则

监督层与写审批接线（"已实现未接通"的 P0 缺口，破坏安全与健壮性）优先于新增扩展域；记忆/skills/Electron（P1，服务意图②③）在 P0 稳定后推进；视频/电商/浏览器（P2）与 MCP/设计（P3）为扩展后续。

### P0（必须先做）

- **P0-1 Agent 监督层**（stuck 2-3 场景 + context 压缩 + cost budget；feature flag 默认关）→ 详见 Batches。
- **P0-2 写审批 + scope_guard + sandbox 接线**（registry 已定义零调用 → 装配 + 分级审批 + SSE 通道）→ 详见 Batches。

### P1（明显提升质量/可维护性）

- **P1-1 记忆分层**（热层 + FTS5 混合 + 时序/图 + 主控接入）。
- **P1-2 skills 蒸馏闭环 + 安全扫描 + 按需分配**。
- **P1-3 Electron 壳健壮性 + 黑匣子 + 诊断导出 + autoUpdater 代码**。

### P2（锦上添花，扩展领域）

- **P2-1 视频生成/剪辑工具集**（对话式剪辑 / 字幕配音 / 智能剪辑 / 竖屏成片）。
- **P2-2 电商 / PPT / 营销 skills**。
- **P2-3 浏览器 / 桌面 GUI 自动化工具**。

### P3（长期/延伸性）

- **P3-1 MCP server 双形态**。
- **P3-2 反 AI-slop 设计升级**。

### 明确不迁

PG/pgvector、Tauri、验证码绕过、纯攻防黑产（只借通用抽象）。

---

# 第五部分：阶段五 Red Team（事前反证）

## Red Team P0-1：Agent 监督层

- **未验证假设**：OpenHands stuck 五场景直接适用 VAP。但 VAP 是视频分析 + Agent 平台，卡死形态不同（长视频工具调用多步，不能误判）。
- **漏了什么**：监督层误判正常长任务 → 用户关 flag → 监督名存实亡。
- **更简单路径**：仅实现最高价值 2 场景（同 action 同 observation 重复 + 同 tool_args 无进展循环），feature flag 默认关，观察 N 轮再扩。
- **最小实验**：1 卡死场景 + 1 正常长任务，验证检测准确率。
- **最终建议**：缩小范围（2 场景），默认关。

## Red Team P0-2：写审批接线

- **未验证假设**：接审批 handler 不破坏 react 路径零回归。实际上 approval 回调返回 False → ToolDenied → ToolResult.error → LLM 调整；若默认 deny 会把正常视频工具都憋死。
- **更简单路径**：白名单工具不弹审批直接放行；仅写危险操作（delete_history / 落盘生成 / IM send）弹审批；SSE Ask 事件 + 30s 超时默认 deny；环境变量分级开关。
- **最小实验**：1 个写工具审批端到端（SSE Ask → 用户确认 → 工具执行 / 拒绝回归）。
- **最终建议**：默认 allow + 危险写拦截白名单，分两档。

## Red Team P1-1：记忆分层

- **未验证假设**：FTS5 混合评分召回率优于 LIKE。VAP 现 experience 量小，混合评分收益需实测。
- **更简单路径**：先做 Working Memory 热层 + FTS5（值小风险低），时序图记忆分批。
- **最终建议**：先热层 + FTS5，时序图最小实验后。

## Red Team P1-2：skills 棘轮

- **漏了什么**：蒸馏 skill 若含恶意 frontmatter / 指令注入会污染主 agent。
- **缓解**：SkillSpector 安全扫描前置 + 棘轮只允许评分提升的 skill 晋升；演示环境跑 self-test。
- **最终建议**：扫描与棘轮一体交付，不拆。

## Red Team P1-3：Electron 壳

- **漏了什么**：断路器误判阻断正常重载；黑匣子脱敏（日志可能含凭据/路径）。
- **缓解**：阈值可配置 + auto re-arm；BoundedLogStore 模式脱敏（密钥/长路径截断）。
- **最终建议**：断路器 + 脱敏一起交付。

## Red Team P2-1：视频生成

- **漏了什么**：付费视频生成 API 无 key 则不可用。
- **缓解**：免费层优先（ASR/Edge TTS/字幕/竖屏），付费走 Mock 协议；用户授权预算后再真连。
- **最终建议**：免费层两端验证，付费 Mock。

## 被否决/降级汇总

| 建议 | 处理 | 原因 |
|------|------|------|
| PG/pgvector | 否决 | 桌面本地 |
| Tauri | 否决 | 生态损失 |
| 验证码绕过/纯黑产 | 否决 | 法律灰色 |
| ECC 181 skills | 否决 | YAGNI |
| OpenHands 五场景全照搬 | 缩小范围 | 分离场景误判 |
| MCP 双形态提前 | 降 P3 | 基础件未齐 |
| 记忆全分层一次做 | 缩小范围 | 先热层+FTS5 |
| 视频生成付费链路 | 缩小范围 | 免费层先行 |

---

# 第六部分：阶段六 全栈实施方案

## 前端层（webapp/）

- **改造**：logs 页升黑匣子实时面板（nodeterm 范式：实时流+过滤+搜索+Copy/Clear）+ agent 页加审批 Ask 弹窗（staged-approval UI：显示工具名+args+Allow/Deny）+ 启动屏（DSH startup 5 步修复）+ P3-2 反 AI-slop 自检。
- **实施批次**：P1-3（logs+启动屏）→ P0-2（审批 UI）→ P3-2。

## 后端层（src/web/ + src/core/）

- **改造**：`supervisor.py`（P0-1）+ `scope_guard.py` + approval/sandbox 装配（P0-2）+ `src/core/memory/`（P1-1）+ `src/skills/` 闭环（P1-2）+ `media_gen/`（P2-1）+ `web_auto/`（P2-3）+ `src/mcp/`（P3-1）。
- **实施批次**：P0-1 → P0-2 → P1-1 → P1-2 → P2-1 → P2-3 → P3-1。

## 数据层（config/ + SQLite + ChromaDB）

- **改造**：experience 表加 FTS5 + 混合评分索引（P1-1，新增表不重建）+ working memory 热层表 + 时序三元组表（P1-1）+ skills 评分卡持久化（P1-2）。
- **实施批次**：P1-1 热层/FTS5 → P1-1 时序图 → P1-2 评分卡。

## 接口层（FastAPI routers + SSE）

- **改造**：agent router 加 approval 审批通道（Ask SSE 事件上行用户确认，P0-2）+ session 记忆注入端点（P1-1）+ MCP stdio/HTTP server（P3-1）。
- **实施批次**：P0-2 → P1-1 → P3-1。

## 配置管理（.env + config）

- **改造**：`VAP_AGENT_SUPERVISOR`（P0-1）+ `VAP_ALLOW_WRITE_*` 分级开关 + `VAP_SANDBOX_ENABLED` 接线（P0-2）+ 记忆 TTL/注入上限（P1-1）+ 断路器阈值（P1-3）。

## 日志监控（logging + GCGuard + 黑匣子）

- **改造**：GCGuard 已接入（无需动）+ 黑匣子实时面板 + 诊断导出（P1-3）+ supervisor 事件写入结构化日志（P0-1）。

## 测试策略

- **改造**：`test_supervisor.py`（stuck/压缩/预算）+ `test_scope_guard.py` + `test_agent_framework.py` 扩审批用例 + `test_memory_working.py` + `test_experience_search.py`（FTS5）+ `test_distiller.py` + `test_spectre.py` + `test_roster.py` + `test_media_gen.py` + `test_web_auto.py`。CI 覆盖率门禁维持 80%。
- **依赖**：每批配套最小测试，非全量跑。

## 部署运维

- **改造**：electron-updater 代码落地 + feed 配置（P1-3，待基建）+ 诊断包导出（P1-3）+ MCP server 进程（P3-1）。

---

# 第七部分：实施授权状态

- **当前授权**：ANALYSIS_ONLY（只读分析，不改源码）。
- **已授权动作**：Read recon 摘要 + evidence_check + 主项目关键源码 + 创建本指南一份文件。均已执行。
- **未授权动作**：任何源码修改、依赖变更、git commit、Electron 改造、记忆 schema 扩展、skills 蒸馏实现、MCP server 创建等均未授权。
- **是否进入实施**：否。交付 team-lead 转呈用户，待 Next Gate 确认后才进入。

---

# 第八部分：需补充信息

## 需用户确认的关键决策（按优先级）

1. **P0 排序**：监督层 + 写审批接线是否为本优先（vs 想先做某 P1/P2）？
2. **写审批默认策略**：默认 allow 白名单（只危险写拦截）？还是默认全部写操作审批？
3. **监督层范围**：先 2-3 卡死场景 + flag 默认关是否可接受？
4. **记忆栈**：SQLite 本地优先继续？有无多端/多租户诉求？
5. **付费预算**：视频生成 / IM / remote 真实凭据当前阶段是否授权（预算）？
6. **electron-updater 基建**：是否配置 update server / 签名？

## 需进一步验证的技术点（UNVERIFIED）

1. **记忆混合评分收益**：VAP 经验量小，FTS5 与 LIKE 对比需最小实验。
2. **MIB/向量二值化召回率**：参考声称，未验证。
3. **logistics**：webapp 实际路由数按 **16 页**（Glob 实测）口径核对。
4. **审批 SSE 通道**：agent router 当前无上行确认接口，需要时设计（P0-2）。

## 需补充的参考项目（可选）

- P0-1：精读 OpenHands `stuck.py` + `agent_controller.py` 恢复逻辑。
- P1-1：精读 okf-agent-memory / claude-mem 的热层实现 + graphiti 的图 schema。
- P1-2：精读 nuwa-skill 评分卡 / SkillSpector 检测规则。
- P1-3：精读 nodeterm LogPanel / DSH runtime-controller.mjs / OpenDesign crash-loop。
- P2-1：精读 VideoCaptioner 免费 ASR 链路 / MoneyPrinterTurbo services 分层。

---

*本指南 v2 由 SynthesisArchitect 综合分析子代理产出（2026-09-08）。以 v10.2.0 真实现状（DIRECT 源码直读）为基准，参考 11 簇收敛摘要 + evidence_check 核查，区分 DIRECT/INFERENCE/UNVERIFIED，孤证降权，P0（监督层/审批接线）优先于扩展。旧 v1 指南 P0 中绝大多数已闭环于 v10.0-v10.2，不重复列为待办。不自动进入实施。*