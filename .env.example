# .env — Video Analysis Pro 实际生效的环境变量

> ⚠️ 本文件只列**代码真正会读取**的环境变量（grep 实测，audit-blinds P1-1 修正）。
> 历史版本曾列 `VAP_LLM_PROVIDER/BASE_URL/MODEL/API_KEY`、`VAP_MONITOR_DIR` 等，
> 但代码从未消费——监控分析的真实凭据来源是 GUI 的「LastUsed」配置（见下方说明）。

## 一、headless HTTP 服务

```bash
# 鉴权（生产强制）：非空则 POST /analyze 需 Bearer Token；空=禁用（仅本地用）
# v6.0：启动时检测 <16 字符弹弱 Token 警告（建议 >=32 字符随机串）
VAP_HEADLESS_TOKEN=
# 上传上限 MB（默认 512）
VAP_MAX_UPLOAD_MB=512
# 分析并发上限（默认 1=串行，单 GPU 安全；CPU 环境可调大）
VAP_ANALYZE_CONCURRENCY=1
# v6.0：同 IP 限流（默认 10 req/min，防恶意刷 /analyze；0=禁用）
VAP_IP_RATE_LIMIT_PER_MIN=10
# 服务端口
VAP_PORT=8000
```

## 二、Ollama 本地模型

```bash
# 上下文窗口大小（默认 4096；长视频/长转录调大防截断）
VAP_OLLAMA_NUM_CTX=4096
```

## 二.5、NVIDIA 多 key 路由调优（ProviderRouter）

```bash
# 503/5xx 短退避秒数：worker 池满时同 key 重试前的等待时间
# 默认 1.5s（实测 NVIDIA 503 "Worker 16/16" 频繁，立即切 key 只会让 11 个 key
# 同时撞墙；退避 1.5s 给 worker 池留空槽时间）
VAP_NV_BACKOFF_SEC=1.5

# 同 key 重试次数：503/5xx 时先短退避重试同 key 多少次才切下一个 key
# 默认 2 次（退避 1.5s × 重试 2 次同 key，仍 5xx 才切 key，减少空转）
VAP_NV_SAME_KEY_RETRIES=2

# 每 key 并发上限：单 key 同时在飞的请求数
# 默认 2（11 key 全打满反互挤，每 key 并发 2 更稳；CPU/高配可调大）
VAP_NV_MAX_CONCURRENT_PER_KEY=2
```

> 这三个变量由 `src/core/provider_router.py:load_router_config_from_env`
> 读取，返回 dict 供调用方（`batch_tab._build_router`）传给 `ProviderRouter`。
> 不配则用模块级默认值（1.5 / 2 / 2）。消费位置见下方「代码消费点实测」表。

## 三、监控分析（SurveillanceAgent）— 真实凭据来源说明

监控分析的 LLM 凭据 **不读环境变量**，而是读 GUI 里保存的「LastUsed」配置
（`config/app_config.ini` 的 `[LastUsed]` 段 + keyring 密钥环）。

如需在 headless/脚本场景用监控分析，在 `surveillance_tab.py` 的配置面板填入
api_url / api_key / model 并保存，或直接编辑 `config/app_config.ini`：

```ini
[LastUsed]
client_type = 1
api_url = https://your-gateway/v1
api_key = (实际存于 keyring，不落 ini)
model_name = glm-5.3-flash
```

## 四、代码消费点实测（grep 实证）

| 变量 | 消费位置 |
|------|---------|
| VAP_HEADLESS_TOKEN | src/server/headless.py:182 |
| VAP_MAX_UPLOAD_MB | src/server/headless.py:226 |
| VAP_ANALYZE_CONCURRENCY | src/server/headless.py:38 |
| VAP_IP_RATE_LIMIT_PER_MIN | src/server/headless.py:42 |
| VAP_PORT | src/server/headless.py:293 |
| VAP_OLLAMA_NUM_CTX | src/core/logic.py:830 |
| VAP_NV_BACKOFF_SEC | src/core/provider_router.py:load_router_config_from_env |
| VAP_NV_SAME_KEY_RETRIES | src/core/provider_router.py:load_router_config_from_env |
| VAP_NV_MAX_CONCURRENT_PER_KEY | src/core/provider_router.py:load_router_config_from_env |

> 不在此表的变量（如 `VAP_LLM_API_KEY`、`VAP_RTSP_URL`）代码不读取，
> 配置了也不生效——以本表为准。
